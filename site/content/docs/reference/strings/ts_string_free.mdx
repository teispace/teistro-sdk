---
title: ts_string_free
description: "Frees a string the library allocated and zeroes the descriptor; null or an empty string is ignored."
---

Frees a string the library allocated and zeroes the descriptor; null or an empty string is ignored.

<Tabs items={['C', 'Node', 'Dart']}>
<Tab value="C">

```c
void ts_string_free(ts_string * string);
```

</Tab>
<Tab value="Node">

```js
string_free(…)
```

The addon's generated layer; `@teistro/sdk` wraps it.

</Tab>
<Tab value="Dart">

```dart
library.stringFree(…)
```

The generated `TeistroLibrary`; `package:teistro` wraps it.

</Tab>
</Tabs>

## Parameters

| name | type | role | what it carries |
|---|---|---|---|
| `string` | `ts_string *` | the string being freed | — |

## Safety

`string` must be null or a descriptor written by this library that is not used again.

Declared in `crates/ffi/src/strings.rs`.
