---
title: ts_calendar_from_fixed
description: "The date a fixed day falls on in a calendar."
---

The date a fixed day falls on in a calendar.

<Tabs items={['C', 'Node', 'Dart']}>
<Tab value="C">

```c
ts_status ts_calendar_from_fixed(const ts_context * context, uint16_t calendar, int64_t fixed, ts_calendar_date * out_date);
```

</Tab>
<Tab value="Node">

```js
context.calendar_from_fixed(…)
```

The addon's generated layer; `@teistro/sdk` wraps it.

</Tab>
<Tab value="Dart">

```dart
context.calendarFromFixed(…)
```

The generated `TeistroLibrary`; `package:teistro` wraps it.

</Tab>
</Tabs>

## Parameters

| name | type | role | what it carries |
|---|---|---|---|
| `context` | `const ts_context *` | the handle | — |
| `calendar` | `uint16_t` | a value | One of `ts_calendar`. |
| `fixed` | `int64_t` | a value | — |
| `out_date` | `ts_calendar_date *` | a struct written | — |

## What it hands back

`date`. The call returns a status; a status other than `TS_STATUS_OK` means nothing was written.

## Safety

`context` must be a live handle; `out_date` valid for a read of its `struct_size` and a write.

Declared in `crates/ffi/src/calendar.rs`.
