# Accuracy

Status: `generated`, by `cargo xtask accuracy` from the measurement tests and
`accuracy-rows.yaml`, held by `cargo xtask check-accuracy`; do not edit.

Every area of the astronomy layer (`01-research/platform/13-astronomy-layer.md`) with its conformance
target, what CI measures on every run (the tests under `crates/astro/tests`
against the recorded tables in `fixtures/teimeris/` and `fixtures/baseline/`,
the worst difference over every value compared, and the bound the test holds
it to), the measurements that need the reference engine present and are run
by hand with their date, and the evidence a reader can rerun. A difference
the SDK traced to the engine is in `02-engine-findings.md`; a convention
either side chose is in the cruxes register.

| area | target | measured in CI | by hand | evidence | page |
|---|---|---|---|---|---|
| time scales | exact | — | the conformance kit's `override_dut1`: no engine declares one (2026-09-05) | TT, UT1 and UTC with the IERS leap seconds; two-part Julian dates keep sub-millisecond resolution (`iau::tests::two_part_dates_agree_with_one_part_dates_to_the_resolution_of_a_double`); the 55 fixture charts reproduce the baseline's instants | `03-design/time-and-timezone.md` |
| Delta T | bit-identical per model | — | the kit's `override_delta_t`: Teimeris's and the Swiss Ephemeris's native Delta T within 0.33 s of the table, at its last row (2026-09-05) | the IERS series regenerated from its source and gated (`cargo xtask check-time`); Espenak and Meeus beyond it with Morrison and Stephenson's uncertainties | `03-design/time-and-timezone.md` |
| sidereal time | 1e-9″ | — | against Teimeris's GAST: 0.0013″ around 2000, 0.088″ at 1850 inside its IERS window; its own 1.9″ step at 2050 and 0.1″ step at 1850 are its (`02-engine-findings.md`, F1; 2026-09-05) | `gmst00`, `ee00b`, `gst00b`, `era00` against ERFA's reference values to 1e-12 rad (`iau::tests`) | `03-design/astro-events-and-crossings.md` |
| precession | bit-identical per model | the Vondrák mean obliquity against the engine's: 0″ over 4 values (bound 1.0e-3″) | exercised through the ayanamsha catalogue against Teimeris over 1044 rows from −700 to 2500 (the CI row below), and its mean obliquity to the printed digit at four instants (`teimeris_stars.rs`) | `p06e`, `pfw06`, `fw2m`, `pmat06`, `bp06`, `bi00`, `ltpecl`, `ltpequ`, `ltp`, `ltpb` against ERFA's reference values; the Vondrák obliquity series a microarcsecond from IAU 2006 at J2000.0; every model the identity at J2000.0 and orthogonal | `03-design/astro-timescales-and-frames.md` |
| nutation and obliquity | 1e-9″ | IAU 2000B against the engine's nutation: 4.4e-4″ over 4 values (bound 2.0e-3″) | the kit's `override_obliquity`: Teimeris's and the Swiss Ephemeris's native obliquity within 4.0e-4″ of the SDK's IAU 2006 and 2000B (2026-09-05) | `nut00b` (the 77-term table checked row by row), `obl06`, `obl80`, `numat` against ERFA's reference values | `03-design/astro-timescales-and-frames.md` |
| frame bias and reference frames | bit-identical | — | — | `bp06`'s bias matrix and `bi00` against ERFA; the star table's places over the engine's own astrometry bit for bit on the mean equator of date (the CI row below) | `03-design/astro-star-table.md` |
| position completion | 1e-6″ when fed identical geometric vectors | — | the kit's completion checks: through the native obliquity 2.0e-10″ from the engine's own ecliptic output, through the SDK's obliquity and nutation 3.9e-4″ (Teimeris and the Swiss Ephemeris, 2026-09-05) | the coordinate and zodiac steps; the centre, corrections and equinox steps wait for the built-in ephemeris (Phase 3) | `03-design/ephemeris-port-and-adapters.md` |
| topocentric correction | 1e-6″ | — | — | not built: the completion's centre step is Phase 3's; a topocentric request is answered by a provider that can, as Teimeris does for the baseline's panchanga transitions | `03-design/ephemeris-port-and-adapters.md` |
| coordinate transforms | exact to rounding | — | — | the ecliptic and equatorial rotations, the horizontal transform in the rise and set solver, `refco`'s refraction constants against ERFA | `03-design/astro-events-and-crossings.md` |
| ayanamsha catalogue | epoch-based bit-identical; star-anchored to 0.001″ | anchored members, the same Hipparcos rows and pole: 2.6e-3″ over 68 values (bound 5.0e-3″); anchored members, the two tables' astrometry apart: 1.455″ over 136 values (bound 2″); epoch-defined members stated in TT: 1.3e-7″ over 384 values (bound 1.0e-5″); epoch-defined members stated in Universal Time: 2.1e-4″ over 456 values (bound 5.0e-4″) | — | `teimeris_ayanamsha.rs` over `fixtures/teimeris/ayanamsha.json`: 1044 recorded values of Teimeris 0.1.0 at 24 Julian epochs from −700 to 2500; the anchored members' bounds carry the two tables' astrometry (cruxes C40 to C42) | `03-design/astro-ayanamsha-catalogue.md` |
| star table | 0.01″ | places over the engine's own astrometry: 4.4e-4″ over 1512 values (bound 2.0e-3″); the two catalogues' astrometry apart: 5.012″ over 492 values (bound 10″) | three of the engine's named rows point at other stars or carry stale motions (`02-engine-findings.md`, F5) | `teimeris_stars.rs` over `fixtures/teimeris/stars.json`: 126 stars at four instants, the pipeline over the engine's own astrometry and the two catalogues' astrometry apart; `epv00`, `pmpx`, `ab`, `ld`, `ldsun` against ERFA's reference values | `03-design/astro-star-table.md` |
| house systems | 1e-6° against Teimeris at 10 latitudes, including the polar failure modes | Sunshine cusps against the baseline's topocentric Sun: 0.05° over 14520 values (bound 0.1°); cusps against the baseline's charts beyond 2200: 3.3e-3° over 528 values (bound 5.0e-3°); cusps against the baseline's charts, 1800 to 2200: 2.2e-4° over 13992 values (bound 2.8e-4°); cusps and angles against Teimeris: 4.8e-6° over 25194 values (bound 1.0e-5°) | — | `teimeris_houses.rs` over `fixtures/teimeris/houses.json` (21 systems, 10 latitudes, 2 longitudes, 3 instants) and `baseline_houses.rs` over the 55 baseline charts | `03-design/astro-house-systems.md` |
| rise, set, transit | 1 s against Teimeris | — | the kit's rise and set checks: 0.13 s under the geometric convention and 7.3 s under the almanac's at Reykjavík (C34); the baseline's 52 non-polar charts within 2.5 s below 60° and 9.8 s at Fairbanks (C35) (2026-09-05) | the solver's own tests (a fixed star's quarter days, the polar cases, the conventions in order) | `03-design/astro-events-and-crossings.md` |
| crossings and stations | 1 s against Teimeris | — | the Sun's twelve ingresses within 0.001 s, Mercury's fourteen within 0.004 s, a lunation's tithis within 0.004 s, Mercury's and Mars's stations within 0.3 s of Teimeris's own searches; the baseline's 280 geocentric panchanga transitions within 7.8 s, median 3.3 s, its own search (2026-09-05) | the kernel over the test provider and a synthetic looping planet; the solver's smooth-curve and step-function costs | `03-design/astro-events-and-crossings.md` |
| planetary phenomena | exact to rounding over the same geometry | magnitudes: 1.4e-7 mag over 176 values (bound 2.0e-3 mag); phase angles and elongations over the engine's geometry: 7.3e-12° over 176 values (bound 1.0e-9°) | through the completion over the engine's positions: phase angles within 1.6e-12°, magnitudes within 1.4e-7; the Sun's disc 0.84″ (the engine's older radius) and the Moon's parallax 0.32″ apart (`02-engine-findings.md`, F2) (2026-09-05) | `teimeris_phenomena.rs` over `fixtures/teimeris/pheno.json`: eleven bodies at sixteen instants over the engine's own geometry | `03-design/astro-planetary-phenomena.md` |
| equation of time | exact | from the engine's Sun with the SDK's sidereal time, through 2030: 2.7e-4 s over 16 values (bound 1.0e-3 s) | within 2 µs of Teimeris over every day of the year 2000; 0.127 s apart from 2050, the engine's sidereal-time step (F1) (2026-09-05) | `teimeris_phenomena.rs`: the engine's equation of time from the same Sun with the SDK's sidereal time, at sixteen instants | `03-design/astro-planetary-phenomena.md` |
| eclipses and occultations | v1.x, 1 s of contact times | — | — | not built (v1.x) | `03-design/astro-events-and-crossings.md` |

A measured value is the worst difference over every value the test compares; a
bound is what the test asserts, set from the measurement with its reason in
the test's source. `—` in the CI column: the area's evidence is a reference
test without a recorded engine table, a by-hand measurement, or the area is
not built yet.
