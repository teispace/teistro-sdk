# Design

Status: `draft`, revised 2026-09-05. Per-module detailed designs are
written one page per module before the module's code. Five pages were
written in Phase 0 because their content is retrofit-hostile (ADR-0016,
ADR-0017): the kernels are falsified against the catalogue before any
code exists; the ephemeris port and Teistro Intl pages came from spikes 3
and 4; the five Phase 1 foundation pages (core types and the catalogue,
settings and profiles, time and time zones, the arithmetic calendars,
Bikram Sambat) were written at Phase 1's start, the Surya Siddhanta page
with its crate for the Bikram Sambat engine, and the events page with
the rise and set solver when the port was promoted.

## What a design page contains

1. Purpose and scope, with the research page it derives from.
2. Inputs, settings knobs it reads, ports and capabilities it needs.
3. The data model: types, keys emitted, result shape, provenance.
4. Algorithms with citations, named variants, and the default per profile.
5. The API: C ABI entry points, ergonomic shape, batch forms.
6. Errors and degenerate states.
7. Performance budget and the benchmark that measures it.
8. Tests: unit examples from texts, golden-vector sets, property tests,
   parity coverage.
9. Localisation: namespaces and keys it introduces.
10. Open questions, linked to `QUESTIONS.md`.

## Drafted

| page | what it settles |
|---|---|
| [`exact-arithmetic.md`](exact-arithmetic.md) | nanoarcsecond angles, integer classification, rational periods (ADR-0016) |
| [`dasha-kernels.md`](dasha-kernels.md) | the udu and rashi kernels, 56 systems as rows with confidence marks, the lazy cursor |
| [`varga-kernel.md`](varga-kernel.md) | one table-driven evaluator for every divisional chart and variant: a classifier, one group per group of signs, and each group's spans and map; arbitrary D-N under a stated convention; built (`crates/vargas`) and measured over 19 530 recorded placements |
| [`strength-schemes.md`](strength-schemes.md) | bala schemes with group membership as data, 18 components |
| [`rules-engine.md`](rules-engine.md) | the predicate algebra v2: references, table lookups, classifying outcomes, cancellation |
| [`ephemeris-port-and-adapters.md`](ephemeris-port-and-adapters.md) | positions required and overrides declared (the obliquity, Delta T, the ayanamsha, rise and set), the frame and its bits, columns instants outermost, the C vtable, frame completion by policy, the adapter rules, the kit's fifteen checks and bounds with the measured values of both engines; built |
| [`astro-events-and-crossings.md`](astro-events-and-crossings.md) | the boundary solver and the rise and set solver over ERFA-ported sidereal time and obliquity, the horizon conventions and the event altitude, polar days as reported absences, the measurements against Teimeris and the baseline's fixtures; crossings and stations planned |
| [`intl-engine-and-packs.md`](intl-engine-and-packs.md) | the source conventions, the stable `MessageFormat 2` grammar and the SDK's functions with the types they imply, selection on entities and contexts, the validation gates with the catalogue as authority, the `.tpack` container and the locale bundle, the typed accessors for TypeScript, Dart and Rust, the `teistro-intl` command line; built (`crates/intl`) |
| [`core-types-and-catalogue.md`](core-types-and-catalogue.md) | forty kinds with their keys and ids, the catalogue sources and generator, the three rules for what an attribute is, the quantity newtypes, closed unions per binding, the envelope and the status codes, registries and limits |
| [`settings-and-profiles.md`](settings-and-profiles.md) | the knob inventory as typed groups, profiles as patches over a cited root, resolution and coherence rules, the canonical form and hash, the five shipped profiles, per-request patches |
| [`time-and-timezone.md`](time-and-timezone.md) | scales and instants, Delta T as the IERS table then a model with an uncertainty on every value, the leap-second table, zone resolution with replay-safe metadata and DST policies over the embedded tzdb, local mean time, the sunrise-anchored day with the polar policies, ghati-pala as exact integer arithmetic; built |
| [`calendar-gregorian-julian.md`](calendar-gregorian-julian.md) | the fixed day and the Julian day, the four arithmetic calendars over Reingold and Dershowitz, the mixed transition, ISO weeks, exhaustive and differential tests |
| [`calendar-bikram-sambat.md`](calendar-bikram-sambat.md) | the official table and the computed extension from the SDK's own engine, the month-start rules as rows with the punya-kala rule chosen by measurement (98.5 % of the official month lengths, every New Year), tabular, computed and divergent resolution, the generated table and its gate, era numbers by new-year rules, the source memo |
| [`siddhanta.md`](siddhanta.md) | the Surya Siddhanta as a computation: the text's numbers by verse, exact integer mean places, the sine table, the manda and sighra equations and the four steps, daily motion, precession, declination and the day's arc; the classical path bit-identical everywhere |
| [`astro-timescales-and-frames.md`](astro-timescales-and-frames.md) | the branded time scales, precession as a catalogue of models (Vondrák 2011 the default, IAU 2006, IAU 1976, Newcomb) over the ERFA ports with the obliquity each is consistent with, nutation, the frame bias, the completion steps built and the ones designed (centre, corrections, equinox, topocentric) |
| [`astro-house-systems.md`](astro-house-systems.md) | the twenty-two house systems as one construction with twenty-two choices of circles, the auxiliary points, the sign-based systems in a sidereal zodiac, the polar behaviour of each system and the four policies; within 5e-6° of Teimeris at ten latitudes and 0.0002° of the baseline's 55 charts; built |
| [`chart-bhava-chalit.md`](chart-bhava-chalit.md) | the falsification pass over the four bhava chalit methods, generated by `cargo xtask chalit` and held by `check-chalit`: which one the recording engine actually computes (Vehlow, on all 55 charts, whatever its label says), and how often each pair puts a graha in a different house (21.8% for the two a Jyotisha application chooses between; 50.5% for the two that share their cusps exactly); measured |
| [`chart-foundation.md`](chart-foundation.md) | what every module above the chart starts from: the day an instant belongs to (which is not its civil date), the lagna and the sunrise that anchors it, a house placement that carries the method that produced it with the madhya beside the sandhi, positions in both frames, the birth timing, and the rule that the foundation holds what is needed to compute and never what is computed; draft |
| [`varga-tables-measured.md`](varga-tables-measured.md) | the falsification pass over the recorded divisional charts, generated by `cargo xtask vargas` and held by `check-vargas`: each chart's table derived from the corpus itself, the proposed rule held to it over 19 530 placements and two zodiacs with nothing left over, D30's unequal spans read off the data, what the corpus cannot pin (and why that is the argument for rules over tables), and the vargottama finding; measured |
| [`panchanga-day-conventions.md`](panchanga-day-conventions.md) | the falsification pass over the recorded daily panchanga, generated by `cargo xtask panchanga` and held by `check-panchanga`: a proposed rule for every one of the section's twenty-seven fields, measured over 55 days — the window, the clipped spans and the karana's chain, the proportional divisions, the choghadiya as the hora's walk, Abhijit and Brahma muhurta, the month, panchaka, the muhurta yogas, the Moon's two windows, the geocentric frame, the polar collapse, and the catalogue's own attributes; measured |
| [`panchanga-day.md`](panchanga-day.md) | the almanac of one day at one place: the day is `time`'s `LocalDay` and no new day model; a limb is a span carrying its own bounds as well as the clipped ones; one divider on two arcs serves the eighths, the choghadiya, the horas and the muhurtas; the choghadiya is the hora's walk; the muhurta yogas are intervals and need a rank-1 source; three new knobs (the centre, the Moon's window, the yoga tables) and four new catalogue kinds; built (`crates/panchanga`) |
| [`astro-ayanamsha-catalogue.md`](astro-ayanamsha-catalogue.md) | the forty-seven ayanamshas as definitions (epoch and value, frame, or anchor) with their sources, the construction that carries a value to any date and the fitted-model correction, mean against nutated, custom definitions, the twelve anchored members refused until the star table; every epoch-defined member within 1e-7″ of Teimeris over 1044 rows; built |
| [`astro-star-table.md`](astro-star-table.md) | the star table: 128 catalogue members with SIMBAD astrometry and its bibcodes, the apparent, mean and geometric places over the SDK's own Earth ephemeris and frame bias, the twelve star-anchored ayanamshas; bit for bit with Teimeris on the same astrometry; built |
| [`astro-planetary-phenomena.md`](astro-planetary-phenomena.md) | elongation, phase, the apparent disc and parallax, the visual magnitude under the Astronomical Almanac's models, the equation of time; visibility and the heliacal phenomena under three named criteria (the Surya Siddhanta's degrees of time, the combustion orb, Ptolemy's arcus visionis); built |
| [`state-tables-measured.md`](state-tables-measured.md) | the falsification pass over the recorded planetary state, generated by `cargo xtask state` and held by `check-state`: the dignity ladder and the order its steps must be tried in, the three friendships, the orb table bracketed body by body and motion by motion, the ages' alternation, the war and its victor, the boundary threshold bracketed rather than stated, and the six avasthas the corpus plus a founded chart cannot decide at all; measured over 837 readings |
| [`state-and-avasthas.md`](state-and-avasthas.md) | what a graha is as opposed to where it is: the dignity ladder with moolatrikona above exaltation and a body its own friend in its own sign, the three friendships, combustion as two cited orb tables that part on the deeper orb, the five ages' alternation, the planetary war, the avasthas that are decidable today and the six that wait on `aspect`, and a boundary as a distance rather than a flag; built (`crates/state`) |
| [`aspect-drishti-measured.md`](aspect-drishti-measured.md) | the falsification pass over the drishti, generated by `cargo xtask aspect` and held by `check-aspect`; the first Phase 4 pass with **no recorded answer to measure against**, established by searching every key of all 115 fixtures, so it measures each system's own invariants, the systems against each other over the corpus's 6696 ordered pairs, the node knob's cost, and the avasthas the state pass refused — which it closes halfway, with a necessary condition and no sufficient one; measured |
| [`aspect-and-drishti.md`](aspect-and-drishti.md) | which bodies reach which: the graha drishti as one table with three special grahas and the node's houses as a settings choice, the Jaimini rashi drishti as a mutual relation between signs, the conjunction as co-presence with the orb engine kept beneath it, a mutual full aspect as the seventh or Mars and Saturn, a drishti as a step function that carries its distance to the sign edge, and the sphuta drishti refused for want of a source (C45); built (`crates/aspect`) |
| [`points-measured.md`](points-measured.md) | the falsification pass over the recorded upagrahas and special lagnas, generated by `cargo xtask points` and held by `check-points`: the five the Sun casts and the two Yogi points exact to the last bit, the Sree lagna's fraction settled against three rivals, the three clock-driven lagnas' rates confirmed and their shared clock bracketed, Gulika and Mandi **derived** by trying all twenty-four candidate instants, and the Varnada refused; measured over 71 fixtures |
| [`derived-points.md`](derived-points.md) | points that are not bodies but behave like them: the Sun's five as a chain whose reflections make two of them run backwards, Gulika where Saturn's eighth begins and Mandi where it ends, the special lagnas from the Sun **at birth**, an `Ascendant` trait so the day-division points need no provider, no new settings knob, and the Varnada left to crux C22; built (`crates/points`) |
| [`houses-measured.md`](houses-measured.md) | the falsification pass over the parts of the recorded houses **nothing else reads**, generated by `cargo xtask houses` and held by `check-houses`: the cusp signs and the midheaven, which hold; the degeneracy flag against the SDK's own outcome, which disagrees in *both* directions; the chalit's shift counted the other way as well; and a settings knob the root populates and nothing had ever asked for; measured |
| [`houses-service.md`](houses-service.md) | which house, under which reading, and whether the chart can be trusted: one place to ask which system a module uses, both readings with the bodies that differ between them, an outcome rather than a boolean because a boolean cannot say what happened, a degenerate chart reported rather than refused, and the classifications `strength` and `rules` both need; built (`crates/houses`) |
| [`serial-measured.md`](serial-measured.md) | the falsification pass over the canonical form, generated by `cargo xtask serial` and held by `check-serial`: what a stamped value actually carries (the content hash was the hash of *nothing* on all but one producer), whether the form is canonical at every depth over the corpus's 55 documents, and how a double is written — where Rust's JSON layer and JavaScript's part company at 10⁻⁶; measured |
| [`serial-and-the-envelope.md`](serial-and-the-envelope.md) | one JSON document for a chart and one way of writing it: the seal whose constructor computes the hash a caller kept forgetting, a number grammar with no exponent so a binding needs no float printer, two forms because a hash that moves with a display setting is a worse cache key, and the document that holds every Phase 4 value under one envelope; built (`crates/serial`) |
| [`chart-at-the-boundary.md`](chart-at-the-boundary.md) | the design for `ts_chart_found` and `ts_panchanga_day` and the two result blobs they answer with, so a binding can show a chart: Phase 4's own exit condition. Written from the shape `schema-measured.md` measured — a foundation is 72 distinct paths in twelve groups, only one of which repeats per row — and the part worth arguing is §3, what is reused rather than described again: `zodiac.request` is a `Frame`, so it is the packed `frame_bits` the positions blob already writes and a binding already unpacks; the day's place is the chart's place; and the two shapes that genuinely appear twice are named once |
| [`panchanga-at-the-boundary-measured.md`](panchanga-at-the-boundary-measured.md) | the falsification pass over the shape of a batch of almanacs, generated by `cargo xtask almanac` and held by `check-almanac`: which of a day's fifteen lists a blob can give a stride and which it cannot, measured over 450 days at three latitudes. The split is not the one the names suggest — every fixed list is a **division of an arc** (24 horas, 15+15 muhurtas, 16 choghadiya, 3 kaalas) and every ragged one a **crossing inside the window**, so a polar day whose synthesised arc runs a fortnight still has 24 horas and 122 karanas. A rectangular layout wastes 10.4% over ordinary days and 78.1% once one polar day joins the batch, which settles the layout as ragged; and the pass could not reach a polar day at all until the horizon scan's step cap was sized from its span |
| [`calendar-indian-lunisolar-measured.md`](calendar-indian-lunisolar-measured.md) | the falsification pass over the Indian lunisolar month, generated by `cargo xtask lunisolar` and held by `check-lunisolar`: the rule that decides **adhika** and **kshaya**, measured over 12 368 lunar months of a millennium computed from the Surya Siddhanta's own Sun and Moon. The corpus records `is_adhika` — the answer — and none of the inputs, so unlike the panchanga's conventions this cannot be arithmetic over recorded numbers. The count of sankrantis in the month decides it: none is adhika, one ordinary, two kshaya, and it reproduces all 55 recorded days. An adhika month needs no naming rule of its own, which was not obvious; kshaya is measured and **not** tested, because the corpus records none |
| [`binding-surface-measured.md`](binding-surface-measured.md) | generated by `cargo xtask surface`: what a binding must marshal (every parameter and struct role, and the one with no instance), what it may not spell (each target's reserved words, counted rather than assumed), the layout it must agree with on both targets, the two spellings Python needs for every scalar, and how much `api:` metadata there is to document a value with |
| [`python-binding.md`](python-binding.md) | the third binding: `ctypes` over the same shared library rather than PyO3, branded `float` subclasses instead of `NewType` stubs, `IntEnum` catalogues with `UNKNOWN`, one copy of a result blob and zero-copy `memoryview` columns over it, `CFUNCTYPE` trampolines that cannot let an exception escape, and the sizes a binding with no compiler has to be told; built (`bindings/python`) |
| [`ffi-abi-and-api-description.md`](ffi-abi-and-api-description.md) | the C ABI as built (the size handshake, the last error, owned and lent memory, the panic guard, the JSON settings patch), the `TSRB` result blob and its schemas, the API description with roles inferred from types and names and the `api:` metadata line, the layout rules and the header's assertions, the thirty-three entry points, the gates; built (`crates/ffi`, `crates/idl`) |

## Planned pages (in roadmap order)

| page | phase |
|---|---|
| `calendar-indian-lunisolar.md` | 2 |
| `astro-star-table.md` (crossings and stations: the next revision of `astro-events-and-crossings.md`) | 2 |
| `ephemeris-builtin.md` (theories, tiers, ingestion tool) | 3 |
| the Python binding (the foundation: `chart-foundation.md`; vargas: `varga-kernel.md`; the state: `state-and-avasthas.md`; the drishti: `aspect-and-drishti.md`; the points: `derived-points.md`; the houses: `houses-service.md`; the canonical form: `serial-and-the-envelope.md`) | 4 |
| `strength-ashtakavarga.md`, `strength-bhava-vimshopaka.md` (Shadbala: see `strength-schemes.md`) | 5 |
| `dasha-registry.md` (the registry and plug-in interface over `dasha-kernels.md`) | 5 |
| `rules-yoga-dosha-packs.md` (the engine: `rules-engine.md`) | 6 |
| `interpret-composers.md` | 4 |
| `jaimini.md`, `kp.md`, `tajika.md`, `gochar.md`, `muhurta.md` | 5 |
| `matching.md`, `prashna.md`, `rectification.md`, `longevity.md`, `remedies.md`, `numerology.md`, `lalkitab.md`, `pakshi.md`, `namakarana.md`, `rashifal.md`, `research.md` | 6 |
| `serial-json-dossier-blob.md`, `chart-geometry.md` | 7 |
| `western-*.md`, `hellenistic-*.md` | v1.x |
