//! Generated by `cargo xtask gen catalogue` from `catalogue/modality.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// Movable, fixed and dual signs.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Modality {
    /// Movable
    Chara = 0,
    /// Fixed
    Sthira = 1,
    /// Dual
    Dwiswabhava = 2,
}

const BY_KEY: [(&str, Modality); 3] = [
    ("CHARA", Modality::Chara),
    ("DWISWABHAVA", Modality::Dwiswabhava),
    ("STHIRA", Modality::Sthira),
];

const ALIASES: [(&str, Modality); 0] = [
];

impl Modality {
    /// The kind.
    pub const KIND: Kind = Kind::Modality;

    /// Every member, in id order.
    pub const ALL: [Modality; 3] = [
        Modality::Chara,
        Modality::Sthira,
        Modality::Dwiswabhava,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Modality::Chara => "CHARA",
            Modality::Sthira => "STHIRA",
            Modality::Dwiswabhava => "DWISWABHAVA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Modality::Chara => "modality.CHARA",
            Modality::Sthira => "modality.STHIRA",
            Modality::Dwiswabhava => "modality.DWISWABHAVA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Modality::Chara => "Movable",
            Modality::Sthira => "Fixed",
            Modality::Dwiswabhava => "Dual",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Modality::Chara => None,
            Modality::Sthira => None,
            Modality::Dwiswabhava => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Modality::Chara => Mark::Verified,
            Modality::Sthira => Mark::Verified,
            Modality::Dwiswabhava => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Modality::Chara => false,
            Modality::Sthira => false,
            Modality::Dwiswabhava => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Modality::Chara => &[Source { text: "baseline-engine", reference: "core data" }],
            Modality::Sthira => &[Source { text: "baseline-engine", reference: "core data" }],
            Modality::Dwiswabhava => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Modality::Chara => &[],
            Modality::Sthira => &[],
            Modality::Dwiswabhava => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Modality, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Modality> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Modality> {
        match id {
            0 => Some(Modality::Chara),
            1 => Some(Modality::Sthira),
            2 => Some(Modality::Dwiswabhava),
            _ => None,
        }
    }
}

impl Catalogued for Modality {
    const KIND: Kind = Kind::Modality;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Modality {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Modality {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Modality>(key))
    }
}

impl From<Modality> for KeyId {
    fn from(member: Modality) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Modality {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Modality) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Modality {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Modality {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Modality>(key)))
    }
}
