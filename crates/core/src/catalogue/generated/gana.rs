//! Generated by `cargo xtask gen catalogue` from `catalogue/gana.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The three ganas of the nakshatras.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Gana {
    /// Deva
    Deva = 0,
    /// Manushya
    Manushya = 1,
    /// Rakshasa
    Rakshasa = 2,
}

const BY_KEY: [(&str, Gana); 3] = [
    ("DEVA", Gana::Deva),
    ("MANUSHYA", Gana::Manushya),
    ("RAKSHASA", Gana::Rakshasa),
];

const ALIASES: [(&str, Gana); 0] = [
];

impl Gana {
    /// The kind.
    pub const KIND: Kind = Kind::Gana;

    /// Every member, in id order.
    pub const ALL: [Gana; 3] = [
        Gana::Deva,
        Gana::Manushya,
        Gana::Rakshasa,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Gana::Deva => "DEVA",
            Gana::Manushya => "MANUSHYA",
            Gana::Rakshasa => "RAKSHASA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Gana::Deva => "gana.DEVA",
            Gana::Manushya => "gana.MANUSHYA",
            Gana::Rakshasa => "gana.RAKSHASA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Gana::Deva => "Deva",
            Gana::Manushya => "Manushya",
            Gana::Rakshasa => "Rakshasa",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Gana::Deva => None,
            Gana::Manushya => None,
            Gana::Rakshasa => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Gana::Deva => Mark::Verified,
            Gana::Manushya => Mark::Verified,
            Gana::Rakshasa => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Gana::Deva => false,
            Gana::Manushya => false,
            Gana::Rakshasa => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Gana::Deva => &[Source { text: "baseline-engine", reference: "core data" }],
            Gana::Manushya => &[Source { text: "baseline-engine", reference: "core data" }],
            Gana::Rakshasa => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Gana::Deva => &[],
            Gana::Manushya => &[],
            Gana::Rakshasa => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Gana, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Gana> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Gana> {
        match id {
            0 => Some(Gana::Deva),
            1 => Some(Gana::Manushya),
            2 => Some(Gana::Rakshasa),
            _ => None,
        }
    }
}

impl Catalogued for Gana {
    const KIND: Kind = Kind::Gana;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Gana {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Gana {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Gana>(key))
    }
}

impl From<Gana> for KeyId {
    fn from(member: Gana) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Gana {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Gana) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Gana {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Gana {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Gana>(key)))
    }
}
