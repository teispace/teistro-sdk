//! Generated by `cargo xtask gen catalogue` from `catalogue/star_class.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// What kind of object a star-table member is.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum StarClass {
    /// A star
    Star = 0,
    /// A compact radio source, such as the galactic centre
    RadioSource = 1,
    /// A fixed direction on the sky without a body, such as a galactic pole
    Direction = 2,
}

const BY_KEY: [(&str, StarClass); 3] = [
    ("DIRECTION", StarClass::Direction),
    ("RADIO_SOURCE", StarClass::RadioSource),
    ("STAR", StarClass::Star),
];

const ALIASES: [(&str, StarClass); 0] = [
];

impl StarClass {
    /// The kind.
    pub const KIND: Kind = Kind::StarClass;

    /// Every member, in id order.
    pub const ALL: [StarClass; 3] = [
        StarClass::Star,
        StarClass::RadioSource,
        StarClass::Direction,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            StarClass::Star => "STAR",
            StarClass::RadioSource => "RADIO_SOURCE",
            StarClass::Direction => "DIRECTION",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            StarClass::Star => "star_class.STAR",
            StarClass::RadioSource => "star_class.RADIO_SOURCE",
            StarClass::Direction => "star_class.DIRECTION",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            StarClass::Star => "A star",
            StarClass::RadioSource => "A compact radio source, such as the galactic centre",
            StarClass::Direction => "A fixed direction on the sky without a body, such as a galactic pole",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            StarClass::Star => None,
            StarClass::RadioSource => None,
            StarClass::Direction => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            StarClass::Star => Mark::Verified,
            StarClass::RadioSource => Mark::Verified,
            StarClass::Direction => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            StarClass::Star => false,
            StarClass::RadioSource => false,
            StarClass::Direction => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            StarClass::Star => &[Source { text: "structural", reference: "design" }],
            StarClass::RadioSource => &[Source { text: "structural", reference: "design" }],
            StarClass::Direction => &[Source { text: "structural", reference: "design" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            StarClass::Star => &[],
            StarClass::RadioSource => &[],
            StarClass::Direction => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::StarClass, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<StarClass> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<StarClass> {
        match id {
            0 => Some(StarClass::Star),
            1 => Some(StarClass::RadioSource),
            2 => Some(StarClass::Direction),
            _ => None,
        }
    }
}

impl Catalogued for StarClass {
    const KIND: Kind = Kind::StarClass;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for StarClass {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for StarClass {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<StarClass>(key))
    }
}

impl From<StarClass> for KeyId {
    fn from(member: StarClass) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for StarClass {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::StarClass) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for StarClass {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for StarClass {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<StarClass>(key)))
    }
}
