//! Generated by `cargo xtask gen catalogue` from `catalogue/point_family.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// What kind of derived point.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum PointFamily {
    /// Angle
    Angle = 0,
    /// Special Lagna
    SpecialLagna = 1,
    /// Upagraha Solar
    UpagrahaSolar = 2,
    /// Upagraha Day
    UpagrahaDay = 3,
    /// Arudha
    Arudha = 4,
    /// Sphuta
    Sphuta = 5,
    /// Jaimini
    Jaimini = 6,
    /// Midpoint
    Midpoint = 7,
}

const BY_KEY: [(&str, PointFamily); 8] = [
    ("ANGLE", PointFamily::Angle),
    ("ARUDHA", PointFamily::Arudha),
    ("JAIMINI", PointFamily::Jaimini),
    ("MIDPOINT", PointFamily::Midpoint),
    ("SPECIAL_LAGNA", PointFamily::SpecialLagna),
    ("SPHUTA", PointFamily::Sphuta),
    ("UPAGRAHA_DAY", PointFamily::UpagrahaDay),
    ("UPAGRAHA_SOLAR", PointFamily::UpagrahaSolar),
];

const ALIASES: [(&str, PointFamily); 0] = [
];

impl PointFamily {
    /// The kind.
    pub const KIND: Kind = Kind::PointFamily;

    /// Every member, in id order.
    pub const ALL: [PointFamily; 8] = [
        PointFamily::Angle,
        PointFamily::SpecialLagna,
        PointFamily::UpagrahaSolar,
        PointFamily::UpagrahaDay,
        PointFamily::Arudha,
        PointFamily::Sphuta,
        PointFamily::Jaimini,
        PointFamily::Midpoint,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            PointFamily::Angle => "ANGLE",
            PointFamily::SpecialLagna => "SPECIAL_LAGNA",
            PointFamily::UpagrahaSolar => "UPAGRAHA_SOLAR",
            PointFamily::UpagrahaDay => "UPAGRAHA_DAY",
            PointFamily::Arudha => "ARUDHA",
            PointFamily::Sphuta => "SPHUTA",
            PointFamily::Jaimini => "JAIMINI",
            PointFamily::Midpoint => "MIDPOINT",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            PointFamily::Angle => "point_family.ANGLE",
            PointFamily::SpecialLagna => "point_family.SPECIAL_LAGNA",
            PointFamily::UpagrahaSolar => "point_family.UPAGRAHA_SOLAR",
            PointFamily::UpagrahaDay => "point_family.UPAGRAHA_DAY",
            PointFamily::Arudha => "point_family.ARUDHA",
            PointFamily::Sphuta => "point_family.SPHUTA",
            PointFamily::Jaimini => "point_family.JAIMINI",
            PointFamily::Midpoint => "point_family.MIDPOINT",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            PointFamily::Angle => "Angle",
            PointFamily::SpecialLagna => "Special Lagna",
            PointFamily::UpagrahaSolar => "Upagraha Solar",
            PointFamily::UpagrahaDay => "Upagraha Day",
            PointFamily::Arudha => "Arudha",
            PointFamily::Sphuta => "Sphuta",
            PointFamily::Jaimini => "Jaimini",
            PointFamily::Midpoint => "Midpoint",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            PointFamily::Angle => None,
            PointFamily::SpecialLagna => None,
            PointFamily::UpagrahaSolar => None,
            PointFamily::UpagrahaDay => None,
            PointFamily::Arudha => None,
            PointFamily::Sphuta => None,
            PointFamily::Jaimini => None,
            PointFamily::Midpoint => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            PointFamily::Angle => Mark::Verified,
            PointFamily::SpecialLagna => Mark::Verified,
            PointFamily::UpagrahaSolar => Mark::Verified,
            PointFamily::UpagrahaDay => Mark::Verified,
            PointFamily::Arudha => Mark::Verified,
            PointFamily::Sphuta => Mark::Verified,
            PointFamily::Jaimini => Mark::Verified,
            PointFamily::Midpoint => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            PointFamily::Angle => false,
            PointFamily::SpecialLagna => false,
            PointFamily::UpagrahaSolar => false,
            PointFamily::UpagrahaDay => false,
            PointFamily::Arudha => false,
            PointFamily::Sphuta => false,
            PointFamily::Jaimini => false,
            PointFamily::Midpoint => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            PointFamily::Angle => &[Source { text: "structural", reference: "design" }],
            PointFamily::SpecialLagna => &[Source { text: "structural", reference: "design" }],
            PointFamily::UpagrahaSolar => &[Source { text: "structural", reference: "design" }],
            PointFamily::UpagrahaDay => &[Source { text: "structural", reference: "design" }],
            PointFamily::Arudha => &[Source { text: "structural", reference: "design" }],
            PointFamily::Sphuta => &[Source { text: "structural", reference: "design" }],
            PointFamily::Jaimini => &[Source { text: "structural", reference: "design" }],
            PointFamily::Midpoint => &[Source { text: "structural", reference: "design" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            PointFamily::Angle => &[],
            PointFamily::SpecialLagna => &[],
            PointFamily::UpagrahaSolar => &[],
            PointFamily::UpagrahaDay => &[],
            PointFamily::Arudha => &[],
            PointFamily::Sphuta => &[],
            PointFamily::Jaimini => &[],
            PointFamily::Midpoint => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::PointFamily, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<PointFamily> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<PointFamily> {
        match id {
            0 => Some(PointFamily::Angle),
            1 => Some(PointFamily::SpecialLagna),
            2 => Some(PointFamily::UpagrahaSolar),
            3 => Some(PointFamily::UpagrahaDay),
            4 => Some(PointFamily::Arudha),
            5 => Some(PointFamily::Sphuta),
            6 => Some(PointFamily::Jaimini),
            7 => Some(PointFamily::Midpoint),
            _ => None,
        }
    }
}

impl Catalogued for PointFamily {
    const KIND: Kind = Kind::PointFamily;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for PointFamily {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for PointFamily {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<PointFamily>(key))
    }
}

impl From<PointFamily> for KeyId {
    fn from(member: PointFamily) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for PointFamily {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::PointFamily) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for PointFamily {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for PointFamily {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<PointFamily>(key)))
    }
}
