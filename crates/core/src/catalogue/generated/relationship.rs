//! Generated by `cargo xtask gen catalogue` from `catalogue/relationship.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The five-fold compound relationship.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Relationship {
    /// Great Friend
    GreatFriend = 0,
    /// Friend
    Friend = 1,
    /// Neutral
    Neutral = 2,
    /// Enemy
    Enemy = 3,
    /// Great Enemy
    GreatEnemy = 4,
}

const BY_KEY: [(&str, Relationship); 5] = [
    ("ENEMY", Relationship::Enemy),
    ("FRIEND", Relationship::Friend),
    ("GREAT_ENEMY", Relationship::GreatEnemy),
    ("GREAT_FRIEND", Relationship::GreatFriend),
    ("NEUTRAL", Relationship::Neutral),
];

const ALIASES: [(&str, Relationship); 0] = [
];

impl Relationship {
    /// The kind.
    pub const KIND: Kind = Kind::Relationship;

    /// Every member, in id order.
    pub const ALL: [Relationship; 5] = [
        Relationship::GreatFriend,
        Relationship::Friend,
        Relationship::Neutral,
        Relationship::Enemy,
        Relationship::GreatEnemy,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Relationship::GreatFriend => "GREAT_FRIEND",
            Relationship::Friend => "FRIEND",
            Relationship::Neutral => "NEUTRAL",
            Relationship::Enemy => "ENEMY",
            Relationship::GreatEnemy => "GREAT_ENEMY",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Relationship::GreatFriend => "relationship.GREAT_FRIEND",
            Relationship::Friend => "relationship.FRIEND",
            Relationship::Neutral => "relationship.NEUTRAL",
            Relationship::Enemy => "relationship.ENEMY",
            Relationship::GreatEnemy => "relationship.GREAT_ENEMY",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Relationship::GreatFriend => "Great Friend",
            Relationship::Friend => "Friend",
            Relationship::Neutral => "Neutral",
            Relationship::Enemy => "Enemy",
            Relationship::GreatEnemy => "Great Enemy",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Relationship::GreatFriend => None,
            Relationship::Friend => None,
            Relationship::Neutral => None,
            Relationship::Enemy => None,
            Relationship::GreatEnemy => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Relationship::GreatFriend => Mark::Verified,
            Relationship::Friend => Mark::Verified,
            Relationship::Neutral => Mark::Verified,
            Relationship::Enemy => Mark::Verified,
            Relationship::GreatEnemy => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Relationship::GreatFriend => false,
            Relationship::Friend => false,
            Relationship::Neutral => false,
            Relationship::Enemy => false,
            Relationship::GreatEnemy => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Relationship::GreatFriend => &[Source { text: "baseline-engine", reference: "core data" }],
            Relationship::Friend => &[Source { text: "baseline-engine", reference: "core data" }],
            Relationship::Neutral => &[Source { text: "baseline-engine", reference: "core data" }],
            Relationship::Enemy => &[Source { text: "baseline-engine", reference: "core data" }],
            Relationship::GreatEnemy => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Relationship::GreatFriend => &[],
            Relationship::Friend => &[],
            Relationship::Neutral => &[],
            Relationship::Enemy => &[],
            Relationship::GreatEnemy => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Relationship, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Relationship> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Relationship> {
        match id {
            0 => Some(Relationship::GreatFriend),
            1 => Some(Relationship::Friend),
            2 => Some(Relationship::Neutral),
            3 => Some(Relationship::Enemy),
            4 => Some(Relationship::GreatEnemy),
            _ => None,
        }
    }
}

impl Catalogued for Relationship {
    const KIND: Kind = Kind::Relationship;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Relationship {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Relationship {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Relationship>(key))
    }
}

impl From<Relationship> for KeyId {
    fn from(member: Relationship) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Relationship {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Relationship) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Relationship {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Relationship {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Relationship>(key)))
    }
}
