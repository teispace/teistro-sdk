//! Generated by `cargo xtask gen catalogue` from `catalogue/avastha_baladi.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The five age states.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum AvasthaBaladi {
    /// Bala
    Bala = 0,
    /// Kumara
    Kumara = 1,
    /// Yuva
    Yuva = 2,
    /// Vriddha
    Vriddha = 3,
    /// Mrita
    Mrita = 4,
}

const BY_KEY: [(&str, AvasthaBaladi); 5] = [
    ("BALA", AvasthaBaladi::Bala),
    ("KUMARA", AvasthaBaladi::Kumara),
    ("MRITA", AvasthaBaladi::Mrita),
    ("VRIDDHA", AvasthaBaladi::Vriddha),
    ("YUVA", AvasthaBaladi::Yuva),
];

const ALIASES: [(&str, AvasthaBaladi); 0] = [
];

impl AvasthaBaladi {
    /// The kind.
    pub const KIND: Kind = Kind::AvasthaBaladi;

    /// Every member, in id order.
    pub const ALL: [AvasthaBaladi; 5] = [
        AvasthaBaladi::Bala,
        AvasthaBaladi::Kumara,
        AvasthaBaladi::Yuva,
        AvasthaBaladi::Vriddha,
        AvasthaBaladi::Mrita,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            AvasthaBaladi::Bala => "BALA",
            AvasthaBaladi::Kumara => "KUMARA",
            AvasthaBaladi::Yuva => "YUVA",
            AvasthaBaladi::Vriddha => "VRIDDHA",
            AvasthaBaladi::Mrita => "MRITA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            AvasthaBaladi::Bala => "avastha_baladi.BALA",
            AvasthaBaladi::Kumara => "avastha_baladi.KUMARA",
            AvasthaBaladi::Yuva => "avastha_baladi.YUVA",
            AvasthaBaladi::Vriddha => "avastha_baladi.VRIDDHA",
            AvasthaBaladi::Mrita => "avastha_baladi.MRITA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            AvasthaBaladi::Bala => "Bala",
            AvasthaBaladi::Kumara => "Kumara",
            AvasthaBaladi::Yuva => "Yuva",
            AvasthaBaladi::Vriddha => "Vriddha",
            AvasthaBaladi::Mrita => "Mrita",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            AvasthaBaladi::Bala => None,
            AvasthaBaladi::Kumara => None,
            AvasthaBaladi::Yuva => None,
            AvasthaBaladi::Vriddha => None,
            AvasthaBaladi::Mrita => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            AvasthaBaladi::Bala => Mark::Verified,
            AvasthaBaladi::Kumara => Mark::Verified,
            AvasthaBaladi::Yuva => Mark::Verified,
            AvasthaBaladi::Vriddha => Mark::Verified,
            AvasthaBaladi::Mrita => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            AvasthaBaladi::Bala => false,
            AvasthaBaladi::Kumara => false,
            AvasthaBaladi::Yuva => false,
            AvasthaBaladi::Vriddha => false,
            AvasthaBaladi::Mrita => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            AvasthaBaladi::Bala => &[Source { text: "baseline-engine", reference: "core data" }],
            AvasthaBaladi::Kumara => &[Source { text: "baseline-engine", reference: "core data" }],
            AvasthaBaladi::Yuva => &[Source { text: "baseline-engine", reference: "core data" }],
            AvasthaBaladi::Vriddha => &[Source { text: "baseline-engine", reference: "core data" }],
            AvasthaBaladi::Mrita => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            AvasthaBaladi::Bala => &[],
            AvasthaBaladi::Kumara => &[],
            AvasthaBaladi::Yuva => &[],
            AvasthaBaladi::Vriddha => &[],
            AvasthaBaladi::Mrita => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::AvasthaBaladi, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<AvasthaBaladi> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<AvasthaBaladi> {
        match id {
            0 => Some(AvasthaBaladi::Bala),
            1 => Some(AvasthaBaladi::Kumara),
            2 => Some(AvasthaBaladi::Yuva),
            3 => Some(AvasthaBaladi::Vriddha),
            4 => Some(AvasthaBaladi::Mrita),
            _ => None,
        }
    }
}

impl Catalogued for AvasthaBaladi {
    const KIND: Kind = Kind::AvasthaBaladi;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for AvasthaBaladi {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for AvasthaBaladi {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<AvasthaBaladi>(key))
    }
}

impl From<AvasthaBaladi> for KeyId {
    fn from(member: AvasthaBaladi) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for AvasthaBaladi {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::AvasthaBaladi) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for AvasthaBaladi {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for AvasthaBaladi {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<AvasthaBaladi>(key)))
    }
}
