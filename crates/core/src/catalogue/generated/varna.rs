//! Generated by `cargo xtask gen catalogue` from `catalogue/varna.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The varna classification of nakshatras and grahas.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Varna {
    /// Brahmin
    Brahmin = 0,
    /// Kshatriya
    Kshatriya = 1,
    /// Vaishya
    Vaishya = 2,
    /// Shudra
    Shudra = 3,
    /// Mleccha
    Mleccha = 4,
}

const BY_KEY: [(&str, Varna); 5] = [
    ("BRAHMIN", Varna::Brahmin),
    ("KSHATRIYA", Varna::Kshatriya),
    ("MLECCHA", Varna::Mleccha),
    ("SHUDRA", Varna::Shudra),
    ("VAISHYA", Varna::Vaishya),
];

const ALIASES: [(&str, Varna); 0] = [
];

impl Varna {
    /// The kind.
    pub const KIND: Kind = Kind::Varna;

    /// Every member, in id order.
    pub const ALL: [Varna; 5] = [
        Varna::Brahmin,
        Varna::Kshatriya,
        Varna::Vaishya,
        Varna::Shudra,
        Varna::Mleccha,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Varna::Brahmin => "BRAHMIN",
            Varna::Kshatriya => "KSHATRIYA",
            Varna::Vaishya => "VAISHYA",
            Varna::Shudra => "SHUDRA",
            Varna::Mleccha => "MLECCHA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Varna::Brahmin => "varna.BRAHMIN",
            Varna::Kshatriya => "varna.KSHATRIYA",
            Varna::Vaishya => "varna.VAISHYA",
            Varna::Shudra => "varna.SHUDRA",
            Varna::Mleccha => "varna.MLECCHA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Varna::Brahmin => "Brahmin",
            Varna::Kshatriya => "Kshatriya",
            Varna::Vaishya => "Vaishya",
            Varna::Shudra => "Shudra",
            Varna::Mleccha => "Mleccha",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Varna::Brahmin => None,
            Varna::Kshatriya => None,
            Varna::Vaishya => None,
            Varna::Shudra => None,
            Varna::Mleccha => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Varna::Brahmin => Mark::Verified,
            Varna::Kshatriya => Mark::Verified,
            Varna::Vaishya => Mark::Verified,
            Varna::Shudra => Mark::Verified,
            Varna::Mleccha => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Varna::Brahmin => false,
            Varna::Kshatriya => false,
            Varna::Vaishya => false,
            Varna::Shudra => false,
            Varna::Mleccha => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Varna::Brahmin => &[Source { text: "baseline-engine", reference: "core data" }],
            Varna::Kshatriya => &[Source { text: "baseline-engine", reference: "core data" }],
            Varna::Vaishya => &[Source { text: "baseline-engine", reference: "core data" }],
            Varna::Shudra => &[Source { text: "baseline-engine", reference: "core data" }],
            Varna::Mleccha => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Varna::Brahmin => &[],
            Varna::Kshatriya => &[],
            Varna::Vaishya => &[],
            Varna::Shudra => &[],
            Varna::Mleccha => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Varna, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Varna> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Varna> {
        match id {
            0 => Some(Varna::Brahmin),
            1 => Some(Varna::Kshatriya),
            2 => Some(Varna::Vaishya),
            3 => Some(Varna::Shudra),
            4 => Some(Varna::Mleccha),
            _ => None,
        }
    }
}

impl Catalogued for Varna {
    const KIND: Kind = Kind::Varna;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Varna {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Varna {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Varna>(key))
    }
}

impl From<Varna> for KeyId {
    fn from(member: Varna) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Varna {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Varna) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Varna {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Varna {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Varna>(key)))
    }
}
