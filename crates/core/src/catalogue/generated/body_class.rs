//! Generated by `cargo xtask gen catalogue` from `catalogue/body_class.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// What kind of body a graha is.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum BodyClass {
    /// Luminary
    Luminary = 0,
    /// Planet
    Planet = 1,
    /// Node
    Node = 2,
    /// Outer
    Outer = 3,
}

const BY_KEY: [(&str, BodyClass); 4] = [
    ("LUMINARY", BodyClass::Luminary),
    ("NODE", BodyClass::Node),
    ("OUTER", BodyClass::Outer),
    ("PLANET", BodyClass::Planet),
];

const ALIASES: [(&str, BodyClass); 0] = [
];

impl BodyClass {
    /// The kind.
    pub const KIND: Kind = Kind::BodyClass;

    /// Every member, in id order.
    pub const ALL: [BodyClass; 4] = [
        BodyClass::Luminary,
        BodyClass::Planet,
        BodyClass::Node,
        BodyClass::Outer,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            BodyClass::Luminary => "LUMINARY",
            BodyClass::Planet => "PLANET",
            BodyClass::Node => "NODE",
            BodyClass::Outer => "OUTER",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            BodyClass::Luminary => "body_class.LUMINARY",
            BodyClass::Planet => "body_class.PLANET",
            BodyClass::Node => "body_class.NODE",
            BodyClass::Outer => "body_class.OUTER",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            BodyClass::Luminary => "Luminary",
            BodyClass::Planet => "Planet",
            BodyClass::Node => "Node",
            BodyClass::Outer => "Outer",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            BodyClass::Luminary => None,
            BodyClass::Planet => None,
            BodyClass::Node => None,
            BodyClass::Outer => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            BodyClass::Luminary => Mark::Verified,
            BodyClass::Planet => Mark::Verified,
            BodyClass::Node => Mark::Verified,
            BodyClass::Outer => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            BodyClass::Luminary => false,
            BodyClass::Planet => false,
            BodyClass::Node => false,
            BodyClass::Outer => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            BodyClass::Luminary => &[Source { text: "structural", reference: "design" }],
            BodyClass::Planet => &[Source { text: "structural", reference: "design" }],
            BodyClass::Node => &[Source { text: "structural", reference: "design" }],
            BodyClass::Outer => &[Source { text: "structural", reference: "design" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            BodyClass::Luminary => &[],
            BodyClass::Planet => &[],
            BodyClass::Node => &[],
            BodyClass::Outer => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::BodyClass, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<BodyClass> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<BodyClass> {
        match id {
            0 => Some(BodyClass::Luminary),
            1 => Some(BodyClass::Planet),
            2 => Some(BodyClass::Node),
            3 => Some(BodyClass::Outer),
            _ => None,
        }
    }
}

impl Catalogued for BodyClass {
    const KIND: Kind = Kind::BodyClass;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for BodyClass {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for BodyClass {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<BodyClass>(key))
    }
}

impl From<BodyClass> for KeyId {
    fn from(member: BodyClass) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for BodyClass {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::BodyClass) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for BodyClass {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for BodyClass {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<BodyClass>(key)))
    }
}
