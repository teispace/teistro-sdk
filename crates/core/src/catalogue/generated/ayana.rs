//! Generated by `cargo xtask gen catalogue` from `catalogue/ayana.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The two halves of the solar year.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Ayana {
    /// Uttarayana
    Uttarayana = 0,
    /// Dakshinayana
    Dakshinayana = 1,
}

const BY_KEY: [(&str, Ayana); 2] = [
    ("DAKSHINAYANA", Ayana::Dakshinayana),
    ("UTTARAYANA", Ayana::Uttarayana),
];

const ALIASES: [(&str, Ayana); 0] = [
];

impl Ayana {
    /// The kind.
    pub const KIND: Kind = Kind::Ayana;

    /// Every member, in id order.
    pub const ALL: [Ayana; 2] = [
        Ayana::Uttarayana,
        Ayana::Dakshinayana,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Ayana::Uttarayana => "UTTARAYANA",
            Ayana::Dakshinayana => "DAKSHINAYANA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Ayana::Uttarayana => "ayana.UTTARAYANA",
            Ayana::Dakshinayana => "ayana.DAKSHINAYANA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Ayana::Uttarayana => "Uttarayana",
            Ayana::Dakshinayana => "Dakshinayana",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Ayana::Uttarayana => None,
            Ayana::Dakshinayana => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Ayana::Uttarayana => Mark::Verified,
            Ayana::Dakshinayana => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Ayana::Uttarayana => false,
            Ayana::Dakshinayana => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Ayana::Uttarayana => &[Source { text: "baseline-engine", reference: "core data" }],
            Ayana::Dakshinayana => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Ayana::Uttarayana => &[],
            Ayana::Dakshinayana => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Ayana, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Ayana> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Ayana> {
        match id {
            0 => Some(Ayana::Uttarayana),
            1 => Some(Ayana::Dakshinayana),
            _ => None,
        }
    }
}

impl Catalogued for Ayana {
    const KIND: Kind = Kind::Ayana;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Ayana {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Ayana {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Ayana>(key))
    }
}

impl From<Ayana> for KeyId {
    fn from(member: Ayana) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Ayana {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Ayana) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Ayana {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Ayana {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Ayana>(key)))
    }
}
