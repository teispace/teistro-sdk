//! Generated by `cargo xtask gen catalogue` from `catalogue/rising.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// How a sign rises: head first, back first, or both.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Rising {
    /// Sirshodaya
    Sirshodaya = 0,
    /// Prishtodaya
    Prishtodaya = 1,
    /// Ubhayodaya
    Ubhayodaya = 2,
}

const BY_KEY: [(&str, Rising); 3] = [
    ("PRISHTODAYA", Rising::Prishtodaya),
    ("SIRSHODAYA", Rising::Sirshodaya),
    ("UBHAYODAYA", Rising::Ubhayodaya),
];

const ALIASES: [(&str, Rising); 0] = [
];

impl Rising {
    /// The kind.
    pub const KIND: Kind = Kind::Rising;

    /// Every member, in id order.
    pub const ALL: [Rising; 3] = [
        Rising::Sirshodaya,
        Rising::Prishtodaya,
        Rising::Ubhayodaya,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Rising::Sirshodaya => "SIRSHODAYA",
            Rising::Prishtodaya => "PRISHTODAYA",
            Rising::Ubhayodaya => "UBHAYODAYA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Rising::Sirshodaya => "rising.SIRSHODAYA",
            Rising::Prishtodaya => "rising.PRISHTODAYA",
            Rising::Ubhayodaya => "rising.UBHAYODAYA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Rising::Sirshodaya => "Sirshodaya",
            Rising::Prishtodaya => "Prishtodaya",
            Rising::Ubhayodaya => "Ubhayodaya",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Rising::Sirshodaya => None,
            Rising::Prishtodaya => None,
            Rising::Ubhayodaya => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Rising::Sirshodaya => Mark::Traditional,
            Rising::Prishtodaya => Mark::Traditional,
            Rising::Ubhayodaya => Mark::Traditional,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Rising::Sirshodaya => false,
            Rising::Prishtodaya => false,
            Rising::Ubhayodaya => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Rising::Sirshodaya => &[Source { text: "BPHS", reference: "4" }],
            Rising::Prishtodaya => &[Source { text: "BPHS", reference: "4" }],
            Rising::Ubhayodaya => &[Source { text: "BPHS", reference: "4" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Rising::Sirshodaya => &[],
            Rising::Prishtodaya => &[],
            Rising::Ubhayodaya => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Rising, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Rising> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Rising> {
        match id {
            0 => Some(Rising::Sirshodaya),
            1 => Some(Rising::Prishtodaya),
            2 => Some(Rising::Ubhayodaya),
            _ => None,
        }
    }
}

impl Catalogued for Rising {
    const KIND: Kind = Kind::Rising;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Rising {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Rising {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Rising>(key))
    }
}

impl From<Rising> for KeyId {
    fn from(member: Rising) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Rising {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Rising) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Rising {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Rising {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Rising>(key)))
    }
}
