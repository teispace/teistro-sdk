//! Generated by `cargo xtask gen catalogue` from `catalogue/tatwa.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The five elements.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Tatwa {
    /// Fire
    Agni = 0,
    /// Earth
    Prithvi = 1,
    /// Air
    Vayu = 2,
    /// Water
    Jala = 3,
    /// Ether
    Akasha = 4,
}

const BY_KEY: [(&str, Tatwa); 5] = [
    ("AGNI", Tatwa::Agni),
    ("AKASHA", Tatwa::Akasha),
    ("JALA", Tatwa::Jala),
    ("PRITHVI", Tatwa::Prithvi),
    ("VAYU", Tatwa::Vayu),
];

const ALIASES: [(&str, Tatwa); 0] = [
];

impl Tatwa {
    /// The kind.
    pub const KIND: Kind = Kind::Tatwa;

    /// Every member, in id order.
    pub const ALL: [Tatwa; 5] = [
        Tatwa::Agni,
        Tatwa::Prithvi,
        Tatwa::Vayu,
        Tatwa::Jala,
        Tatwa::Akasha,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Tatwa::Agni => "AGNI",
            Tatwa::Prithvi => "PRITHVI",
            Tatwa::Vayu => "VAYU",
            Tatwa::Jala => "JALA",
            Tatwa::Akasha => "AKASHA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Tatwa::Agni => "tatwa.AGNI",
            Tatwa::Prithvi => "tatwa.PRITHVI",
            Tatwa::Vayu => "tatwa.VAYU",
            Tatwa::Jala => "tatwa.JALA",
            Tatwa::Akasha => "tatwa.AKASHA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Tatwa::Agni => "Fire",
            Tatwa::Prithvi => "Earth",
            Tatwa::Vayu => "Air",
            Tatwa::Jala => "Water",
            Tatwa::Akasha => "Ether",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Tatwa::Agni => None,
            Tatwa::Prithvi => None,
            Tatwa::Vayu => None,
            Tatwa::Jala => None,
            Tatwa::Akasha => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Tatwa::Agni => Mark::Verified,
            Tatwa::Prithvi => Mark::Verified,
            Tatwa::Vayu => Mark::Verified,
            Tatwa::Jala => Mark::Verified,
            Tatwa::Akasha => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Tatwa::Agni => false,
            Tatwa::Prithvi => false,
            Tatwa::Vayu => false,
            Tatwa::Jala => false,
            Tatwa::Akasha => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Tatwa::Agni => &[Source { text: "baseline-engine", reference: "core data" }],
            Tatwa::Prithvi => &[Source { text: "baseline-engine", reference: "core data" }],
            Tatwa::Vayu => &[Source { text: "baseline-engine", reference: "core data" }],
            Tatwa::Jala => &[Source { text: "baseline-engine", reference: "core data" }],
            Tatwa::Akasha => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Tatwa::Agni => &[],
            Tatwa::Prithvi => &[],
            Tatwa::Vayu => &[],
            Tatwa::Jala => &[],
            Tatwa::Akasha => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Tatwa, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Tatwa> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Tatwa> {
        match id {
            0 => Some(Tatwa::Agni),
            1 => Some(Tatwa::Prithvi),
            2 => Some(Tatwa::Vayu),
            3 => Some(Tatwa::Jala),
            4 => Some(Tatwa::Akasha),
            _ => None,
        }
    }
}

impl Catalogued for Tatwa {
    const KIND: Kind = Kind::Tatwa;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Tatwa {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Tatwa {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Tatwa>(key))
    }
}

impl From<Tatwa> for KeyId {
    fn from(member: Tatwa) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Tatwa {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Tatwa) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Tatwa {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Tatwa {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Tatwa>(key)))
    }
}
