//! Generated by `cargo xtask gen catalogue` from `catalogue/muhurta_yoga.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The muhurta yogas a day may carry. The members are attested; the tables that say when each holds are module data with marks of their own, because the corpus cannot derive a seven-by-twenty-seven table from twelve positive days (`03-design/panchanga-day-conventions.md` §8).
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum MuhurtaYoga {
    /// Amrit Siddhi yoga: one vara and nakshatra pair each
    AmritSiddhi = 0,
    /// Sarvartha Siddhi yoga: a set of nakshatras per vara
    SarvarthaSiddhi = 1,
    /// Siddha yoga
    Siddha = 2,
    /// Dwipushkar yoga: a Bhadra tithi on a Sunday, Tuesday or Saturday in a two-footed nakshatra, which doubles what the day brings
    Dwipushkar = 3,
    /// Tripushkar yoga: a Bhadra tithi on a Sunday, Tuesday or Saturday in a three-footed nakshatra, which triples what the day brings
    Tripushkar = 4,
}

/// The attributes of a `muhurta_yoga` member.
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct MuhurtaYogaAttributes {
    /// `auspicious`.
    pub auspicious: bool,
}

static ATTRIBUTES: [MuhurtaYogaAttributes; 5] = [
    MuhurtaYogaAttributes { auspicious: true, },
    MuhurtaYogaAttributes { auspicious: true, },
    MuhurtaYogaAttributes { auspicious: true, },
    MuhurtaYogaAttributes { auspicious: false, },
    MuhurtaYogaAttributes { auspicious: false, },
];

const BY_KEY: [(&str, MuhurtaYoga); 5] = [
    ("AMRIT_SIDDHI", MuhurtaYoga::AmritSiddhi),
    ("DWIPUSHKAR", MuhurtaYoga::Dwipushkar),
    ("SARVARTHA_SIDDHI", MuhurtaYoga::SarvarthaSiddhi),
    ("SIDDHA", MuhurtaYoga::Siddha),
    ("TRIPUSHKAR", MuhurtaYoga::Tripushkar),
];

const ALIASES: [(&str, MuhurtaYoga); 0] = [
];

impl MuhurtaYoga {
    /// The kind.
    pub const KIND: Kind = Kind::MuhurtaYoga;

    /// Every member, in id order.
    pub const ALL: [MuhurtaYoga; 5] = [
        MuhurtaYoga::AmritSiddhi,
        MuhurtaYoga::SarvarthaSiddhi,
        MuhurtaYoga::Siddha,
        MuhurtaYoga::Dwipushkar,
        MuhurtaYoga::Tripushkar,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            MuhurtaYoga::AmritSiddhi => "AMRIT_SIDDHI",
            MuhurtaYoga::SarvarthaSiddhi => "SARVARTHA_SIDDHI",
            MuhurtaYoga::Siddha => "SIDDHA",
            MuhurtaYoga::Dwipushkar => "DWIPUSHKAR",
            MuhurtaYoga::Tripushkar => "TRIPUSHKAR",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            MuhurtaYoga::AmritSiddhi => "muhurta_yoga.AMRIT_SIDDHI",
            MuhurtaYoga::SarvarthaSiddhi => "muhurta_yoga.SARVARTHA_SIDDHI",
            MuhurtaYoga::Siddha => "muhurta_yoga.SIDDHA",
            MuhurtaYoga::Dwipushkar => "muhurta_yoga.DWIPUSHKAR",
            MuhurtaYoga::Tripushkar => "muhurta_yoga.TRIPUSHKAR",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            MuhurtaYoga::AmritSiddhi => "Amrit Siddhi yoga: one vara and nakshatra pair each",
            MuhurtaYoga::SarvarthaSiddhi => "Sarvartha Siddhi yoga: a set of nakshatras per vara",
            MuhurtaYoga::Siddha => "Siddha yoga",
            MuhurtaYoga::Dwipushkar => "Dwipushkar yoga: a Bhadra tithi on a Sunday, Tuesday or Saturday in a two-footed nakshatra, which doubles what the day brings",
            MuhurtaYoga::Tripushkar => "Tripushkar yoga: a Bhadra tithi on a Sunday, Tuesday or Saturday in a three-footed nakshatra, which triples what the day brings",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            MuhurtaYoga::AmritSiddhi => None,
            MuhurtaYoga::SarvarthaSiddhi => None,
            MuhurtaYoga::Siddha => None,
            MuhurtaYoga::Dwipushkar => None,
            MuhurtaYoga::Tripushkar => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            MuhurtaYoga::AmritSiddhi => Mark::Traditional,
            MuhurtaYoga::SarvarthaSiddhi => Mark::Traditional,
            MuhurtaYoga::Siddha => Mark::Traditional,
            MuhurtaYoga::Dwipushkar => Mark::Traditional,
            MuhurtaYoga::Tripushkar => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            MuhurtaYoga::AmritSiddhi => false,
            MuhurtaYoga::SarvarthaSiddhi => false,
            MuhurtaYoga::Siddha => false,
            MuhurtaYoga::Dwipushkar => false,
            MuhurtaYoga::Tripushkar => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            MuhurtaYoga::AmritSiddhi => &[Source { text: "baseline-engine", reference: "panchanga_day.muhurta_yogas.amrit_siddhi" }],
            MuhurtaYoga::SarvarthaSiddhi => &[Source { text: "baseline-engine", reference: "panchanga_day.muhurta_yogas.sarvartha_siddhi" }],
            MuhurtaYoga::Siddha => &[Source { text: "baseline-engine", reference: "panchanga_day.muhurta_yogas.siddha" }],
            MuhurtaYoga::Dwipushkar => &[Source { text: "baseline-engine", reference: "panchanga_day.muhurta_yogas.dwipushkar; no recorded day carries one" }],
            MuhurtaYoga::Tripushkar => &[Source { text: "baseline-engine", reference: "panchanga_day.muhurta_yogas.tripushkar, measured: the classical rule reproduces the corpus's one instance with no false positives" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            MuhurtaYoga::AmritSiddhi => &[],
            MuhurtaYoga::SarvarthaSiddhi => &[],
            MuhurtaYoga::Siddha => &[],
            MuhurtaYoga::Dwipushkar => &[],
            MuhurtaYoga::Tripushkar => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::MuhurtaYoga, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<MuhurtaYoga> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<MuhurtaYoga> {
        match id {
            0 => Some(MuhurtaYoga::AmritSiddhi),
            1 => Some(MuhurtaYoga::SarvarthaSiddhi),
            2 => Some(MuhurtaYoga::Siddha),
            3 => Some(MuhurtaYoga::Dwipushkar),
            4 => Some(MuhurtaYoga::Tripushkar),
            _ => None,
        }
    }

    /// The member's attributes.
    #[must_use]
    #[allow(clippy::indexing_slicing, reason = "the discriminants are dense from zero")]
    pub fn attributes(self) -> &'static MuhurtaYogaAttributes {
        &ATTRIBUTES[self as usize]
    }
}

impl Catalogued for MuhurtaYoga {
    const KIND: Kind = Kind::MuhurtaYoga;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for MuhurtaYoga {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for MuhurtaYoga {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<MuhurtaYoga>(key))
    }
}

impl From<MuhurtaYoga> for KeyId {
    fn from(member: MuhurtaYoga) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for MuhurtaYoga {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::MuhurtaYoga) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for MuhurtaYoga {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for MuhurtaYoga {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <std::borrow::Cow<'_, str>>::deserialize(deserializer)?;
        Self::from_key(&key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<MuhurtaYoga>(&key)))
    }
}
