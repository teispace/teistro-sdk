//! Generated by `cargo xtask gen catalogue` from `catalogue/era.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The era numbers a date may carry.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Era {
    /// Vikrama
    Vikrama = 0,
    /// Shaka
    Shaka = 1,
    /// Kali
    Kali = 2,
    /// Nepal Sambat
    NepalSambat = 3,
    /// Buddha
    Buddha = 4,
    /// Kollam
    Kollam = 5,
    /// Bengali
    Bengali = 6,
}

const BY_KEY: [(&str, Era); 7] = [
    ("BENGALI", Era::Bengali),
    ("BUDDHA", Era::Buddha),
    ("KALI", Era::Kali),
    ("KOLLAM", Era::Kollam),
    ("NEPAL_SAMBAT", Era::NepalSambat),
    ("SHAKA", Era::Shaka),
    ("VIKRAMA", Era::Vikrama),
];

const ALIASES: [(&str, Era); 0] = [
];

impl Era {
    /// The kind.
    pub const KIND: Kind = Kind::Era;

    /// Every member, in id order.
    pub const ALL: [Era; 7] = [
        Era::Vikrama,
        Era::Shaka,
        Era::Kali,
        Era::NepalSambat,
        Era::Buddha,
        Era::Kollam,
        Era::Bengali,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Era::Vikrama => "VIKRAMA",
            Era::Shaka => "SHAKA",
            Era::Kali => "KALI",
            Era::NepalSambat => "NEPAL_SAMBAT",
            Era::Buddha => "BUDDHA",
            Era::Kollam => "KOLLAM",
            Era::Bengali => "BENGALI",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Era::Vikrama => "era.VIKRAMA",
            Era::Shaka => "era.SHAKA",
            Era::Kali => "era.KALI",
            Era::NepalSambat => "era.NEPAL_SAMBAT",
            Era::Buddha => "era.BUDDHA",
            Era::Kollam => "era.KOLLAM",
            Era::Bengali => "era.BENGALI",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Era::Vikrama => "Vikrama",
            Era::Shaka => "Shaka",
            Era::Kali => "Kali",
            Era::NepalSambat => "Nepal Sambat",
            Era::Buddha => "Buddha",
            Era::Kollam => "Kollam",
            Era::Bengali => "Bengali",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Era::Vikrama => None,
            Era::Shaka => None,
            Era::Kali => None,
            Era::NepalSambat => None,
            Era::Buddha => None,
            Era::Kollam => None,
            Era::Bengali => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Era::Vikrama => Mark::Verified,
            Era::Shaka => Mark::Verified,
            Era::Kali => Mark::Verified,
            Era::NepalSambat => Mark::Verified,
            Era::Buddha => Mark::Verified,
            Era::Kollam => Mark::Verified,
            Era::Bengali => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Era::Vikrama => false,
            Era::Shaka => false,
            Era::Kali => false,
            Era::NepalSambat => false,
            Era::Buddha => false,
            Era::Kollam => false,
            Era::Bengali => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Era::Vikrama => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::Shaka => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::Kali => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::NepalSambat => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::Buddha => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::Kollam => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
            Era::Bengali => &[Source { text: "calendar-time-architecture", reference: "docs/02-architecture/04-calendar-time-architecture.md" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Era::Vikrama => &[],
            Era::Shaka => &[],
            Era::Kali => &[],
            Era::NepalSambat => &[],
            Era::Buddha => &[],
            Era::Kollam => &[],
            Era::Bengali => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Era, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Era> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Era> {
        match id {
            0 => Some(Era::Vikrama),
            1 => Some(Era::Shaka),
            2 => Some(Era::Kali),
            3 => Some(Era::NepalSambat),
            4 => Some(Era::Buddha),
            5 => Some(Era::Kollam),
            6 => Some(Era::Bengali),
            _ => None,
        }
    }
}

impl Catalogued for Era {
    const KIND: Kind = Kind::Era;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Era {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Era {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Era>(key))
    }
}

impl From<Era> for KeyId {
    fn from(member: Era) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Era {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Era) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Era {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Era {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Era>(key)))
    }
}
