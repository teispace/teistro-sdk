//! Generated by `cargo xtask gen catalogue` from `catalogue/nature.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The natural benefic or malefic character of a graha.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Nature {
    /// Benefic
    Benefic = 0,
    /// Malefic
    Malefic = 1,
    /// Neutral
    Neutral = 2,
}

const BY_KEY: [(&str, Nature); 3] = [
    ("BENEFIC", Nature::Benefic),
    ("MALEFIC", Nature::Malefic),
    ("NEUTRAL", Nature::Neutral),
];

const ALIASES: [(&str, Nature); 0] = [
];

impl Nature {
    /// The kind.
    pub const KIND: Kind = Kind::Nature;

    /// Every member, in id order.
    pub const ALL: [Nature; 3] = [
        Nature::Benefic,
        Nature::Malefic,
        Nature::Neutral,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Nature::Benefic => "BENEFIC",
            Nature::Malefic => "MALEFIC",
            Nature::Neutral => "NEUTRAL",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Nature::Benefic => "nature.BENEFIC",
            Nature::Malefic => "nature.MALEFIC",
            Nature::Neutral => "nature.NEUTRAL",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Nature::Benefic => "Benefic",
            Nature::Malefic => "Malefic",
            Nature::Neutral => "Neutral",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Nature::Benefic => None,
            Nature::Malefic => None,
            Nature::Neutral => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Nature::Benefic => Mark::Verified,
            Nature::Malefic => Mark::Verified,
            Nature::Neutral => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Nature::Benefic => false,
            Nature::Malefic => false,
            Nature::Neutral => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Nature::Benefic => &[Source { text: "baseline-engine", reference: "core data" }],
            Nature::Malefic => &[Source { text: "baseline-engine", reference: "core data" }],
            Nature::Neutral => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Nature::Benefic => &[],
            Nature::Malefic => &[],
            Nature::Neutral => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Nature, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Nature> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Nature> {
        match id {
            0 => Some(Nature::Benefic),
            1 => Some(Nature::Malefic),
            2 => Some(Nature::Neutral),
            _ => None,
        }
    }
}

impl Catalogued for Nature {
    const KIND: Kind = Kind::Nature;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Nature {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Nature {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Nature>(key))
    }
}

impl From<Nature> for KeyId {
    fn from(member: Nature) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Nature {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Nature) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Nature {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Nature {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Nature>(key)))
    }
}
