//! Generated by `cargo xtask gen catalogue` from `catalogue/nadi.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The three nadis of the nakshatras.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Nadi {
    /// Aadi
    Aadi = 0,
    /// Madhya
    Madhya = 1,
    /// Antya
    Antya = 2,
}

const BY_KEY: [(&str, Nadi); 3] = [
    ("AADI", Nadi::Aadi),
    ("ANTYA", Nadi::Antya),
    ("MADHYA", Nadi::Madhya),
];

const ALIASES: [(&str, Nadi); 0] = [
];

impl Nadi {
    /// The kind.
    pub const KIND: Kind = Kind::Nadi;

    /// Every member, in id order.
    pub const ALL: [Nadi; 3] = [
        Nadi::Aadi,
        Nadi::Madhya,
        Nadi::Antya,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Nadi::Aadi => "AADI",
            Nadi::Madhya => "MADHYA",
            Nadi::Antya => "ANTYA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Nadi::Aadi => "nadi.AADI",
            Nadi::Madhya => "nadi.MADHYA",
            Nadi::Antya => "nadi.ANTYA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Nadi::Aadi => "Aadi",
            Nadi::Madhya => "Madhya",
            Nadi::Antya => "Antya",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Nadi::Aadi => None,
            Nadi::Madhya => None,
            Nadi::Antya => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Nadi::Aadi => Mark::Verified,
            Nadi::Madhya => Mark::Verified,
            Nadi::Antya => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Nadi::Aadi => false,
            Nadi::Madhya => false,
            Nadi::Antya => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Nadi::Aadi => &[Source { text: "baseline-engine", reference: "core data" }],
            Nadi::Madhya => &[Source { text: "baseline-engine", reference: "core data" }],
            Nadi::Antya => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Nadi::Aadi => &[],
            Nadi::Madhya => &[],
            Nadi::Antya => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Nadi, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Nadi> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Nadi> {
        match id {
            0 => Some(Nadi::Aadi),
            1 => Some(Nadi::Madhya),
            2 => Some(Nadi::Antya),
            _ => None,
        }
    }
}

impl Catalogued for Nadi {
    const KIND: Kind = Kind::Nadi;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Nadi {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Nadi {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Nadi>(key))
    }
}

impl From<Nadi> for KeyId {
    fn from(member: Nadi) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Nadi {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Nadi) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Nadi {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Nadi {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Nadi>(key)))
    }
}
