//! Generated by `cargo xtask gen catalogue` from `catalogue/muhurta_nature.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The seven muhurta natures of the nakshatras.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum MuhurtaNature {
    /// Dhruva
    Dhruva = 0,
    /// Chara
    Chara = 1,
    /// Ugra
    Ugra = 2,
    /// Kshipra
    Kshipra = 3,
    /// Mridu
    Mridu = 4,
    /// Mishra
    Mishra = 5,
    /// Tikshna
    Tikshna = 6,
}

const BY_KEY: [(&str, MuhurtaNature); 7] = [
    ("CHARA", MuhurtaNature::Chara),
    ("DHRUVA", MuhurtaNature::Dhruva),
    ("KSHIPRA", MuhurtaNature::Kshipra),
    ("MISHRA", MuhurtaNature::Mishra),
    ("MRIDU", MuhurtaNature::Mridu),
    ("TIKSHNA", MuhurtaNature::Tikshna),
    ("UGRA", MuhurtaNature::Ugra),
];

const ALIASES: [(&str, MuhurtaNature); 0] = [
];

impl MuhurtaNature {
    /// The kind.
    pub const KIND: Kind = Kind::MuhurtaNature;

    /// Every member, in id order.
    pub const ALL: [MuhurtaNature; 7] = [
        MuhurtaNature::Dhruva,
        MuhurtaNature::Chara,
        MuhurtaNature::Ugra,
        MuhurtaNature::Kshipra,
        MuhurtaNature::Mridu,
        MuhurtaNature::Mishra,
        MuhurtaNature::Tikshna,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            MuhurtaNature::Dhruva => "DHRUVA",
            MuhurtaNature::Chara => "CHARA",
            MuhurtaNature::Ugra => "UGRA",
            MuhurtaNature::Kshipra => "KSHIPRA",
            MuhurtaNature::Mridu => "MRIDU",
            MuhurtaNature::Mishra => "MISHRA",
            MuhurtaNature::Tikshna => "TIKSHNA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            MuhurtaNature::Dhruva => "muhurta_nature.DHRUVA",
            MuhurtaNature::Chara => "muhurta_nature.CHARA",
            MuhurtaNature::Ugra => "muhurta_nature.UGRA",
            MuhurtaNature::Kshipra => "muhurta_nature.KSHIPRA",
            MuhurtaNature::Mridu => "muhurta_nature.MRIDU",
            MuhurtaNature::Mishra => "muhurta_nature.MISHRA",
            MuhurtaNature::Tikshna => "muhurta_nature.TIKSHNA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            MuhurtaNature::Dhruva => "Dhruva",
            MuhurtaNature::Chara => "Chara",
            MuhurtaNature::Ugra => "Ugra",
            MuhurtaNature::Kshipra => "Kshipra",
            MuhurtaNature::Mridu => "Mridu",
            MuhurtaNature::Mishra => "Mishra",
            MuhurtaNature::Tikshna => "Tikshna",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            MuhurtaNature::Dhruva => None,
            MuhurtaNature::Chara => None,
            MuhurtaNature::Ugra => None,
            MuhurtaNature::Kshipra => None,
            MuhurtaNature::Mridu => None,
            MuhurtaNature::Mishra => None,
            MuhurtaNature::Tikshna => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            MuhurtaNature::Dhruva => Mark::Verified,
            MuhurtaNature::Chara => Mark::Verified,
            MuhurtaNature::Ugra => Mark::Verified,
            MuhurtaNature::Kshipra => Mark::Verified,
            MuhurtaNature::Mridu => Mark::Verified,
            MuhurtaNature::Mishra => Mark::Verified,
            MuhurtaNature::Tikshna => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            MuhurtaNature::Dhruva => false,
            MuhurtaNature::Chara => false,
            MuhurtaNature::Ugra => false,
            MuhurtaNature::Kshipra => false,
            MuhurtaNature::Mridu => false,
            MuhurtaNature::Mishra => false,
            MuhurtaNature::Tikshna => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            MuhurtaNature::Dhruva => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Chara => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Ugra => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Kshipra => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Mridu => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Mishra => &[Source { text: "baseline-engine", reference: "core data" }],
            MuhurtaNature::Tikshna => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            MuhurtaNature::Dhruva => &[],
            MuhurtaNature::Chara => &[],
            MuhurtaNature::Ugra => &[],
            MuhurtaNature::Kshipra => &[],
            MuhurtaNature::Mridu => &[],
            MuhurtaNature::Mishra => &[],
            MuhurtaNature::Tikshna => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::MuhurtaNature, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<MuhurtaNature> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<MuhurtaNature> {
        match id {
            0 => Some(MuhurtaNature::Dhruva),
            1 => Some(MuhurtaNature::Chara),
            2 => Some(MuhurtaNature::Ugra),
            3 => Some(MuhurtaNature::Kshipra),
            4 => Some(MuhurtaNature::Mridu),
            5 => Some(MuhurtaNature::Mishra),
            6 => Some(MuhurtaNature::Tikshna),
            _ => None,
        }
    }
}

impl Catalogued for MuhurtaNature {
    const KIND: Kind = Kind::MuhurtaNature;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for MuhurtaNature {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for MuhurtaNature {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<MuhurtaNature>(key))
    }
}

impl From<MuhurtaNature> for KeyId {
    fn from(member: MuhurtaNature) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for MuhurtaNature {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::MuhurtaNature) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for MuhurtaNature {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for MuhurtaNature {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<MuhurtaNature>(key)))
    }
}
