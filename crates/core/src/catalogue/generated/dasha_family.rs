//! Generated by `cargo xtask gen catalogue` from `catalogue/dasha_family.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// Which kernel a dasha system runs on.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum DashaFamily {
    /// Udu
    Udu = 0,
    /// Rashi
    Rashi = 1,
    /// Kalachakra
    Kalachakra = 2,
    /// Scale
    Scale = 3,
    /// Composition
    Composition = 4,
    /// Own
    Own = 5,
}

const BY_KEY: [(&str, DashaFamily); 6] = [
    ("COMPOSITION", DashaFamily::Composition),
    ("KALACHAKRA", DashaFamily::Kalachakra),
    ("OWN", DashaFamily::Own),
    ("RASHI", DashaFamily::Rashi),
    ("SCALE", DashaFamily::Scale),
    ("UDU", DashaFamily::Udu),
];

const ALIASES: [(&str, DashaFamily); 0] = [
];

impl DashaFamily {
    /// The kind.
    pub const KIND: Kind = Kind::DashaFamily;

    /// Every member, in id order.
    pub const ALL: [DashaFamily; 6] = [
        DashaFamily::Udu,
        DashaFamily::Rashi,
        DashaFamily::Kalachakra,
        DashaFamily::Scale,
        DashaFamily::Composition,
        DashaFamily::Own,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            DashaFamily::Udu => "UDU",
            DashaFamily::Rashi => "RASHI",
            DashaFamily::Kalachakra => "KALACHAKRA",
            DashaFamily::Scale => "SCALE",
            DashaFamily::Composition => "COMPOSITION",
            DashaFamily::Own => "OWN",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            DashaFamily::Udu => "dasha_family.UDU",
            DashaFamily::Rashi => "dasha_family.RASHI",
            DashaFamily::Kalachakra => "dasha_family.KALACHAKRA",
            DashaFamily::Scale => "dasha_family.SCALE",
            DashaFamily::Composition => "dasha_family.COMPOSITION",
            DashaFamily::Own => "dasha_family.OWN",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            DashaFamily::Udu => "Udu",
            DashaFamily::Rashi => "Rashi",
            DashaFamily::Kalachakra => "Kalachakra",
            DashaFamily::Scale => "Scale",
            DashaFamily::Composition => "Composition",
            DashaFamily::Own => "Own",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            DashaFamily::Udu => None,
            DashaFamily::Rashi => None,
            DashaFamily::Kalachakra => None,
            DashaFamily::Scale => None,
            DashaFamily::Composition => None,
            DashaFamily::Own => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            DashaFamily::Udu => Mark::Verified,
            DashaFamily::Rashi => Mark::Verified,
            DashaFamily::Kalachakra => Mark::Verified,
            DashaFamily::Scale => Mark::Verified,
            DashaFamily::Composition => Mark::Verified,
            DashaFamily::Own => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            DashaFamily::Udu => false,
            DashaFamily::Rashi => false,
            DashaFamily::Kalachakra => false,
            DashaFamily::Scale => false,
            DashaFamily::Composition => false,
            DashaFamily::Own => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            DashaFamily::Udu => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
            DashaFamily::Rashi => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
            DashaFamily::Kalachakra => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
            DashaFamily::Scale => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
            DashaFamily::Composition => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
            DashaFamily::Own => &[Source { text: "dasha-kernels", reference: "docs/03-design/dasha-kernels.md" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            DashaFamily::Udu => &[],
            DashaFamily::Rashi => &[],
            DashaFamily::Kalachakra => &[],
            DashaFamily::Scale => &[],
            DashaFamily::Composition => &[],
            DashaFamily::Own => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::DashaFamily, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<DashaFamily> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<DashaFamily> {
        match id {
            0 => Some(DashaFamily::Udu),
            1 => Some(DashaFamily::Rashi),
            2 => Some(DashaFamily::Kalachakra),
            3 => Some(DashaFamily::Scale),
            4 => Some(DashaFamily::Composition),
            5 => Some(DashaFamily::Own),
            _ => None,
        }
    }
}

impl Catalogued for DashaFamily {
    const KIND: Kind = Kind::DashaFamily;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for DashaFamily {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for DashaFamily {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<DashaFamily>(key))
    }
}

impl From<DashaFamily> for KeyId {
    fn from(member: DashaFamily) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for DashaFamily {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::DashaFamily) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for DashaFamily {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for DashaFamily {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<DashaFamily>(key)))
    }
}
