//! Generated by `cargo xtask gen catalogue` from `catalogue/kaala.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The three inauspicious eighths of the daylight, each taking a different eighth on each day of the week.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Kaala {
    /// Rahu kaala. The eighth by vara, Sunday first, counted from one; the one row of the three that is not arithmetic.
    RahuKaala = 0,
    /// Yamaghanda. The third eighth counted backwards from the vara, modulo seven.
    Yamaghanda = 1,
    /// Gulika kaala. The fifth eighth counted backwards from the vara, modulo seven.
    GulikaKaala = 2,
}

/// The attributes of a `kaala` member.
#[derive(Clone, Copy, Debug, PartialEq)]
pub struct KaalaAttributes {
    /// `eighth_by_vara`.
    pub eighth_by_vara: [u8; 7],
}

static ATTRIBUTES: [KaalaAttributes; 3] = [
    KaalaAttributes { eighth_by_vara: [8, 2, 7, 5, 6, 4, 3], },
    KaalaAttributes { eighth_by_vara: [5, 4, 3, 2, 1, 7, 6], },
    KaalaAttributes { eighth_by_vara: [7, 6, 5, 4, 3, 2, 1], },
];

const BY_KEY: [(&str, Kaala); 3] = [
    ("GULIKA_KAALA", Kaala::GulikaKaala),
    ("RAHU_KAALA", Kaala::RahuKaala),
    ("YAMAGHANDA", Kaala::Yamaghanda),
];

const ALIASES: [(&str, Kaala); 0] = [
];

impl Kaala {
    /// The kind.
    pub const KIND: Kind = Kind::Kaala;

    /// Every member, in id order.
    pub const ALL: [Kaala; 3] = [
        Kaala::RahuKaala,
        Kaala::Yamaghanda,
        Kaala::GulikaKaala,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Kaala::RahuKaala => "RAHU_KAALA",
            Kaala::Yamaghanda => "YAMAGHANDA",
            Kaala::GulikaKaala => "GULIKA_KAALA",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Kaala::RahuKaala => "kaala.RAHU_KAALA",
            Kaala::Yamaghanda => "kaala.YAMAGHANDA",
            Kaala::GulikaKaala => "kaala.GULIKA_KAALA",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Kaala::RahuKaala => "Rahu kaala. The eighth by vara, Sunday first, counted from one; the one row of the three that is not arithmetic.",
            Kaala::Yamaghanda => "Yamaghanda. The third eighth counted backwards from the vara, modulo seven.",
            Kaala::GulikaKaala => "Gulika kaala. The fifth eighth counted backwards from the vara, modulo seven.",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Kaala::RahuKaala => None,
            Kaala::Yamaghanda => None,
            Kaala::GulikaKaala => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Kaala::RahuKaala => Mark::Verified,
            Kaala::Yamaghanda => Mark::Verified,
            Kaala::GulikaKaala => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Kaala::RahuKaala => false,
            Kaala::Yamaghanda => false,
            Kaala::GulikaKaala => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Kaala::RahuKaala => &[Source { text: "baseline-engine", reference: "panchanga_day.rahu_kaal, measured on all 55 days" }],
            Kaala::Yamaghanda => &[Source { text: "baseline-engine", reference: "panchanga_day.yamaghanda, measured on all 55 days" }],
            Kaala::GulikaKaala => &[Source { text: "baseline-engine", reference: "panchanga_day.gulika_kaal, measured on all 55 days" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Kaala::RahuKaala => &[],
            Kaala::Yamaghanda => &[],
            Kaala::GulikaKaala => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Kaala, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Kaala> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Kaala> {
        match id {
            0 => Some(Kaala::RahuKaala),
            1 => Some(Kaala::Yamaghanda),
            2 => Some(Kaala::GulikaKaala),
            _ => None,
        }
    }

    /// The member's attributes.
    #[must_use]
    #[allow(clippy::indexing_slicing, reason = "the discriminants are dense from zero")]
    pub fn attributes(self) -> &'static KaalaAttributes {
        &ATTRIBUTES[self as usize]
    }
}

impl Catalogued for Kaala {
    const KIND: Kind = Kind::Kaala;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Kaala {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Kaala {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Kaala>(key))
    }
}

impl From<Kaala> for KeyId {
    fn from(member: Kaala) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Kaala {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Kaala) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Kaala {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Kaala {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Kaala>(key)))
    }
}
