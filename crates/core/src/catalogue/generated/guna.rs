//! Generated by `cargo xtask gen catalogue` from `catalogue/guna.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// The three gunas.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Guna {
    /// Sattva
    Sattva = 0,
    /// Rajas
    Rajas = 1,
    /// Tamas
    Tamas = 2,
}

const BY_KEY: [(&str, Guna); 3] = [
    ("RAJAS", Guna::Rajas),
    ("SATTVA", Guna::Sattva),
    ("TAMAS", Guna::Tamas),
];

const ALIASES: [(&str, Guna); 0] = [
];

impl Guna {
    /// The kind.
    pub const KIND: Kind = Kind::Guna;

    /// Every member, in id order.
    pub const ALL: [Guna; 3] = [
        Guna::Sattva,
        Guna::Rajas,
        Guna::Tamas,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Guna::Sattva => "SATTVA",
            Guna::Rajas => "RAJAS",
            Guna::Tamas => "TAMAS",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Guna::Sattva => "guna.SATTVA",
            Guna::Rajas => "guna.RAJAS",
            Guna::Tamas => "guna.TAMAS",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Guna::Sattva => "Sattva",
            Guna::Rajas => "Rajas",
            Guna::Tamas => "Tamas",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Guna::Sattva => None,
            Guna::Rajas => None,
            Guna::Tamas => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Guna::Sattva => Mark::Verified,
            Guna::Rajas => Mark::Verified,
            Guna::Tamas => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Guna::Sattva => false,
            Guna::Rajas => false,
            Guna::Tamas => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Guna::Sattva => &[Source { text: "baseline-engine", reference: "core data" }],
            Guna::Rajas => &[Source { text: "baseline-engine", reference: "core data" }],
            Guna::Tamas => &[Source { text: "baseline-engine", reference: "core data" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Guna::Sattva => &[],
            Guna::Rajas => &[],
            Guna::Tamas => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Guna, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Guna> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Guna> {
        match id {
            0 => Some(Guna::Sattva),
            1 => Some(Guna::Rajas),
            2 => Some(Guna::Tamas),
            _ => None,
        }
    }
}

impl Catalogued for Guna {
    const KIND: Kind = Kind::Guna;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Guna {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Guna {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Guna>(key))
    }
}

impl From<Guna> for KeyId {
    fn from(member: Guna) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Guna {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Guna) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Guna {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Guna {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Guna>(key)))
    }
}
