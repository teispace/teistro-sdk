//! Generated by `cargo xtask gen catalogue` from `catalogue/degeneracy.yaml` (version 1); do not edit.

#![allow(clippy::match_same_arms, clippy::unreadable_literal, clippy::too_many_lines, reason = "generated tables")]

use crate::catalogue::{Catalogued, Mark, Source, UnknownKey};
use crate::key::{Kind, KeyId};

/// Whether a house system is undefined at some latitudes.
///
/// Members are appended only; the discriminants are the catalogue ids.
#[repr(u16)]
#[non_exhaustive]
#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord, Hash)]
pub enum Degeneracy {
    /// None
    None = 0,
    /// Polar Undefined
    PolarUndefined = 1,
}

const BY_KEY: [(&str, Degeneracy); 2] = [
    ("NONE", Degeneracy::None),
    ("POLAR_UNDEFINED", Degeneracy::PolarUndefined),
];

const ALIASES: [(&str, Degeneracy); 0] = [
];

impl Degeneracy {
    /// The kind.
    pub const KIND: Kind = Kind::Degeneracy;

    /// Every member, in id order.
    pub const ALL: [Degeneracy; 2] = [
        Degeneracy::None,
        Degeneracy::PolarUndefined,
    ];

    /// The key inside the kind (`SUN`).
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Degeneracy::None => "NONE",
            Degeneracy::PolarUndefined => "POLAR_UNDEFINED",
        }
    }

    /// The full key (`graha.SUN`).
    #[must_use]
    pub const fn full_key(self) -> &'static str {
        match self {
            Degeneracy::None => "degeneracy.NONE",
            Degeneracy::PolarUndefined => "degeneracy.POLAR_UNDEFINED",
        }
    }

    /// The member's documentation.
    #[must_use]
    pub const fn doc(self) -> &'static str {
        match self {
            Degeneracy::None => "None",
            Degeneracy::PolarUndefined => "Polar Undefined",
        }
    }

    /// The glyph, when the member has one.
    #[must_use]
    pub const fn glyph(self) -> Option<&'static str> {
        match self {
            Degeneracy::None => None,
            Degeneracy::PolarUndefined => None,
        }
    }

    /// The confidence mark (ADR-0018).
    #[must_use]
    pub const fn mark(self) -> Mark {
        match self {
            Degeneracy::None => Mark::Verified,
            Degeneracy::PolarUndefined => Mark::Verified,
        }
    }

    /// Whether the member is deprecated; it still resolves.
    #[must_use]
    pub const fn deprecated(self) -> bool {
        match self {
            Degeneracy::None => false,
            Degeneracy::PolarUndefined => false,
        }
    }

    /// The sources the member cites.
    #[must_use]
    pub const fn sources(self) -> &'static [Source] {
        match self {
            Degeneracy::None => &[Source { text: "structural", reference: "design" }],
            Degeneracy::PolarUndefined => &[Source { text: "structural", reference: "design" }],
        }
    }

    /// Attributes whose values are traditional inside a verified row.
    #[must_use]
    pub const fn unverified(self) -> &'static [&'static str] {
        match self {
            Degeneracy::None => &[],
            Degeneracy::PolarUndefined => &[],
        }
    }

    /// The catalogue id: the discriminant.
    #[must_use]
    pub const fn id(self) -> u16 {
        self as u16
    }

    /// The packed key id for the C boundary.
    #[must_use]
    pub const fn key_id(self) -> KeyId {
        KeyId::new(Kind::Degeneracy, self as u16)
    }

    /// The member with a key or a former key.
    #[must_use]
    pub fn from_key(key: &str) -> Option<Degeneracy> {
        crate::catalogue::lookup(&BY_KEY, key).or_else(|| crate::catalogue::lookup(&ALIASES, key))
    }

    /// The member with an id.
    #[must_use]
    pub const fn from_id(id: u16) -> Option<Degeneracy> {
        match id {
            0 => Some(Degeneracy::None),
            1 => Some(Degeneracy::PolarUndefined),
            _ => None,
        }
    }
}

impl Catalogued for Degeneracy {
    const KIND: Kind = Kind::Degeneracy;

    fn all() -> &'static [Self] {
        &Self::ALL
    }

    fn key(self) -> &'static str {
        self.key()
    }

    fn id(self) -> u16 {
        self.id()
    }

    fn mark(self) -> Mark {
        self.mark()
    }

    fn from_key(key: &str) -> Option<Self> {
        Self::from_key(key)
    }

    fn from_id(id: u16) -> Option<Self> {
        Self::from_id(id)
    }
}

impl core::fmt::Display for Degeneracy {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.write_str(self.key())
    }
}

impl core::str::FromStr for Degeneracy {
    type Err = UnknownKey;

    fn from_str(key: &str) -> Result<Self, UnknownKey> {
        Self::from_key(key).ok_or_else(|| UnknownKey::in_kind::<Degeneracy>(key))
    }
}

impl From<Degeneracy> for KeyId {
    fn from(member: Degeneracy) -> KeyId {
        member.key_id()
    }
}

impl TryFrom<KeyId> for Degeneracy {
    type Error = UnknownKey;

    fn try_from(id: KeyId) -> Result<Self, UnknownKey> {
        match id.kind() {
            Some(Kind::Degeneracy) => Self::from_id(id.id()).ok_or_else(|| UnknownKey::id(id)),
            _ => Err(UnknownKey::id(id)),
        }
    }
}

impl serde::Serialize for Degeneracy {
    fn serialize<S: serde::Serializer>(&self, serializer: S) -> Result<S::Ok, S::Error> {
        serializer.serialize_str(self.key())
    }
}

impl<'de> serde::Deserialize<'de> for Degeneracy {
    fn deserialize<D: serde::Deserializer<'de>>(deserializer: D) -> Result<Self, D::Error> {
        let key = <&str>::deserialize(deserializer)?;
        Self::from_key(key).ok_or_else(|| serde::de::Error::custom(UnknownKey::in_kind::<Degeneracy>(key)))
    }
}
