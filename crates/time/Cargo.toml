[package]
name = "teistro-time"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Teistro SDK time layer: zone offset histories and local mean time; scales, Delta T, zone resolution, the local day and ghati-pala follow"
readme = "README.md"
keywords = ["time", "timezone", "astrology"]
categories = ["date-and-time"]
# Not published until the Phase 1 exit (docs/07-roadmap/00-roadmap.md).
publish = false

[dependencies]
teistro-core = { path = "../core" }

[lints]
workspace = true
