//! Generated by teistro-intl from i18n/en-Latn. Do not edit.
//! Keys and parameter shapes only; text comes from packs.

/// The `gender` context.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub enum Gender {
    /// `m`.
    M,
    /// `f`.
    F,
    /// `n`.
    N,
}

impl Gender {
    /// The value's key.
    #[must_use]
    pub const fn key(self) -> &'static str {
        match self {
            Gender::M => "m",
            Gender::F => "f",
            Gender::N => "n",
        }
    }
}

impl From<Gender> for crate::Value {
    fn from(value: Gender) -> Self {
        crate::Value::Str(value.key().to_string())
    }
}

/// The `sdk` group.
pub mod sdk {
    /// The `entity` group.
    pub mod entity {
        /// The `graha` group.
        pub mod graha {
        }

        /// The `nakshatra` group.
        pub mod nakshatra {
        }

        /// The `point` group.
        pub mod point {
        }

        /// The `rashi` group.
        pub mod rashi {
        }

    }

    /// The `reason` group.
    pub mod reason {
        /// The message `sdk.reason.appName`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct AppName;

        impl crate::TypedMessage for AppName {
            const KEY: &'static str = "sdk.reason.appName";
            fn params(&self) -> crate::Params {
                crate::Params::new()
            }
        }

        /// The message `sdk.reason.conjunction`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct Conjunction {
            /// The `count` parameter.
            pub count: i64,
            /// The `graha` parameter.
            pub graha: teistro_core::catalogue::Graha,
        }

        impl crate::TypedMessage for Conjunction {
            const KEY: &'static str = "sdk.reason.conjunction";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("count", crate::Value::Int(self.count)),
                    ("graha", crate::Value::catalogued(self.graha)),
                ])
            }
        }

        /// The message `sdk.reason.exactLongitude`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct ExactLongitude {
            /// The `longitude` parameter.
            pub longitude: f64,
        }

        impl crate::TypedMessage for ExactLongitude {
            const KEY: &'static str = "sdk.reason.exactLongitude";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("longitude", crate::Value::Num(self.longitude)),
                ])
            }
        }

        /// The message `sdk.reason.grahaAt`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct GrahaAt {
            /// The `graha` parameter.
            pub graha: teistro_core::catalogue::Graha,
            /// The `longitude` parameter.
            pub longitude: f64,
        }

        impl crate::TypedMessage for GrahaAt {
            const KEY: &'static str = "sdk.reason.grahaAt";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("graha", crate::Value::catalogued(self.graha)),
                    ("longitude", crate::Value::Num(self.longitude)),
                ])
            }
        }

        /// The message `sdk.reason.grahaInBhava`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct GrahaInBhava {
            /// The `bhava` parameter.
            pub bhava: i64,
            /// The `graha` parameter.
            pub graha: teistro_core::catalogue::Graha,
        }

        impl crate::TypedMessage for GrahaInBhava {
            const KEY: &'static str = "sdk.reason.grahaInBhava";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("bhava", crate::Value::Int(self.bhava)),
                    ("graha", crate::Value::catalogued(self.graha)),
                ])
            }
        }

        /// The message `sdk.reason.grahaInRashi`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct GrahaInRashi {
            /// The `graha` parameter.
            pub graha: teistro_core::catalogue::Graha,
            /// The `rashi` parameter.
            pub rashi: teistro_core::catalogue::Rashi,
        }

        impl crate::TypedMessage for GrahaInRashi {
            const KEY: &'static str = "sdk.reason.grahaInRashi";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("graha", crate::Value::catalogued(self.graha)),
                    ("rashi", crate::Value::catalogued(self.rashi)),
                ])
            }
        }

        /// The message `sdk.reason.greeting`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct Greeting {
            /// The `gender` parameter.
            pub gender: crate::messages::Gender,
            /// The `name` parameter.
            pub name: String,
        }

        impl crate::TypedMessage for Greeting {
            const KEY: &'static str = "sdk.reason.greeting";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("gender", crate::Value::Str(self.gender.key().to_string())),
                    ("name", crate::Value::Str(self.name.clone())),
                ])
            }
        }

        /// The message `sdk.reason.lordship`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct Lordship {
            /// The `bhava` parameter.
            pub bhava: i64,
            /// The `graha` parameter.
            pub graha: teistro_core::catalogue::Graha,
        }

        impl crate::TypedMessage for Lordship {
            const KEY: &'static str = "sdk.reason.lordship";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("bhava", crate::Value::Int(self.bhava)),
                    ("graha", crate::Value::catalogued(self.graha)),
                ])
            }
        }

        /// The message `sdk.reason.occupants`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct Occupants {
            /// The `grahas` parameter.
            pub grahas: Vec<crate::Value>,
            /// The `rashi` parameter.
            pub rashi: teistro_core::catalogue::Rashi,
        }

        impl crate::TypedMessage for Occupants {
            const KEY: &'static str = "sdk.reason.occupants";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("grahas", crate::Value::List(self.grahas.clone())),
                    ("rashi", crate::Value::catalogued(self.rashi)),
                ])
            }
        }

        /// The message `sdk.reason.rashiNature`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct RashiNature {
            /// The `rashi` parameter.
            pub rashi: teistro_core::catalogue::Rashi,
        }

        impl crate::TypedMessage for RashiNature {
            const KEY: &'static str = "sdk.reason.rashiNature";
            fn params(&self) -> crate::Params {
                crate::params([
                    ("rashi", crate::Value::catalogued(self.rashi)),
                ])
            }
        }

        /// The `strength` group.
        pub mod strength {
            /// The message `sdk.reason.strength.rank`.
            #[derive(Clone, Debug, PartialEq)]
            pub struct Rank {
                /// The `rank` parameter.
                pub rank: i64,
            }

            impl crate::TypedMessage for Rank {
                const KEY: &'static str = "sdk.reason.strength.rank";
                fn params(&self) -> crate::Params {
                    crate::params([
                        ("rank", crate::Value::Int(self.rank)),
                    ])
                }
            }

            /// The message `sdk.reason.strength.score`.
            #[derive(Clone, Debug, PartialEq)]
            pub struct Score {
                /// The `graha` parameter.
                pub graha: teistro_core::catalogue::Graha,
                /// The `score` parameter.
                pub score: f64,
            }

            impl crate::TypedMessage for Score {
                const KEY: &'static str = "sdk.reason.strength.score";
                fn params(&self) -> crate::Params {
                    crate::params([
                        ("graha", crate::Value::catalogued(self.graha)),
                        ("score", crate::Value::Num(self.score)),
                    ])
                }
            }

        }

        /// The message `sdk.reason.welcome`.
        #[derive(Clone, Debug, PartialEq)]
        pub struct Welcome;

        impl crate::TypedMessage for Welcome {
            const KEY: &'static str = "sdk.reason.welcome";
            fn params(&self) -> crate::Params {
                crate::Params::new()
            }
        }

    }

}

