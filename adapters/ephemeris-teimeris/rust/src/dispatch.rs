//! The engine's own functions, reachable by name. **Generated by
//! `cargo xtask engine`. Do not edit.**
//!
//! One arm per function the marshaller can carry, from the engine's
//! own description at version `0.1.0`. What is here and what is not is
//! measured in `docs/03-design/engine-passthrough-measured.md`: the
//! functions this adapter will never offer are excluded by what they
//! touch, and the rest are a queue for the generator.
//!
//! This is the adapter's only `unsafe`. Everything else goes through
//! the safe binding; these are the calls that binding does not wrap,
//! which is the whole reason a consumer would reach for them.

#![allow(
    unsafe_code,
    clippy::too_many_lines,
    reason = "generated: one arm per engine function"
)]

use serde_json::{Map, Value, json};
use teimeris::sys;
use teistro_port_ephemeris::ProviderError;

use crate::passthrough::{Within, Keep, borrowed, length, extent, fill, gather, narrow, number, numbers, objects, optional_object, pointed, room, settled, unwritten, checked, record, status, text, wholes};

/// What this adapter offers of the engine's own surface, as the
/// port's `native_manifest` answers it.
///
/// `mutatesEngineState` is the flag a consumer needs and no other
/// manifest would carry: after such a call the engine answers under
/// settings the SDK's provenance does not record.
pub(crate) const MANIFEST: &str = r#"{"engine":"teimeris","version":"0.1.0","functions":[{"name":"tm_status_name","params":[{"name":"s","role":"in","type":"tm_status"}],"returns":"string","mutatesEngineState":false},{"name":"tm_context_release_caches","params":[],"returns":"void","mutatesEngineState":false},{"name":"tm_version_string","params":[],"returns":"string","mutatesEngineState":false},{"name":"tm_version","params":[{"name":"major","role":"out","type":"int"},{"name":"minor","role":"out","type":"int"},{"name":"patch","role":"out","type":"int"}],"returns":"void","mutatesEngineState":false},{"name":"tm_body_name","params":[{"name":"body","role":"in","type":"tm_body"},{"name":"buf","role":"out","type":"string"}],"returns":"void","mutatesEngineState":false},{"name":"tm_julian_day","params":[{"name":"dt","role":"in","type":"tm_datetime","optional":true},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out_jd","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_calendar_date","params":[{"name":"jd","role":"in","type":"double"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out","role":"out","type":"tm_datetime"}],"returns":"status","mutatesEngineState":false},{"name":"tm_utc_to_jd","params":[{"name":"utc","role":"in","type":"tm_datetime","optional":true},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out_jd_tt","role":"out","type":"double"},{"name":"out_jd_ut1","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_jd_to_utc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out","role":"out","type":"tm_datetime"}],"returns":"status","mutatesEngineState":false},{"name":"tm_delta_t","params":[{"name":"jd_ut1","role":"in","type":"double"},{"name":"out_seconds","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_sidereal_time","params":[{"name":"jd_ut1","role":"in","type":"double"},{"name":"geo_lon_deg","role":"in","type":"double"},{"name":"out_hours","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_day_of_week","params":[{"name":"jd","role":"in","type":"double"}],"returns":"tm_weekday","mutatesEngineState":false},{"name":"tm_weekday_name","params":[{"name":"day","role":"in","type":"tm_weekday"}],"returns":"string","mutatesEngineState":false},{"name":"tm_julian_day_many","params":[{"name":"dts","role":"in","type":"tm_datetime[]"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out_jd","role":"out","type":"double[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_calendar_date_many","params":[{"name":"jds","role":"in","type":"double[]"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out","role":"out","type":"tm_datetime[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_delta_t_many","params":[{"name":"jds_ut1","role":"in","type":"double[]"},{"name":"out_seconds","role":"out","type":"double[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_sidereal_time_many","params":[{"name":"jds_ut1","role":"in","type":"double[]"},{"name":"geo_lon_deg","role":"in","type":"double"},{"name":"out_hours","role":"out","type":"double[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_equation_of_time","params":[{"name":"jd_ut1","role":"in","type":"double"},{"name":"out_seconds","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_local_mean_to_apparent","params":[{"name":"jd_local_mean","role":"in","type":"double"},{"name":"geo_lon_deg","role":"in","type":"double"},{"name":"out_jd_local_apparent","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_local_apparent_to_mean","params":[{"name":"jd_local_apparent","role":"in","type":"double"},{"name":"geo_lon_deg","role":"in","type":"double"},{"name":"out_jd_local_mean","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_local_to_utc","params":[{"name":"local","role":"in","type":"tm_datetime","optional":true},{"name":"utc_offset_hours","role":"in","type":"double"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out_utc","role":"out","type":"tm_datetime"}],"returns":"status","mutatesEngineState":false},{"name":"tm_utc_to_local","params":[{"name":"utc","role":"in","type":"tm_datetime","optional":true},{"name":"utc_offset_hours","role":"in","type":"double"},{"name":"cal","role":"in","type":"tm_calendar"},{"name":"out_local","role":"out","type":"tm_datetime"}],"returns":"status","mutatesEngineState":false},{"name":"tm_position_calc","params":[{"name":"req","role":"in","type":"tm_position_request","optional":true},{"name":"out","role":"out","type":"tm_position"}],"returns":"status","mutatesEngineState":false},{"name":"tm_position_value","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"field","role":"in","type":"tm_position_field"},{"name":"out","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_position_calc_many","params":[{"name":"reqs","role":"in","type":"tm_position_request[]"},{"name":"out","role":"out","type":"tm_position[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_position_calc_grid","params":[{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"jds","role":"in","type":"double[]"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_position[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_house_cusp_count","params":[{"name":"sys","role":"in","type":"tm_house_system"}],"returns":"size_t","mutatesEngineState":false},{"name":"tm_house_system_name","params":[{"name":"sys","role":"in","type":"tm_house_system"}],"returns":"string","mutatesEngineState":false},{"name":"tm_house_system_count","params":[],"returns":"size_t","mutatesEngineState":false},{"name":"tm_houses_calc","params":[{"name":"req","role":"in","type":"tm_houses_request","optional":true},{"name":"cusps","role":"out","type":"double[]"},{"name":"cusp_speeds","role":"out","type":"double[]"},{"name":"out_angles","role":"out","type":"tm_angles"}],"returns":"status","mutatesEngineState":false},{"name":"tm_house_position","params":[{"name":"armc","role":"in","type":"double"},{"name":"geo_lat_deg","role":"in","type":"double"},{"name":"obliquity_deg","role":"in","type":"double"},{"name":"sys","role":"in","type":"tm_house_system"},{"name":"lon_deg","role":"in","type":"double"},{"name":"lat_deg","role":"in","type":"double"},{"name":"out_house","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_chart_default_bodies","params":[{"name":"out_bodies","role":"out","type":"tm_body[]"}],"returns":"void","mutatesEngineState":false},{"name":"tm_chart_calc","params":[{"name":"req","role":"in","type":"tm_chart_request","optional":true},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"out_positions","role":"out","type":"tm_position[]"},{"name":"out_cusps","role":"out","type":"double[]"},{"name":"out_cusp_speeds","role":"out","type":"double[]"},{"name":"out_angles","role":"out","type":"tm_angles"}],"returns":"status","mutatesEngineState":false},{"name":"tm_set_jpl_file","params":[{"name":"filename","role":"in","type":"string"}],"returns":"status","mutatesEngineState":true},{"name":"tm_jpl_info","params":[{"name":"out_denum","role":"out","type":"int32_t"},{"name":"out_jd_start","role":"out","type":"double"},{"name":"out_jd_end","role":"out","type":"double"},{"name":"out_segment_days","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_set_ayanamsha","params":[{"name":"mode","role":"in","type":"tm_ayanamsha"},{"name":"t0","role":"in","type":"double"},{"name":"ayan_t0","role":"in","type":"double"}],"returns":"status","mutatesEngineState":true},{"name":"tm_ayanamsha_value","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"out_degrees","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_ayanamsha_name","params":[{"name":"mode","role":"in","type":"tm_ayanamsha"}],"returns":"string","mutatesEngineState":false},{"name":"tm_ayanamsha_count","params":[],"returns":"size_t","mutatesEngineState":false},{"name":"tm_set_model","params":[{"name":"kind","role":"in","type":"tm_model_kind"},{"name":"model","role":"in","type":"int32_t"}],"returns":"status","mutatesEngineState":true},{"name":"tm_get_model","params":[{"name":"kind","role":"in","type":"tm_model_kind"},{"name":"out_model","role":"out","type":"int32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_model_name","params":[{"name":"kind","role":"in","type":"tm_model_kind"},{"name":"model","role":"in","type":"int32_t"}],"returns":"string","mutatesEngineState":false},{"name":"tm_model_kind_name","params":[{"name":"kind","role":"in","type":"tm_model_kind"}],"returns":"string","mutatesEngineState":false},{"name":"tm_model_count","params":[{"name":"kind","role":"in","type":"tm_model_kind"}],"returns":"size_t","mutatesEngineState":false},{"name":"tm_model_info","params":[{"name":"kind","role":"in","type":"tm_model_kind"},{"name":"index","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_model_details"}],"returns":"status","mutatesEngineState":false},{"name":"tm_set_tidal_acceleration","params":[{"name":"arcsec_per_century2","role":"in","type":"double"}],"returns":"status","mutatesEngineState":true},{"name":"tm_clear_tidal_acceleration","params":[],"returns":"status","mutatesEngineState":true},{"name":"tm_get_tidal_acceleration","params":[{"name":"out_value","role":"out","type":"double"},{"name":"out_is_set","role":"out","type":"int32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_set_delta_t_override","params":[{"name":"seconds","role":"in","type":"double"}],"returns":"status","mutatesEngineState":true},{"name":"tm_clear_delta_t_override","params":[],"returns":"status","mutatesEngineState":true},{"name":"tm_get_delta_t_override","params":[{"name":"out_seconds","role":"out","type":"double"},{"name":"out_is_set","role":"out","type":"int32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_set_nutation_interpolation","params":[{"name":"enable","role":"in","type":"int32_t"}],"returns":"status","mutatesEngineState":true},{"name":"tm_get_nutation_interpolation","params":[{"name":"out","role":"out","type":"int32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_atmosphere_init_sized","params":[{"name":"altitude_m","role":"in","type":"double"},{"name":"struct_size","role":"in","type":"size_t"},{"name":"atm","role":"out","type":"tm_atmosphere"}],"returns":"status","mutatesEngineState":false},{"name":"tm_refract","params":[{"name":"model","role":"in","type":"tm_refraction_model"},{"name":"dir","role":"in","type":"tm_refraction_direction"},{"name":"altitude_deg","role":"in","type":"double"},{"name":"obs","role":"in","type":"tm_observer","optional":true},{"name":"atm","role":"in","type":"tm_atmosphere","optional":true},{"name":"out","role":"out","type":"tm_refraction"}],"returns":"status","mutatesEngineState":false},{"name":"tm_refract_many","params":[{"name":"model","role":"in","type":"tm_refraction_model"},{"name":"dir","role":"in","type":"tm_refraction_direction"},{"name":"altitudes_deg","role":"in","type":"double[]"},{"name":"obs","role":"in","type":"tm_observer","optional":true},{"name":"atm","role":"in","type":"tm_atmosphere","optional":true},{"name":"out","role":"out","type":"tm_refraction[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_obliquity_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"out","role":"out","type":"tm_obliquity"}],"returns":"status","mutatesEngineState":false},{"name":"tm_coord_rotate","params":[{"name":"direction","role":"in","type":"tm_frame_rotation"},{"name":"obliquity_deg","role":"in","type":"double"},{"name":"positions","role":"in","type":"tm_position[]"},{"name":"out","role":"out","type":"tm_position[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_to_horizontal","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"system","role":"in","type":"tm_horizontal_system"},{"name":"obs","role":"in","type":"tm_observer","optional":true},{"name":"atm","role":"in","type":"tm_atmosphere","optional":true},{"name":"lon_deg","role":"in","type":"double"},{"name":"lat_deg","role":"in","type":"double"},{"name":"out","role":"out","type":"tm_horizontal"}],"returns":"status","mutatesEngineState":false},{"name":"tm_to_horizontal_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"system","role":"in","type":"tm_horizontal_system"},{"name":"obs","role":"in","type":"tm_observer","optional":true},{"name":"atm","role":"in","type":"tm_atmosphere","optional":true},{"name":"coords","role":"in","type":"tm_coord[]"},{"name":"out","role":"out","type":"tm_horizontal[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_from_horizontal","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"system","role":"in","type":"tm_horizontal_system"},{"name":"obs","role":"in","type":"tm_observer","optional":true},{"name":"azimuth_deg","role":"in","type":"double"},{"name":"altitude_deg","role":"in","type":"double"},{"name":"out_lon_deg","role":"out","type":"double"},{"name":"out_lat_deg","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_embedded_coverage","params":[{"name":"out_jd_start","role":"out","type":"double"},{"name":"out_jd_end","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_body_coverage","params":[{"name":"body","role":"in","type":"tm_body"},{"name":"out","role":"out","type":"tm_coverage"}],"returns":"status","mutatesEngineState":false},{"name":"tm_loaded_files","params":[{"name":"out_files","role":"out","type":"tm_loaded_file[]"}],"returns":"void","mutatesEngineState":false},{"name":"tm_last_loaded_file","params":[{"name":"out_file","role":"out","type":"tm_loaded_file"}],"returns":"status","mutatesEngineState":false},{"name":"tm_fallback_stats_get","params":[{"name":"out","role":"out","type":"tm_fallback_stats"}],"returns":"status","mutatesEngineState":false},{"name":"tm_fallback_stats_reset","params":[],"returns":"status","mutatesEngineState":false},{"name":"tm_cache_dir_default","params":[{"name":"buf","role":"out","type":"string"}],"returns":"void","mutatesEngineState":false},{"name":"tm_nodes_apsides_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"method","role":"in","type":"tm_nodes_method"},{"name":"apsis","role":"in","type":"tm_apsis_point"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_nodes_apsides"}],"returns":"status","mutatesEngineState":false},{"name":"tm_nodes_apsides_calc_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"method","role":"in","type":"tm_nodes_method"},{"name":"apsis","role":"in","type":"tm_apsis_point"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_nodes_apsides[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_orbital_elements_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"masses","role":"in","type":"tm_mass_model"},{"name":"out","role":"out","type":"tm_orbital_elements"}],"returns":"status","mutatesEngineState":false},{"name":"tm_orbital_elements_calc_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"masses","role":"in","type":"tm_mass_model"},{"name":"out","role":"out","type":"tm_orbital_elements[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_orbit_distances_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"out","role":"out","type":"tm_orbit_distances"}],"returns":"status","mutatesEngineState":false},{"name":"tm_orbit_distances_calc_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"out","role":"out","type":"tm_orbit_distances[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_phenomena_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_phenomena"}],"returns":"status","mutatesEngineState":false},{"name":"tm_phenomena_calc_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_phenomena[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_name","params":[{"name":"star","role":"in","type":"tm_star","optional":true},{"name":"buf","role":"out","type":"string"}],"returns":"void","mutatesEngineState":false},{"name":"tm_star_designation","params":[{"name":"star","role":"in","type":"tm_star","optional":true},{"name":"buf","role":"out","type":"string"}],"returns":"void","mutatesEngineState":false},{"name":"tm_star_load_catalogue","params":[{"name":"path","role":"in","type":"string"}],"returns":"status","mutatesEngineState":true},{"name":"tm_star_count","params":[],"returns":"size_t","mutatesEngineState":false},{"name":"tm_star_find","params":[{"name":"name","role":"in","type":"string"},{"name":"out","role":"out","type":"tm_star"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_find_all","params":[{"name":"name","role":"in","type":"string"},{"name":"out","role":"out","type":"tm_star[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_query_init_sized","params":[{"name":"q","role":"out","type":"tm_star_query"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_search","params":[{"name":"query","role":"in","type":"tm_star_query","optional":true},{"name":"out","role":"out","type":"tm_star[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_calc","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"name","role":"in","type":"string"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_position"}],"returns":"status","mutatesEngineState":false},{"name":"tm_star_calc_many","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"stars","role":"in","type":"tm_star[]"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out","role":"out","type":"tm_position[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_eclipse_type_name","params":[{"name":"bit","role":"in","type":"tm_eclipse_type"}],"returns":"string","mutatesEngineState":false},{"name":"tm_eclipse_request_init_sized","params":[{"name":"req","role":"out","type":"tm_eclipse_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_solar_eclipse_search","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_solar_eclipse[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_lunar_eclipse_search","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_lunar_eclipse[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_occultation_search","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_occultation[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_solar_eclipse_search_local","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_solar_eclipse_local[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_lunar_eclipse_search_local","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_lunar_eclipse_local[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_occultation_search_local","params":[{"name":"req","role":"in","type":"tm_eclipse_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_occultation_local[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_solar_eclipse_where","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"out_location","role":"out","type":"tm_eclipse_location"},{"name":"out_attributes","role":"out","type":"tm_eclipse_attributes"},{"name":"out_type","role":"out","type":"uint32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_occultation_where","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"star","role":"in","type":"string"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"out_location","role":"out","type":"tm_eclipse_location"},{"name":"out_attributes","role":"out","type":"tm_eclipse_attributes"},{"name":"out_type","role":"out","type":"uint32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_solar_eclipse_how","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out_attributes","role":"out","type":"tm_eclipse_attributes"},{"name":"out_type","role":"out","type":"uint32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_occultation_how","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"star","role":"in","type":"string"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out_attributes","role":"out","type":"tm_eclipse_attributes"},{"name":"out_type","role":"out","type":"uint32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_lunar_eclipse_how","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"out_attributes","role":"out","type":"tm_lunar_eclipse_attributes"},{"name":"out_type","role":"out","type":"uint32_t"}],"returns":"status","mutatesEngineState":false},{"name":"tm_event_kind_name","params":[{"name":"kind","role":"in","type":"tm_event_kind"}],"returns":"string","mutatesEngineState":false},{"name":"tm_event_request_init_sized","params":[{"name":"req","role":"out","type":"tm_event_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_event_search","params":[{"name":"req","role":"in","type":"tm_event_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_event[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_gauquelin_sector","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"star","role":"in","type":"string"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"method","role":"in","type":"tm_gauquelin_method"},{"name":"refraction","role":"in","type":"int32_t"},{"name":"disc_center","role":"in","type":"int32_t"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"atmosphere","role":"in","type":"tm_atmosphere","optional":true},{"name":"out_sector","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_visibility_defaults","params":[{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"atmosphere","role":"out","type":"tm_visibility_atmosphere"},{"name":"eye","role":"out","type":"tm_observer_eye"}],"returns":"status","mutatesEngineState":false},{"name":"tm_visibility_limit","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"body","role":"in","type":"tm_body"},{"name":"star","role":"in","type":"string"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"atmosphere","role":"in","type":"tm_visibility_atmosphere","optional":true},{"name":"eye","role":"in","type":"tm_observer_eye","optional":true},{"name":"out","role":"out","type":"tm_visibility"}],"returns":"status","mutatesEngineState":false},{"name":"tm_visibility_arcus","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"atmosphere","role":"in","type":"tm_visibility_atmosphere","optional":true},{"name":"eye","role":"in","type":"tm_observer_eye","optional":true},{"name":"magnitude","role":"in","type":"double"},{"name":"object_altitude_deg","role":"in","type":"double"},{"name":"object_azimuth_deg","role":"in","type":"double"},{"name":"sun_azimuth_deg","role":"in","type":"double"},{"name":"moon_altitude_deg","role":"in","type":"double"},{"name":"moon_azimuth_deg","role":"in","type":"double"},{"name":"out_arcus_deg","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_visibility_best_altitude","params":[{"name":"jd","role":"in","type":"double"},{"name":"scale","role":"in","type":"tm_timescale"},{"name":"flags","role":"in","type":"tm_flags"},{"name":"observer","role":"in","type":"tm_observer","optional":true},{"name":"atmosphere","role":"in","type":"tm_visibility_atmosphere","optional":true},{"name":"eye","role":"in","type":"tm_observer_eye","optional":true},{"name":"magnitude","role":"in","type":"double"},{"name":"object_azimuth_deg","role":"in","type":"double"},{"name":"sun_azimuth_deg","role":"in","type":"double"},{"name":"moon_altitude_deg","role":"in","type":"double"},{"name":"moon_azimuth_deg","role":"in","type":"double"},{"name":"out","role":"out","type":"tm_visibility_best"}],"returns":"status","mutatesEngineState":false},{"name":"tm_heliacal_request_init_sized","params":[{"name":"req","role":"out","type":"tm_heliacal_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_heliacal_search","params":[{"name":"req","role":"in","type":"tm_heliacal_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_heliacal[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_heliacal_detail","params":[{"name":"req","role":"in","type":"tm_heliacal_request","optional":true},{"name":"out","role":"out","type":"tm_heliacal_details"}],"returns":"status","mutatesEngineState":false},{"name":"tm_crossing_request_init_sized","params":[{"name":"req","role":"out","type":"tm_crossing_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_crossing_search","params":[{"name":"req","role":"in","type":"tm_crossing_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_crossing[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_node_crossing_search","params":[{"name":"req","role":"in","type":"tm_crossing_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_crossing[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_calendar_request_init_sized","params":[{"name":"req","role":"out","type":"tm_calendar_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_calendar_grid","params":[{"name":"req","role":"in","type":"tm_calendar_request","optional":true},{"name":"bodies","role":"in","type":"tm_body[]"},{"name":"out_days","role":"out","type":"tm_calendar_day[]"},{"name":"out_positions","role":"out","type":"tm_position[]"}],"returns":"status","mutatesEngineState":false},{"name":"tm_scan_request_init_sized","params":[{"name":"req","role":"out","type":"tm_scan_request"}],"returns":"status","mutatesEngineState":false},{"name":"tm_scan_grid","params":[{"name":"req","role":"in","type":"tm_scan_request","optional":true},{"name":"out_capacity","role":"in","type":"size_t"},{"name":"out","role":"out","type":"tm_bracket[]"},{"name":"out_samples","role":"out","type":"size_t"},{"name":"out_jd_reached","role":"out","type":"double"}],"returns":"status","mutatesEngineState":false},{"name":"tm_angle_normalize_deg","params":[{"name":"deg","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_normalize_rad","params":[{"name":"rad","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_diff_deg","params":[{"name":"a","role":"in","type":"double"},{"name":"b","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_diff_deg_positive","params":[{"name":"a","role":"in","type":"double"},{"name":"b","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_diff_rad","params":[{"name":"a","role":"in","type":"double"},{"name":"b","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_midpoint_deg","params":[{"name":"a","role":"in","type":"double"},{"name":"b","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_angle_midpoint_rad","params":[{"name":"a","role":"in","type":"double"},{"name":"b","role":"in","type":"double"}],"returns":"double","mutatesEngineState":false},{"name":"tm_degrees_to_centiseconds","params":[{"name":"deg","role":"in","type":"double"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_centiseconds_to_degrees","params":[{"name":"centiseconds","role":"in","type":"int32_t"}],"returns":"double","mutatesEngineState":false},{"name":"tm_centiseconds_normalize","params":[{"name":"cs","role":"in","type":"int32_t"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_centiseconds_diff","params":[{"name":"a","role":"in","type":"int32_t"},{"name":"b","role":"in","type":"int32_t"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_centiseconds_diff_signed","params":[{"name":"a","role":"in","type":"int32_t"},{"name":"b","role":"in","type":"int32_t"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_centiseconds_round_seconds","params":[{"name":"cs","role":"in","type":"int32_t"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_round_half_away","params":[{"name":"x","role":"in","type":"double"}],"returns":"int32_t","mutatesEngineState":false},{"name":"tm_angle_split","params":[{"name":"deg","role":"in","type":"double"},{"name":"options","role":"in","type":"uint32_t"},{"name":"out","role":"out","type":"tm_angle_parts"}],"returns":"status","mutatesEngineState":false},{"name":"tm_angle_format","params":[{"name":"deg","role":"in","type":"double"},{"name":"style","role":"in","type":"tm_angle_style"},{"name":"decimals","role":"in","type":"int32_t"},{"name":"buf","role":"out","type":"string"}],"returns":"void","mutatesEngineState":false},{"name":"tm_zodiac_sign_name","params":[{"name":"sign","role":"in","type":"int32_t"}],"returns":"string","mutatesEngineState":false},{"name":"tm_nakshatra_name","params":[{"name":"nakshatra","role":"in","type":"int32_t"}],"returns":"string","mutatesEngineState":false},{"name":"tm_nakshatra_lord","params":[{"name":"nakshatra","role":"in","type":"int32_t"}],"returns":"string","mutatesEngineState":false},{"name":"tm_chart_blob_size","params":[{"name":"body_count","role":"in","type":"size_t"},{"name":"cusp_count","role":"in","type":"size_t"},{"name":"with_cusp_speeds","role":"in","type":"int32_t"},{"name":"with_angles","role":"in","type":"int32_t"}],"returns":"size_t","mutatesEngineState":false},{"name":"tm_ayanamsha_info","params":[{"name":"mode","role":"in","type":"tm_ayanamsha"},{"name":"out","role":"out","type":"tm_ayanamsha_details"}],"returns":"status","mutatesEngineState":false}]}"#;

/// Calls one of them by name.
///
/// # Errors
///
/// `Unsupported` for a name this adapter does not offer, `Invalid`
/// for an argument missing or of the wrong kind, and the engine's
/// own refusal otherwise.
pub(crate) fn call(
    context: *mut sys::tm_context,
    function: &str,
    args: &Map<String, Value>,
) -> Result<Value, ProviderError> {
    match function {
        "tm_status_name" => {
            let s: sys::tm_status = narrow(args, "s")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_status_name(s) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_context_release_caches" => {
            // SAFETY: the context is the adapter's own and live for this
            // call.
            unsafe { sys::tm_context_release_caches(context) };
            Ok(json!({}))
        }
        "tm_version_string" => {
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_version_string() };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_version" => {
            let mut major: ::core::ffi::c_int = 0;
            let mut minor: ::core::ffi::c_int = 0;
            let mut patch: ::core::ffi::c_int = 0;
            // SAFETY: every out-parameter is a local of the width the
            // engine declares.
            unsafe { sys::tm_version(&raw mut major, &raw mut minor, &raw mut patch) };
            Ok(json!({"major": major, "minor": minor, "patch": patch}))
        }
        "tm_body_name" => {
            let body: sys::tm_body = narrow(args, "body")?;
            let buf = fill("tm_body_name", |buffer, capacity| {
                // SAFETY: the context is the adapter's own and live for this
                // call; the buffer and its capacity are the fill protocol's
                // own, and it passes the length of what it allocated.
                unsafe { sys::tm_body_name(context, body, buffer, capacity) }
            })?;
            Ok(json!({"buf": buf}))
        }
        "tm_julian_day" => {
            let dt = optional_object(args, "dt")?
                .map(|within| read_tm_datetime(&within))
                .transpose()?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out_jd: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm.
            let answered = unsafe { sys::tm_julian_day(context, dt.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), cal, &raw mut out_jd) };
            checked(context, &before, answered, "tm_julian_day")?;
            Ok(json!({"out_jd": out_jd}))
        }
        "tm_calendar_date" => {
            let jd: f64 = number(args, "jd")?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out: sys::tm_datetime = sys::tm_datetime::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_calendar_date(context, jd, cal, &raw mut out) };
            checked(context, &before, answered, "tm_calendar_date")?;
            Ok(json!({"out": write_tm_datetime(&out)}))
        }
        "tm_utc_to_jd" => {
            let utc = optional_object(args, "utc")?
                .map(|within| read_tm_datetime(&within))
                .transpose()?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out_jd_tt: f64 = 0.0;
            let mut out_jd_ut1: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm.
            let answered = unsafe { sys::tm_utc_to_jd(context, utc.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), cal, &raw mut out_jd_tt, &raw mut out_jd_ut1) };
            checked(context, &before, answered, "tm_utc_to_jd")?;
            Ok(json!({"out_jd_tt": out_jd_tt, "out_jd_ut1": out_jd_ut1}))
        }
        "tm_jd_to_utc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out: sys::tm_datetime = sys::tm_datetime::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_jd_to_utc(context, jd, scale, cal, &raw mut out) };
            checked(context, &before, answered, "tm_jd_to_utc")?;
            Ok(json!({"out": write_tm_datetime(&out)}))
        }
        "tm_delta_t" => {
            let jd_ut1: f64 = number(args, "jd_ut1")?;
            let mut out_seconds: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_delta_t(context, jd_ut1, &raw mut out_seconds) };
            checked(context, &before, answered, "tm_delta_t")?;
            Ok(json!({"out_seconds": out_seconds}))
        }
        "tm_sidereal_time" => {
            let jd_ut1: f64 = number(args, "jd_ut1")?;
            let geo_lon_deg: f64 = number(args, "geo_lon_deg")?;
            let mut out_hours: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_sidereal_time(context, jd_ut1, geo_lon_deg, &raw mut out_hours) };
            checked(context, &before, answered, "tm_sidereal_time")?;
            Ok(json!({"out_hours": out_hours}))
        }
        "tm_day_of_week" => {
            let jd: f64 = number(args, "jd")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_day_of_week(jd) };
            Ok(json!({"return": answered}))
        }
        "tm_weekday_name" => {
            let day: sys::tm_weekday = narrow(args, "day")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_weekday_name(day) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_julian_day_many" => {
            let dts: Vec<sys::tm_datetime> = objects(args, "dts", read_tm_datetime)?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out_jd: Vec<f64> = room(dts.len(), "out_jd")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_julian_day_many(context, dts.as_ptr(), dts.len(), cal, out_jd.as_mut_ptr(), out_jd.len()) };
            checked(context, &before, answered, "tm_julian_day_many")?;
            Ok(json!({"out_jd": out_jd}))
        }
        "tm_calendar_date_many" => {
            let jds: Vec<f64> = numbers(args, "jds")?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out: Vec<sys::tm_datetime> = room(jds.len(), "out")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_calendar_date_many(context, jds.as_ptr(), jds.len(), cal, out.as_mut_ptr(), out.len()) };
            checked(context, &before, answered, "tm_calendar_date_many")?;
            Ok(json!({"out": out.iter().map(write_tm_datetime).collect::<Vec<_>>()}))
        }
        "tm_delta_t_many" => {
            let jds_ut1: Vec<f64> = numbers(args, "jds_ut1")?;
            let mut out_seconds: Vec<f64> = room(jds_ut1.len(), "out_seconds")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_delta_t_many(context, jds_ut1.as_ptr(), jds_ut1.len(), out_seconds.as_mut_ptr(), out_seconds.len()) };
            checked(context, &before, answered, "tm_delta_t_many")?;
            Ok(json!({"out_seconds": out_seconds}))
        }
        "tm_sidereal_time_many" => {
            let jds_ut1: Vec<f64> = numbers(args, "jds_ut1")?;
            let geo_lon_deg: f64 = number(args, "geo_lon_deg")?;
            let mut out_hours: Vec<f64> = room(jds_ut1.len(), "out_hours")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_sidereal_time_many(context, jds_ut1.as_ptr(), jds_ut1.len(), geo_lon_deg, out_hours.as_mut_ptr(), out_hours.len()) };
            checked(context, &before, answered, "tm_sidereal_time_many")?;
            Ok(json!({"out_hours": out_hours}))
        }
        "tm_equation_of_time" => {
            let jd_ut1: f64 = number(args, "jd_ut1")?;
            let mut out_seconds: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_equation_of_time(context, jd_ut1, &raw mut out_seconds) };
            checked(context, &before, answered, "tm_equation_of_time")?;
            Ok(json!({"out_seconds": out_seconds}))
        }
        "tm_local_mean_to_apparent" => {
            let jd_local_mean: f64 = number(args, "jd_local_mean")?;
            let geo_lon_deg: f64 = number(args, "geo_lon_deg")?;
            let mut out_jd_local_apparent: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_local_mean_to_apparent(context, jd_local_mean, geo_lon_deg, &raw mut out_jd_local_apparent) };
            checked(context, &before, answered, "tm_local_mean_to_apparent")?;
            Ok(json!({"out_jd_local_apparent": out_jd_local_apparent}))
        }
        "tm_local_apparent_to_mean" => {
            let jd_local_apparent: f64 = number(args, "jd_local_apparent")?;
            let geo_lon_deg: f64 = number(args, "geo_lon_deg")?;
            let mut out_jd_local_mean: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_local_apparent_to_mean(context, jd_local_apparent, geo_lon_deg, &raw mut out_jd_local_mean) };
            checked(context, &before, answered, "tm_local_apparent_to_mean")?;
            Ok(json!({"out_jd_local_mean": out_jd_local_mean}))
        }
        "tm_local_to_utc" => {
            let local = optional_object(args, "local")?
                .map(|within| read_tm_datetime(&within))
                .transpose()?;
            let utc_offset_hours: f64 = number(args, "utc_offset_hours")?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out_utc: sys::tm_datetime = sys::tm_datetime::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_local_to_utc(context, local.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), utc_offset_hours, cal, &raw mut out_utc) };
            checked(context, &before, answered, "tm_local_to_utc")?;
            Ok(json!({"out_utc": write_tm_datetime(&out_utc)}))
        }
        "tm_utc_to_local" => {
            let utc = optional_object(args, "utc")?
                .map(|within| read_tm_datetime(&within))
                .transpose()?;
            let utc_offset_hours: f64 = number(args, "utc_offset_hours")?;
            let cal: sys::tm_calendar = narrow(args, "cal")?;
            let mut out_local: sys::tm_datetime = sys::tm_datetime::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_utc_to_local(context, utc.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), utc_offset_hours, cal, &raw mut out_local) };
            checked(context, &before, answered, "tm_utc_to_local")?;
            Ok(json!({"out_local": write_tm_datetime(&out_local)}))
        }
        "tm_position_calc" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_position_request(&within, &keep))
                .transpose()?;
            let mut out: sys::tm_position = sys::tm_position::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_position_calc(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_position_calc")?;
            Ok(json!({"out": write_tm_position(&out)}))
        }
        "tm_position_value" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let field: sys::tm_position_field = narrow(args, "field")?;
            let mut out: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_position_value(context, jd, scale, body, flags, field, &raw mut out) };
            checked(context, &before, answered, "tm_position_value")?;
            Ok(json!({"out": out}))
        }
        "tm_position_calc_many" => {
            let keep = Keep::default();
            let reqs: Vec<sys::tm_position_request> = objects(args, "reqs", |within| read_tm_position_request(within, &keep))?;
            let mut out: Vec<sys::tm_position> = room(reqs.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_position_calc_many(context, reqs.as_ptr(), reqs.len(), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_position_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_position).collect::<Vec<_>>()}))
        }
        "tm_position_calc_grid" => {
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let jds: Vec<f64> = numbers(args, "jds")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: Vec<sys::tm_position> = room(extent(&[bodies.len(), jds.len()], "out")?, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_position_calc_grid(context, bodies.as_ptr(), bodies.len(), jds.as_ptr(), jds.len(), scale, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_position_calc_grid"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_position).collect::<Vec<_>>()}))
        }
        "tm_house_cusp_count" => {
            let sys: sys::tm_house_system = narrow(args, "sys")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_house_cusp_count(sys) };
            Ok(json!({"return": answered}))
        }
        "tm_house_system_name" => {
            let sys: sys::tm_house_system = narrow(args, "sys")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_house_system_name(sys) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_house_system_count" => {
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_house_system_count() };
            Ok(json!({"return": answered}))
        }
        "tm_houses_calc" => {
            let req = optional_object(args, "req")?
                .map(|within| read_tm_houses_request(&within))
                .transpose()?;
            // SAFETY: `tm_house_cusp_count` takes values only and answers a length.
            let mut cusps: Vec<f64> = room(req.as_ref().map_or(0, |one| unsafe { sys::tm_house_cusp_count(one.system) }), "cusps")?;
            let mut cusp_speeds: Vec<f64> = room(cusps.len(), "cusp_speeds")?;
            let mut out_angles: sys::tm_angles = sys::tm_angles::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`; every output array is a
            // `Vec` the arm sized to the engine's stated extent, passed
            // with its own length.
            let answered = unsafe { sys::tm_houses_calc(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), cusps.as_mut_ptr(), cusps.len(), cusp_speeds.as_mut_ptr(), &raw mut out_angles) };
            checked(context, &before, answered, "tm_houses_calc")?;
            Ok(json!({"cusps": cusps, "cusp_speeds": cusp_speeds, "out_angles": write_tm_angles(&out_angles)}))
        }
        "tm_house_position" => {
            let armc: f64 = number(args, "armc")?;
            let geo_lat_deg: f64 = number(args, "geo_lat_deg")?;
            let obliquity_deg: f64 = number(args, "obliquity_deg")?;
            let sys: sys::tm_house_system = narrow(args, "sys")?;
            let lon_deg: f64 = number(args, "lon_deg")?;
            let lat_deg: f64 = number(args, "lat_deg")?;
            let mut out_house: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_house_position(context, armc, geo_lat_deg, obliquity_deg, sys, lon_deg, lat_deg, &raw mut out_house) };
            checked(context, &before, answered, "tm_house_position")?;
            Ok(json!({"out_house": out_house}))
        }
        "tm_chart_default_bodies" => {
            let out_bodies = gather::<sys::tm_body>("tm_chart_default_bodies", |room| {
                // SAFETY: the output room is the gather protocol's own, passed
                // with its own length.
                let answered = unsafe { sys::tm_chart_default_bodies(room.as_mut_ptr(), room.len()) };
                Ok(answered)
            })?;
            Ok(json!({"out_bodies": out_bodies}))
        }
        "tm_chart_calc" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_chart_request(&within, &keep))
                .transpose()?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let mut out_positions: Vec<sys::tm_position> = room(bodies.len(), "out_positions")?;
            unwritten(&mut out_positions, |one| &mut one.status);
            // SAFETY: `tm_house_cusp_count` takes values only and answers a length.
            let mut out_cusps: Vec<f64> = room(req.as_ref().map_or(0, |one| unsafe { sys::tm_house_cusp_count(one.system) }), "out_cusps")?;
            let mut out_cusp_speeds: Vec<f64> = room(out_cusps.len(), "out_cusp_speeds")?;
            let mut out_angles: sys::tm_angles = sys::tm_angles::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`; every input array is a `Vec`
            // the arm owns, passed with its own length; every output array
            // is a `Vec` the arm sized to the engine's stated extent,
            // passed with its own length.
            let answered = unsafe { sys::tm_chart_calc(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), bodies.as_ptr(), bodies.len(), out_positions.as_mut_ptr(), out_positions.len(), out_cusps.as_mut_ptr(), out_cusps.len(), out_cusp_speeds.as_mut_ptr(), &raw mut out_angles) };
            settled(checked(context, &before, answered, "tm_chart_calc"), &mut out_positions, |one| &mut one.status)?;
            Ok(json!({"out_positions": out_positions.iter().map(write_tm_position).collect::<Vec<_>>(), "out_cusps": out_cusps, "out_cusp_speeds": out_cusp_speeds, "out_angles": write_tm_angles(&out_angles)}))
        }
        "tm_set_jpl_file" => {
            let filename = text(args, "filename")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every string argument is a `CString` that outlives the
            // call.
            let answered = unsafe { sys::tm_set_jpl_file(context, filename.as_ptr()) };
            checked(context, &before, answered, "tm_set_jpl_file")?;
            Ok(json!({}))
        }
        "tm_jpl_info" => {
            let mut out_denum: i32 = 0;
            let mut out_jd_start: f64 = 0.0;
            let mut out_jd_end: f64 = 0.0;
            let mut out_segment_days: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_jpl_info(context, &raw mut out_denum, &raw mut out_jd_start, &raw mut out_jd_end, &raw mut out_segment_days) };
            checked(context, &before, answered, "tm_jpl_info")?;
            Ok(json!({"out_denum": out_denum, "out_jd_start": out_jd_start, "out_jd_end": out_jd_end, "out_segment_days": out_segment_days}))
        }
        "tm_set_ayanamsha" => {
            let mode: sys::tm_ayanamsha = narrow(args, "mode")?;
            let t0: f64 = number(args, "t0")?;
            let ayan_t0: f64 = number(args, "ayan_t0")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_set_ayanamsha(context, mode, t0, ayan_t0) };
            checked(context, &before, answered, "tm_set_ayanamsha")?;
            Ok(json!({}))
        }
        "tm_ayanamsha_value" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let mut out_degrees: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_ayanamsha_value(context, jd, scale, flags, &raw mut out_degrees) };
            checked(context, &before, answered, "tm_ayanamsha_value")?;
            Ok(json!({"out_degrees": out_degrees}))
        }
        "tm_ayanamsha_name" => {
            let mode: sys::tm_ayanamsha = narrow(args, "mode")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_ayanamsha_name(mode) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_ayanamsha_count" => {
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_ayanamsha_count() };
            Ok(json!({"return": answered}))
        }
        "tm_set_model" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            let model: i32 = narrow(args, "model")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_set_model(context, kind, model) };
            checked(context, &before, answered, "tm_set_model")?;
            Ok(json!({}))
        }
        "tm_get_model" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            let mut out_model: i32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_get_model(context, kind, &raw mut out_model) };
            checked(context, &before, answered, "tm_get_model")?;
            Ok(json!({"out_model": out_model}))
        }
        "tm_model_name" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            let model: i32 = narrow(args, "model")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_model_name(kind, model) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_model_kind_name" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_model_kind_name(kind) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_model_count" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_model_count(kind) };
            Ok(json!({"return": answered}))
        }
        "tm_model_info" => {
            let kind: sys::tm_model_kind = narrow(args, "kind")?;
            let index: usize = narrow(args, "index")?;
            let mut out: sys::tm_model_details = sys::tm_model_details::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_model_info(context, kind, index, &raw mut out) };
            checked(context, &before, answered, "tm_model_info")?;
            Ok(json!({"out": write_tm_model_details(&out)}))
        }
        "tm_set_tidal_acceleration" => {
            let arcsec_per_century2: f64 = number(args, "arcsec_per_century2")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_set_tidal_acceleration(context, arcsec_per_century2) };
            checked(context, &before, answered, "tm_set_tidal_acceleration")?;
            Ok(json!({}))
        }
        "tm_clear_tidal_acceleration" => {
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_clear_tidal_acceleration(context) };
            checked(context, &before, answered, "tm_clear_tidal_acceleration")?;
            Ok(json!({}))
        }
        "tm_get_tidal_acceleration" => {
            let mut out_value: f64 = 0.0;
            let mut out_is_set: i32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_get_tidal_acceleration(context, &raw mut out_value, &raw mut out_is_set) };
            checked(context, &before, answered, "tm_get_tidal_acceleration")?;
            Ok(json!({"out_value": out_value, "out_is_set": out_is_set}))
        }
        "tm_set_delta_t_override" => {
            let seconds: f64 = number(args, "seconds")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_set_delta_t_override(context, seconds) };
            checked(context, &before, answered, "tm_set_delta_t_override")?;
            Ok(json!({}))
        }
        "tm_clear_delta_t_override" => {
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_clear_delta_t_override(context) };
            checked(context, &before, answered, "tm_clear_delta_t_override")?;
            Ok(json!({}))
        }
        "tm_get_delta_t_override" => {
            let mut out_seconds: f64 = 0.0;
            let mut out_is_set: i32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_get_delta_t_override(context, &raw mut out_seconds, &raw mut out_is_set) };
            checked(context, &before, answered, "tm_get_delta_t_override")?;
            Ok(json!({"out_seconds": out_seconds, "out_is_set": out_is_set}))
        }
        "tm_set_nutation_interpolation" => {
            let enable: i32 = narrow(args, "enable")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_set_nutation_interpolation(context, enable) };
            checked(context, &before, answered, "tm_set_nutation_interpolation")?;
            Ok(json!({}))
        }
        "tm_get_nutation_interpolation" => {
            let mut out: i32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares.
            let answered = unsafe { sys::tm_get_nutation_interpolation(context, &raw mut out) };
            checked(context, &before, answered, "tm_get_nutation_interpolation")?;
            Ok(json!({"out": out}))
        }
        "tm_atmosphere_init_sized" => {
            let mut atm: sys::tm_atmosphere = sys::tm_atmosphere::default();
            let altitude_m: f64 = number(args, "altitude_m")?;
            let struct_size: usize = narrow(args, "struct_size")?;
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_atmosphere_init_sized(&raw mut atm, altitude_m, struct_size) };
            status(answered, "tm_atmosphere_init_sized")?;
            Ok(json!({"atm": write_tm_atmosphere(&atm)}))
        }
        "tm_refract" => {
            let model: sys::tm_refraction_model = narrow(args, "model")?;
            let dir: sys::tm_refraction_direction = narrow(args, "dir")?;
            let altitude_deg: f64 = number(args, "altitude_deg")?;
            let obs = optional_object(args, "obs")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atm = optional_object(args, "atm")?
                .map(|within| read_tm_atmosphere(&within))
                .transpose()?;
            let mut out: sys::tm_refraction = sys::tm_refraction::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_refract(context, model, dir, altitude_deg, obs.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atm.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_refract")?;
            Ok(json!({"out": write_tm_refraction(&out)}))
        }
        "tm_refract_many" => {
            let model: sys::tm_refraction_model = narrow(args, "model")?;
            let dir: sys::tm_refraction_direction = narrow(args, "dir")?;
            let altitudes_deg: Vec<f64> = numbers(args, "altitudes_deg")?;
            let obs = optional_object(args, "obs")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atm = optional_object(args, "atm")?
                .map(|within| read_tm_atmosphere(&within))
                .transpose()?;
            let mut out: Vec<sys::tm_refraction> = room(altitudes_deg.len(), "out")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_refract_many(context, model, dir, altitudes_deg.as_ptr(), altitudes_deg.len(), obs.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atm.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len()) };
            checked(context, &before, answered, "tm_refract_many")?;
            Ok(json!({"out": out.iter().map(write_tm_refraction).collect::<Vec<_>>()}))
        }
        "tm_obliquity_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let mut out: sys::tm_obliquity = sys::tm_obliquity::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_obliquity_calc(context, jd, scale, &raw mut out) };
            checked(context, &before, answered, "tm_obliquity_calc")?;
            Ok(json!({"out": write_tm_obliquity(&out)}))
        }
        "tm_coord_rotate" => {
            let direction: sys::tm_frame_rotation = narrow(args, "direction")?;
            let obliquity_deg: f64 = number(args, "obliquity_deg")?;
            let positions: Vec<sys::tm_position> = objects(args, "positions", read_tm_position)?;
            let mut out: Vec<sys::tm_position> = room(positions.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_coord_rotate(context, direction, obliquity_deg, positions.as_ptr(), positions.len(), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_coord_rotate"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_position).collect::<Vec<_>>()}))
        }
        "tm_to_horizontal" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let system: sys::tm_horizontal_system = narrow(args, "system")?;
            let obs = optional_object(args, "obs")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atm = optional_object(args, "atm")?
                .map(|within| read_tm_atmosphere(&within))
                .transpose()?;
            let lon_deg: f64 = number(args, "lon_deg")?;
            let lat_deg: f64 = number(args, "lat_deg")?;
            let mut out: sys::tm_horizontal = sys::tm_horizontal::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_to_horizontal(context, jd, scale, system, obs.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atm.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), lon_deg, lat_deg, &raw mut out) };
            checked(context, &before, answered, "tm_to_horizontal")?;
            Ok(json!({"out": write_tm_horizontal(&out)}))
        }
        "tm_to_horizontal_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let system: sys::tm_horizontal_system = narrow(args, "system")?;
            let obs = optional_object(args, "obs")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atm = optional_object(args, "atm")?
                .map(|within| read_tm_atmosphere(&within))
                .transpose()?;
            let coords: Vec<sys::tm_coord> = objects(args, "coords", read_tm_coord)?;
            let mut out: Vec<sys::tm_horizontal> = room(coords.len(), "out")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_to_horizontal_many(context, jd, scale, system, obs.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atm.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), coords.as_ptr(), coords.len(), out.as_mut_ptr(), out.len()) };
            checked(context, &before, answered, "tm_to_horizontal_many")?;
            Ok(json!({"out": out.iter().map(write_tm_horizontal).collect::<Vec<_>>()}))
        }
        "tm_from_horizontal" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let system: sys::tm_horizontal_system = narrow(args, "system")?;
            let obs = optional_object(args, "obs")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let azimuth_deg: f64 = number(args, "azimuth_deg")?;
            let altitude_deg: f64 = number(args, "altitude_deg")?;
            let mut out_lon_deg: f64 = 0.0;
            let mut out_lat_deg: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm.
            let answered = unsafe { sys::tm_from_horizontal(context, jd, scale, system, obs.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), azimuth_deg, altitude_deg, &raw mut out_lon_deg, &raw mut out_lat_deg) };
            checked(context, &before, answered, "tm_from_horizontal")?;
            Ok(json!({"out_lon_deg": out_lon_deg, "out_lat_deg": out_lat_deg}))
        }
        "tm_embedded_coverage" => {
            let mut out_jd_start: f64 = 0.0;
            let mut out_jd_end: f64 = 0.0;
            // SAFETY: every out-parameter is a local of the width the
            // engine declares.
            let answered = unsafe { sys::tm_embedded_coverage(&raw mut out_jd_start, &raw mut out_jd_end) };
            status(answered, "tm_embedded_coverage")?;
            Ok(json!({"out_jd_start": out_jd_start, "out_jd_end": out_jd_end}))
        }
        "tm_body_coverage" => {
            let body: sys::tm_body = narrow(args, "body")?;
            let mut out: sys::tm_coverage = sys::tm_coverage::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_body_coverage(context, body, &raw mut out) };
            checked(context, &before, answered, "tm_body_coverage")?;
            Ok(json!({"out": write_tm_coverage(&out)}))
        }
        "tm_loaded_files" => {
            let out_files = gather::<sys::tm_loaded_file>("tm_loaded_files", |room| {
                // SAFETY: the context is the adapter's own and live for this
                // call; the output room is the gather protocol's own, passed
                // with its own length.
                let answered = unsafe { sys::tm_loaded_files(context, room.as_mut_ptr(), room.len()) };
                Ok(answered)
            })?;
            Ok(json!({"out_files": out_files.iter().map(write_tm_loaded_file).collect::<Vec<_>>()}))
        }
        "tm_last_loaded_file" => {
            let mut out_file: sys::tm_loaded_file = sys::tm_loaded_file::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_last_loaded_file(context, &raw mut out_file) };
            checked(context, &before, answered, "tm_last_loaded_file")?;
            Ok(json!({"out_file": write_tm_loaded_file(&out_file)}))
        }
        "tm_fallback_stats_get" => {
            let mut out: sys::tm_fallback_stats = sys::tm_fallback_stats::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_fallback_stats_get(context, &raw mut out) };
            checked(context, &before, answered, "tm_fallback_stats_get")?;
            Ok(json!({"out": write_tm_fallback_stats(&out)}))
        }
        "tm_fallback_stats_reset" => {
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_fallback_stats_reset(context) };
            checked(context, &before, answered, "tm_fallback_stats_reset")?;
            Ok(json!({}))
        }
        "tm_cache_dir_default" => {
            let buf = fill("tm_cache_dir_default", |buffer, capacity| {
                // SAFETY: the buffer and its capacity are the fill protocol's
                // own, and it passes the length of what it allocated.
                unsafe { sys::tm_cache_dir_default(buffer, capacity) }
            })?;
            Ok(json!({"buf": buf}))
        }
        "tm_nodes_apsides_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let method: sys::tm_nodes_method = narrow(args, "method")?;
            let apsis: sys::tm_apsis_point = narrow(args, "apsis")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: sys::tm_nodes_apsides = sys::tm_nodes_apsides::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_nodes_apsides_calc(context, jd, scale, body, flags, method, apsis, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_nodes_apsides_calc")?;
            Ok(json!({"out": write_tm_nodes_apsides(&out)}))
        }
        "tm_nodes_apsides_calc_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let method: sys::tm_nodes_method = narrow(args, "method")?;
            let apsis: sys::tm_apsis_point = narrow(args, "apsis")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: Vec<sys::tm_nodes_apsides> = room(bodies.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_nodes_apsides_calc_many(context, jd, scale, bodies.as_ptr(), bodies.len(), flags, method, apsis, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_nodes_apsides_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_nodes_apsides).collect::<Vec<_>>()}))
        }
        "tm_orbital_elements_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let masses: sys::tm_mass_model = narrow(args, "masses")?;
            let mut out: sys::tm_orbital_elements = sys::tm_orbital_elements::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_orbital_elements_calc(context, jd, scale, body, flags, masses, &raw mut out) };
            checked(context, &before, answered, "tm_orbital_elements_calc")?;
            Ok(json!({"out": write_tm_orbital_elements(&out)}))
        }
        "tm_orbital_elements_calc_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let masses: sys::tm_mass_model = narrow(args, "masses")?;
            let mut out: Vec<sys::tm_orbital_elements> = room(bodies.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_orbital_elements_calc_many(context, jd, scale, bodies.as_ptr(), bodies.len(), flags, masses, out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_orbital_elements_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_orbital_elements).collect::<Vec<_>>()}))
        }
        "tm_orbit_distances_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let mut out: sys::tm_orbit_distances = sys::tm_orbit_distances::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_orbit_distances_calc(context, jd, scale, body, flags, &raw mut out) };
            checked(context, &before, answered, "tm_orbit_distances_calc")?;
            Ok(json!({"out": write_tm_orbit_distances(&out)}))
        }
        "tm_orbit_distances_calc_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let mut out: Vec<sys::tm_orbit_distances> = room(bodies.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every input array is a `Vec` the arm owns, passed with
            // its own length; every output array is a `Vec` the arm sized
            // to the engine's stated extent, passed with its own length.
            let answered = unsafe { sys::tm_orbit_distances_calc_many(context, jd, scale, bodies.as_ptr(), bodies.len(), flags, out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_orbit_distances_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_orbit_distances).collect::<Vec<_>>()}))
        }
        "tm_phenomena_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: sys::tm_phenomena = sys::tm_phenomena::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_phenomena_calc(context, jd, scale, body, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_phenomena_calc")?;
            Ok(json!({"out": write_tm_phenomena(&out)}))
        }
        "tm_phenomena_calc_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: Vec<sys::tm_phenomena> = room(bodies.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_phenomena_calc_many(context, jd, scale, bodies.as_ptr(), bodies.len(), flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_phenomena_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_phenomena).collect::<Vec<_>>()}))
        }
        "tm_star_name" => {
            let star = optional_object(args, "star")?
                .map(|within| read_tm_star(&within))
                .transpose()?;
            let buf = fill("tm_star_name", |buffer, capacity| {
                // SAFETY: the context is the adapter's own and live for this
                // call; every struct argument is a local of the type the
                // engine declares, its extent filled by the arm; the buffer
                // and its capacity are the fill protocol's own, and it passes
                // the length of what it allocated.
                unsafe { sys::tm_star_name(context, star.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), buffer, capacity) }
            })?;
            Ok(json!({"buf": buf}))
        }
        "tm_star_designation" => {
            let star = optional_object(args, "star")?
                .map(|within| read_tm_star(&within))
                .transpose()?;
            let buf = fill("tm_star_designation", |buffer, capacity| {
                // SAFETY: the context is the adapter's own and live for this
                // call; every struct argument is a local of the type the
                // engine declares, its extent filled by the arm; the buffer
                // and its capacity are the fill protocol's own, and it passes
                // the length of what it allocated.
                unsafe { sys::tm_star_designation(context, star.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), buffer, capacity) }
            })?;
            Ok(json!({"buf": buf}))
        }
        "tm_star_load_catalogue" => {
            let path = text(args, "path")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every string argument is a `CString` that outlives the
            // call.
            let answered = unsafe { sys::tm_star_load_catalogue(context, path.as_ptr()) };
            checked(context, &before, answered, "tm_star_load_catalogue")?;
            Ok(json!({}))
        }
        "tm_star_count" => {
            // SAFETY: the context is the adapter's own and live for this
            // call.
            let answered = unsafe { sys::tm_star_count(context) };
            Ok(json!({"return": answered}))
        }
        "tm_star_find" => {
            let name = text(args, "name")?;
            let mut out: sys::tm_star = sys::tm_star::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every string argument is a `CString` that outlives the
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_star_find(context, name.as_ptr(), &raw mut out) };
            checked(context, &before, answered, "tm_star_find")?;
            Ok(json!({"out": write_tm_star(&out)}))
        }
        "tm_star_find_all" => {
            let name = text(args, "name")?;
            let out = gather::<sys::tm_star>("tm_star_find_all", |room| {
                let mut out_count: usize = 0;
                let before = record(context);
                // SAFETY: the context is the adapter's own and live for this
                // call; every out-parameter is a local of the width the engine
                // declares; every string argument is a `CString` that outlives
                // the call; the output room is the gather protocol's own,
                // passed with its own length.
                let answered = unsafe { sys::tm_star_find_all(context, name.as_ptr(), room.as_mut_ptr(), room.len(), &raw mut out_count) };
                checked(context, &before, answered, "tm_star_find_all")?;
                Ok(out_count)
            })?;
            Ok(json!({"out": out.iter().map(write_tm_star).collect::<Vec<_>>()}))
        }
        "tm_star_query_init_sized" => {
            let mut q: sys::tm_star_query = sys::tm_star_query::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_star_query_init_sized(&raw mut q, core::mem::size_of_val(&q)) };
            status(answered, "tm_star_query_init_sized")?;
            Ok(json!({"q": write_tm_star_query(&q)}))
        }
        "tm_star_search" => {
            let keep = Keep::default();
            let query = optional_object(args, "query")?
                .map(|within| read_tm_star_query(&within, &keep))
                .transpose()?;
            let out = gather::<sys::tm_star>("tm_star_search", |room| {
                let mut out_count: usize = 0;
                let before = record(context);
                // SAFETY: the context is the adapter's own and live for this
                // call; every out-parameter is a local of the width the engine
                // declares; every struct argument is a local of the type the
                // engine declares, its extent filled by the arm; the output
                // room is the gather protocol's own, passed with its own
                // length.
                let answered = unsafe { sys::tm_star_search(context, query.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), room.as_mut_ptr(), room.len(), &raw mut out_count) };
                checked(context, &before, answered, "tm_star_search")?;
                Ok(out_count)
            })?;
            Ok(json!({"out": out.iter().map(write_tm_star).collect::<Vec<_>>()}))
        }
        "tm_star_calc" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let name = text(args, "name")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: sys::tm_position = sys::tm_position::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every string argument is a `CString` that outlives the
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_star_calc(context, jd, scale, name.as_ptr(), flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_star_calc")?;
            Ok(json!({"out": write_tm_position(&out)}))
        }
        "tm_star_calc_many" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let stars: Vec<sys::tm_star> = objects(args, "stars", read_tm_star)?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out: Vec<sys::tm_position> = room(stars.len(), "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_star_calc_many(context, jd, scale, stars.as_ptr(), stars.len(), flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len()) };
            settled(checked(context, &before, answered, "tm_star_calc_many"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_position).collect::<Vec<_>>()}))
        }
        "tm_eclipse_type_name" => {
            let bit: sys::tm_eclipse_type = narrow(args, "bit")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_eclipse_type_name(bit) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_eclipse_request_init_sized" => {
            let mut req: sys::tm_eclipse_request = sys::tm_eclipse_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_eclipse_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_eclipse_request_init_sized")?;
            Ok(json!({"req": write_tm_eclipse_request(&req)}))
        }
        "tm_solar_eclipse_search" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_solar_eclipse> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_solar_eclipse_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_solar_eclipse_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_solar_eclipse).collect::<Vec<_>>()}))
        }
        "tm_lunar_eclipse_search" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_lunar_eclipse> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_lunar_eclipse_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_lunar_eclipse_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_lunar_eclipse).collect::<Vec<_>>()}))
        }
        "tm_occultation_search" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_occultation> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_occultation_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_occultation_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_occultation).collect::<Vec<_>>()}))
        }
        "tm_solar_eclipse_search_local" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_solar_eclipse_local> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_solar_eclipse_search_local(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_solar_eclipse_search_local"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_solar_eclipse_local).collect::<Vec<_>>()}))
        }
        "tm_lunar_eclipse_search_local" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_lunar_eclipse_local> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_lunar_eclipse_search_local(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_lunar_eclipse_search_local"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_lunar_eclipse_local).collect::<Vec<_>>()}))
        }
        "tm_occultation_search_local" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_eclipse_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_occultation_local> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_occultation_search_local(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_occultation_search_local"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_occultation_local).collect::<Vec<_>>()}))
        }
        "tm_solar_eclipse_where" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let mut out_location: sys::tm_eclipse_location = sys::tm_eclipse_location::default();
            let mut out_attributes: sys::tm_eclipse_attributes = sys::tm_eclipse_attributes::default();
            let mut out_type: u32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_solar_eclipse_where(context, jd, scale, flags, &raw mut out_location, &raw mut out_attributes, &raw mut out_type) };
            checked(context, &before, answered, "tm_solar_eclipse_where")?;
            Ok(json!({"out_location": write_tm_eclipse_location(&out_location), "out_attributes": write_tm_eclipse_attributes(&out_attributes), "out_type": out_type}))
        }
        "tm_occultation_where" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let star = text(args, "star")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let mut out_location: sys::tm_eclipse_location = sys::tm_eclipse_location::default();
            let mut out_attributes: sys::tm_eclipse_attributes = sys::tm_eclipse_attributes::default();
            let mut out_type: u32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every string argument is a `CString` that outlives
            // the call; every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_occultation_where(context, jd, scale, body, star.as_ptr(), flags, &raw mut out_location, &raw mut out_attributes, &raw mut out_type) };
            checked(context, &before, answered, "tm_occultation_where")?;
            Ok(json!({"out_location": write_tm_eclipse_location(&out_location), "out_attributes": write_tm_eclipse_attributes(&out_attributes), "out_type": out_type}))
        }
        "tm_solar_eclipse_how" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out_attributes: sys::tm_eclipse_attributes = sys::tm_eclipse_attributes::default();
            let mut out_type: u32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_solar_eclipse_how(context, jd, scale, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out_attributes, &raw mut out_type) };
            checked(context, &before, answered, "tm_solar_eclipse_how")?;
            Ok(json!({"out_attributes": write_tm_eclipse_attributes(&out_attributes), "out_type": out_type}))
        }
        "tm_occultation_how" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let star = text(args, "star")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out_attributes: sys::tm_eclipse_attributes = sys::tm_eclipse_attributes::default();
            let mut out_type: u32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every string argument is a `CString` that outlives
            // the call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_occultation_how(context, jd, scale, body, star.as_ptr(), flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out_attributes, &raw mut out_type) };
            checked(context, &before, answered, "tm_occultation_how")?;
            Ok(json!({"out_attributes": write_tm_eclipse_attributes(&out_attributes), "out_type": out_type}))
        }
        "tm_lunar_eclipse_how" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut out_attributes: sys::tm_lunar_eclipse_attributes = sys::tm_lunar_eclipse_attributes::default();
            let mut out_type: u32 = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_lunar_eclipse_how(context, jd, scale, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out_attributes, &raw mut out_type) };
            checked(context, &before, answered, "tm_lunar_eclipse_how")?;
            Ok(json!({"out_attributes": write_tm_lunar_eclipse_attributes(&out_attributes), "out_type": out_type}))
        }
        "tm_event_kind_name" => {
            let kind: sys::tm_event_kind = narrow(args, "kind")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_event_kind_name(kind) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_event_request_init_sized" => {
            let mut req: sys::tm_event_request = sys::tm_event_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_event_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_event_request_init_sized")?;
            Ok(json!({"req": write_tm_event_request(&req)}))
        }
        "tm_event_search" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_event_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_event> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_event_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_event_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_event).collect::<Vec<_>>()}))
        }
        "tm_gauquelin_sector" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let star = text(args, "star")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let method: sys::tm_gauquelin_method = narrow(args, "method")?;
            let refraction: i32 = narrow(args, "refraction")?;
            let disc_center: i32 = narrow(args, "disc_center")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atmosphere = optional_object(args, "atmosphere")?
                .map(|within| read_tm_atmosphere(&within))
                .transpose()?;
            let mut out_sector: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every string argument is a `CString` that outlives
            // the call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm.
            let answered = unsafe { sys::tm_gauquelin_sector(context, jd, scale, body, star.as_ptr(), flags, method, refraction, disc_center, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atmosphere.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out_sector) };
            checked(context, &before, answered, "tm_gauquelin_sector")?;
            Ok(json!({"out_sector": out_sector}))
        }
        "tm_visibility_defaults" => {
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let mut atmosphere: sys::tm_visibility_atmosphere = sys::tm_visibility_atmosphere::default();
            let mut eye: sys::tm_observer_eye = sys::tm_observer_eye::default();
            // SAFETY: every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_visibility_defaults(observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut atmosphere, &raw mut eye) };
            status(answered, "tm_visibility_defaults")?;
            Ok(json!({"atmosphere": write_tm_visibility_atmosphere(&atmosphere), "eye": write_tm_observer_eye(&eye)}))
        }
        "tm_visibility_limit" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let body: sys::tm_body = narrow(args, "body")?;
            let star = text(args, "star")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atmosphere = optional_object(args, "atmosphere")?
                .map(|within| read_tm_visibility_atmosphere(&within))
                .transpose()?;
            let eye = optional_object(args, "eye")?
                .map(|within| read_tm_observer_eye(&within))
                .transpose()?;
            let mut out: sys::tm_visibility = sys::tm_visibility::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every string argument is a `CString` that outlives the
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_visibility_limit(context, jd, scale, body, star.as_ptr(), flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atmosphere.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), eye.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_visibility_limit")?;
            Ok(json!({"out": write_tm_visibility(&out)}))
        }
        "tm_visibility_arcus" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atmosphere = optional_object(args, "atmosphere")?
                .map(|within| read_tm_visibility_atmosphere(&within))
                .transpose()?;
            let eye = optional_object(args, "eye")?
                .map(|within| read_tm_observer_eye(&within))
                .transpose()?;
            let magnitude: f64 = number(args, "magnitude")?;
            let object_altitude_deg: f64 = number(args, "object_altitude_deg")?;
            let object_azimuth_deg: f64 = number(args, "object_azimuth_deg")?;
            let sun_azimuth_deg: f64 = number(args, "sun_azimuth_deg")?;
            let moon_altitude_deg: f64 = number(args, "moon_altitude_deg")?;
            let moon_azimuth_deg: f64 = number(args, "moon_azimuth_deg")?;
            let mut out_arcus_deg: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm.
            let answered = unsafe { sys::tm_visibility_arcus(context, jd, scale, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atmosphere.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), eye.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), magnitude, object_altitude_deg, object_azimuth_deg, sun_azimuth_deg, moon_altitude_deg, moon_azimuth_deg, &raw mut out_arcus_deg) };
            checked(context, &before, answered, "tm_visibility_arcus")?;
            Ok(json!({"out_arcus_deg": out_arcus_deg}))
        }
        "tm_visibility_best_altitude" => {
            let jd: f64 = number(args, "jd")?;
            let scale: sys::tm_timescale = narrow(args, "scale")?;
            let flags: sys::tm_flags = narrow(args, "flags")?;
            let observer = optional_object(args, "observer")?
                .map(|within| read_tm_observer(&within))
                .transpose()?;
            let atmosphere = optional_object(args, "atmosphere")?
                .map(|within| read_tm_visibility_atmosphere(&within))
                .transpose()?;
            let eye = optional_object(args, "eye")?
                .map(|within| read_tm_observer_eye(&within))
                .transpose()?;
            let magnitude: f64 = number(args, "magnitude")?;
            let object_azimuth_deg: f64 = number(args, "object_azimuth_deg")?;
            let sun_azimuth_deg: f64 = number(args, "sun_azimuth_deg")?;
            let moon_altitude_deg: f64 = number(args, "moon_altitude_deg")?;
            let moon_azimuth_deg: f64 = number(args, "moon_azimuth_deg")?;
            let mut out: sys::tm_visibility_best = sys::tm_visibility_best::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_visibility_best_altitude(context, jd, scale, flags, observer.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), atmosphere.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), eye.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), magnitude, object_azimuth_deg, sun_azimuth_deg, moon_altitude_deg, moon_azimuth_deg, &raw mut out) };
            checked(context, &before, answered, "tm_visibility_best_altitude")?;
            Ok(json!({"out": write_tm_visibility_best(&out)}))
        }
        "tm_heliacal_request_init_sized" => {
            let mut req: sys::tm_heliacal_request = sys::tm_heliacal_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_heliacal_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_heliacal_request_init_sized")?;
            Ok(json!({"req": write_tm_heliacal_request(&req)}))
        }
        "tm_heliacal_search" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_heliacal_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_heliacal> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_heliacal_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_heliacal_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_heliacal).collect::<Vec<_>>()}))
        }
        "tm_heliacal_detail" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_heliacal_request(&within, &keep))
                .transpose()?;
            let mut out: sys::tm_heliacal_details = sys::tm_heliacal_details::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every struct
            // out-parameter is a local of the type the engine declares,
            // its extent filled by `default`.
            let answered = unsafe { sys::tm_heliacal_detail(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), &raw mut out) };
            checked(context, &before, answered, "tm_heliacal_detail")?;
            Ok(json!({"out": write_tm_heliacal_details(&out)}))
        }
        "tm_crossing_request_init_sized" => {
            let mut req: sys::tm_crossing_request = sys::tm_crossing_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_crossing_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_crossing_request_init_sized")?;
            Ok(json!({"req": write_tm_crossing_request(&req)}))
        }
        "tm_crossing_search" => {
            let req = optional_object(args, "req")?
                .map(|within| read_tm_crossing_request(&within))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_crossing> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_crossing_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_crossing_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_crossing).collect::<Vec<_>>()}))
        }
        "tm_node_crossing_search" => {
            let req = optional_object(args, "req")?
                .map(|within| read_tm_crossing_request(&within))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_crossing> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_node_crossing_search(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_node_crossing_search"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_crossing).collect::<Vec<_>>()}))
        }
        "tm_calendar_request_init_sized" => {
            let mut req: sys::tm_calendar_request = sys::tm_calendar_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_calendar_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_calendar_request_init_sized")?;
            Ok(json!({"req": write_tm_calendar_request(&req)}))
        }
        "tm_calendar_grid" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_calendar_request(&within, &keep))
                .transpose()?;
            let bodies: Vec<sys::tm_body> = wholes(args, "bodies")?;
            let mut out_days: Vec<sys::tm_calendar_day> = room(req.as_ref().map_or(Ok(0), |one| length(one.day_count, "req.day_count"))?, "out_days")?;
            unwritten(&mut out_days, |one| &mut one.status);
            let mut out_positions: Vec<sys::tm_position> = room(extent(&[req.as_ref().map_or(Ok(0), |one| length(one.day_count, "req.day_count"))?, bodies.len()], "out_positions")?, "out_positions")?;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every input
            // array is a `Vec` the arm owns, passed with its own length;
            // every output array is a `Vec` the arm sized to the engine's
            // stated extent, passed with its own length.
            let answered = unsafe { sys::tm_calendar_grid(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), bodies.as_ptr(), bodies.len(), out_days.as_mut_ptr(), out_days.len(), out_positions.as_mut_ptr(), out_positions.len()) };
            settled(checked(context, &before, answered, "tm_calendar_grid"), &mut out_days, |one| &mut one.status)?;
            Ok(json!({"out_days": out_days.iter().map(write_tm_calendar_day).collect::<Vec<_>>(), "out_positions": out_positions.iter().map(write_tm_position).collect::<Vec<_>>()}))
        }
        "tm_scan_request_init_sized" => {
            let mut req: sys::tm_scan_request = sys::tm_scan_request::default();
            // SAFETY: every struct out-parameter is a local of the type
            // the engine declares, its extent filled by `default`; the
            // extent passed is the size of that local.
            let answered = unsafe { sys::tm_scan_request_init_sized(&raw mut req, core::mem::size_of_val(&req)) };
            status(answered, "tm_scan_request_init_sized")?;
            Ok(json!({"req": write_tm_scan_request(&req)}))
        }
        "tm_scan_grid" => {
            let keep = Keep::default();
            let req = optional_object(args, "req")?
                .map(|within| read_tm_scan_request(&within, &keep))
                .transpose()?;
            let out_capacity: usize = narrow(args, "out_capacity")?;
            let mut out: Vec<sys::tm_bracket> = room(out_capacity, "out")?;
            unwritten(&mut out, |one| &mut one.status);
            let mut out_count: usize = 0;
            let mut out_samples: usize = 0;
            let mut out_jd_reached: f64 = 0.0;
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every out-parameter is a local of the width the engine
            // declares; every struct argument is a local of the type the
            // engine declares, its extent filled by the arm; every output
            // array is a `Vec` the arm sized to the engine's stated
            // extent, passed with its own length.
            let answered = unsafe { sys::tm_scan_grid(context, req.as_ref().map_or(core::ptr::null(), core::ptr::from_ref), out.as_mut_ptr(), out.len(), &raw mut out_count, &raw mut out_samples, &raw mut out_jd_reached) };
            out.truncate(out_count);
            settled(checked(context, &before, answered, "tm_scan_grid"), &mut out, |one| &mut one.status)?;
            Ok(json!({"out": out.iter().map(write_tm_bracket).collect::<Vec<_>>(), "out_samples": out_samples, "out_jd_reached": out_jd_reached}))
        }
        "tm_angle_normalize_deg" => {
            let deg: f64 = number(args, "deg")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_normalize_deg(deg) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_normalize_rad" => {
            let rad: f64 = number(args, "rad")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_normalize_rad(rad) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_diff_deg" => {
            let a: f64 = number(args, "a")?;
            let b: f64 = number(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_diff_deg(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_diff_deg_positive" => {
            let a: f64 = number(args, "a")?;
            let b: f64 = number(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_diff_deg_positive(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_diff_rad" => {
            let a: f64 = number(args, "a")?;
            let b: f64 = number(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_diff_rad(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_midpoint_deg" => {
            let a: f64 = number(args, "a")?;
            let b: f64 = number(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_midpoint_deg(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_midpoint_rad" => {
            let a: f64 = number(args, "a")?;
            let b: f64 = number(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_angle_midpoint_rad(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_degrees_to_centiseconds" => {
            let deg: f64 = number(args, "deg")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_degrees_to_centiseconds(deg) };
            Ok(json!({"return": answered}))
        }
        "tm_centiseconds_to_degrees" => {
            let centiseconds: i32 = narrow(args, "centiseconds")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_centiseconds_to_degrees(centiseconds) };
            Ok(json!({"return": answered}))
        }
        "tm_centiseconds_normalize" => {
            let cs: i32 = narrow(args, "cs")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_centiseconds_normalize(cs) };
            Ok(json!({"return": answered}))
        }
        "tm_centiseconds_diff" => {
            let a: i32 = narrow(args, "a")?;
            let b: i32 = narrow(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_centiseconds_diff(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_centiseconds_diff_signed" => {
            let a: i32 = narrow(args, "a")?;
            let b: i32 = narrow(args, "b")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_centiseconds_diff_signed(a, b) };
            Ok(json!({"return": answered}))
        }
        "tm_centiseconds_round_seconds" => {
            let cs: i32 = narrow(args, "cs")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_centiseconds_round_seconds(cs) };
            Ok(json!({"return": answered}))
        }
        "tm_round_half_away" => {
            let x: f64 = number(args, "x")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_round_half_away(x) };
            Ok(json!({"return": answered}))
        }
        "tm_angle_split" => {
            let deg: f64 = number(args, "deg")?;
            let options: u32 = narrow(args, "options")?;
            let mut out: sys::tm_angle_parts = sys::tm_angle_parts::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_angle_split(context, deg, options, &raw mut out) };
            checked(context, &before, answered, "tm_angle_split")?;
            Ok(json!({"out": write_tm_angle_parts(&out)}))
        }
        "tm_angle_format" => {
            let deg: f64 = number(args, "deg")?;
            let style: sys::tm_angle_style = narrow(args, "style")?;
            let decimals: i32 = narrow(args, "decimals")?;
            let buf = fill("tm_angle_format", |buffer, capacity| {
                // SAFETY: the context is the adapter's own and live for this
                // call; the buffer and its capacity are the fill protocol's
                // own, and it passes the length of what it allocated.
                unsafe { sys::tm_angle_format(context, deg, style, decimals, buffer, capacity) }
            })?;
            Ok(json!({"buf": buf}))
        }
        "tm_zodiac_sign_name" => {
            let sign: i32 = narrow(args, "sign")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_zodiac_sign_name(sign) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_nakshatra_name" => {
            let nakshatra: i32 = narrow(args, "nakshatra")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_nakshatra_name(nakshatra) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_nakshatra_lord" => {
            let nakshatra: i32 = narrow(args, "nakshatra")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_nakshatra_lord(nakshatra) };
            Ok(json!({"return": borrowed(answered)}))
        }
        "tm_chart_blob_size" => {
            let body_count: usize = narrow(args, "body_count")?;
            let cusp_count: usize = narrow(args, "cusp_count")?;
            let with_cusp_speeds: i32 = narrow(args, "with_cusp_speeds")?;
            let with_angles: i32 = narrow(args, "with_angles")?;
            // SAFETY: the call takes nothing but values of the widths the
            // engine declares.
            let answered = unsafe { sys::tm_chart_blob_size(body_count, cusp_count, with_cusp_speeds, with_angles) };
            Ok(json!({"return": answered}))
        }
        "tm_ayanamsha_info" => {
            let mode: sys::tm_ayanamsha = narrow(args, "mode")?;
            let mut out: sys::tm_ayanamsha_details = sys::tm_ayanamsha_details::default();
            let before = record(context);
            // SAFETY: the context is the adapter's own and live for this
            // call; every struct out-parameter is a local of the type the
            // engine declares, its extent filled by `default`.
            let answered = unsafe { sys::tm_ayanamsha_info(context, mode, &raw mut out) };
            checked(context, &before, answered, "tm_ayanamsha_info")?;
            Ok(json!({"out": write_tm_ayanamsha_details(&out)}))
        }
        other => Err(ProviderError::unsupported(format!(
            "the engine offers no `{other}` through this adapter; \
             `native_manifest` lists what it does"
        ))),
    }
}

/// A `tm_datetime` read out of the object a caller passed.
fn read_tm_datetime(within: &Within<'_>) -> Result<sys::tm_datetime, ProviderError> {
    Ok(sys::tm_datetime {
        struct_size: core::mem::size_of::<sys::tm_datetime>(),
        year: within.narrow("year")?,
        month: within.narrow("month")?,
        day: within.narrow("day")?,
        hour: within.narrow("hour")?,
        minute: within.narrow("minute")?,
        second: within.number("second")?,
    })
}

/// A `tm_observer` read out of the object a caller passed.
fn read_tm_observer(within: &Within<'_>) -> Result<sys::tm_observer, ProviderError> {
    Ok(sys::tm_observer {
        struct_size: core::mem::size_of::<sys::tm_observer>(),
        longitude_deg: within.number("longitude_deg")?,
        latitude_deg: within.number("latitude_deg")?,
        altitude_m: within.number("altitude_m")?,
    })
}

/// A `tm_position` read out of the object a caller passed.
fn read_tm_position(within: &Within<'_>) -> Result<sys::tm_position, ProviderError> {
    Ok(sys::tm_position {
        struct_size: core::mem::size_of::<sys::tm_position>(),
        lon: within.number("lon")?,
        lat: within.number("lat")?,
        dist: within.number("dist")?,
        lon_speed: within.number("lon_speed")?,
        lat_speed: within.number("lat_speed")?,
        dist_speed: within.number("dist_speed")?,
        flags_used: within.narrow("flags_used")?,
        status: within.narrow("status")?,
    })
}

/// A `tm_position_request` read out of the object a caller passed.
fn read_tm_position_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_position_request, ProviderError> {
    Ok(sys::tm_position_request {
        struct_size: core::mem::size_of::<sys::tm_position_request>(),
        jd: within.number("jd")?,
        scale: within.narrow("scale")?,
        body: within.narrow("body")?,
        flags: within.narrow("flags")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
        center: within.narrow("center")?,
        ayanamsha: within.narrow("ayanamsha")?,
        ayanamsha_set: within.narrow("ayanamsha_set")?,
    })
}

/// A `tm_houses_request` read out of the object a caller passed.
fn read_tm_houses_request(within: &Within<'_>) -> Result<sys::tm_houses_request, ProviderError> {
    Ok(sys::tm_houses_request {
        struct_size: core::mem::size_of::<sys::tm_houses_request>(),
        jd_ut1: within.number("jd_ut1")?,
        geo_lat_deg: within.number("geo_lat_deg")?,
        geo_lon_deg: within.number("geo_lon_deg")?,
        system: within.narrow("system")?,
        flags: within.narrow("flags")?,
    })
}

/// A `tm_chart_request` read out of the object a caller passed.
fn read_tm_chart_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_chart_request, ProviderError> {
    Ok(sys::tm_chart_request {
        struct_size: core::mem::size_of::<sys::tm_chart_request>(),
        jd: within.number("jd")?,
        scale: within.narrow("scale")?,
        parts: within.narrow("parts")?,
        place: within.pointer("place", keep, read_tm_observer)?,
        flags: within.narrow("flags")?,
        system: within.narrow("system")?,
        house_flags: within.narrow("house_flags")?,
    })
}

/// A `tm_atmosphere` read out of the object a caller passed.
fn read_tm_atmosphere(within: &Within<'_>) -> Result<sys::tm_atmosphere, ProviderError> {
    Ok(sys::tm_atmosphere {
        struct_size: core::mem::size_of::<sys::tm_atmosphere>(),
        pressure_mbar: within.number("pressure_mbar")?,
        temperature_c: within.number("temperature_c")?,
        lapse_rate_k_per_m: within.number("lapse_rate_k_per_m")?,
    })
}

/// A `tm_coord` read out of the object a caller passed.
fn read_tm_coord(within: &Within<'_>) -> Result<sys::tm_coord, ProviderError> {
    Ok(sys::tm_coord {
        lon: within.number("lon")?,
        lat: within.number("lat")?,
    })
}

/// A `tm_star` read out of the object a caller passed.
fn read_tm_star(within: &Within<'_>) -> Result<sys::tm_star, ProviderError> {
    Ok(sys::tm_star {
        struct_size: core::mem::size_of::<sys::tm_star>(),
        id: within.narrow("id")?,
        catalogue_generation: within.narrow("catalogue_generation")?,
        magnitude: within.number("magnitude")?,
        epoch: within.number("epoch")?,
        ra_deg: within.number("ra_deg")?,
        dec_deg: within.number("dec_deg")?,
        pm_ra_arcsec_per_century: within.number("pm_ra_arcsec_per_century")?,
        pm_dec_arcsec_per_century: within.number("pm_dec_arcsec_per_century")?,
        radial_velocity_au_per_century: within.number("radial_velocity_au_per_century")?,
        parallax_arcsec: within.number("parallax_arcsec")?,
    })
}

/// A `tm_star_query` read out of the object a caller passed.
fn read_tm_star_query(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_star_query, ProviderError> {
    Ok(sys::tm_star_query {
        struct_size: core::mem::size_of::<sys::tm_star_query>(),
        use_magnitude: within.narrow("use_magnitude")?,
        magnitude_min: within.number("magnitude_min")?,
        magnitude_max: within.number("magnitude_max")?,
        ra_deg: within.number("ra_deg")?,
        dec_deg: within.number("dec_deg")?,
        radius_deg: within.number("radius_deg")?,
        name_contains: within.text("name_contains", keep)?,
    })
}

/// A `tm_eclipse_request` read out of the object a caller passed.
fn read_tm_eclipse_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_eclipse_request, ProviderError> {
    Ok(sys::tm_eclipse_request {
        struct_size: core::mem::size_of::<sys::tm_eclipse_request>(),
        jd_start: within.number("jd_start")?,
        scale: within.narrow("scale")?,
        flags: within.narrow("flags")?,
        want: within.narrow("want")?,
        backward: within.narrow("backward")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
        body: within.narrow("body")?,
        star: within.text("star", keep)?,
        one_try: within.narrow("one_try")?,
        jd_end: within.number("jd_end")?,
    })
}

/// A `tm_event_request` read out of the object a caller passed.
fn read_tm_event_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_event_request, ProviderError> {
    Ok(sys::tm_event_request {
        struct_size: core::mem::size_of::<sys::tm_event_request>(),
        jd_start: within.number("jd_start")?,
        scale: within.narrow("scale")?,
        body: within.narrow("body")?,
        star: within.text("star", keep)?,
        flags: within.narrow("flags")?,
        kind: within.narrow("kind")?,
        options: within.narrow("options")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
        atmosphere: within.pointer("atmosphere", keep, read_tm_atmosphere)?,
        horizon_height_deg: within.number("horizon_height_deg")?,
        jd_end: within.number("jd_end")?,
    })
}

/// A `tm_visibility_atmosphere` read out of the object a caller passed.
fn read_tm_visibility_atmosphere(within: &Within<'_>) -> Result<sys::tm_visibility_atmosphere, ProviderError> {
    Ok(sys::tm_visibility_atmosphere {
        struct_size: core::mem::size_of::<sys::tm_visibility_atmosphere>(),
        pressure_mbar: within.number("pressure_mbar")?,
        temperature_c: within.number("temperature_c")?,
        humidity_pct: within.number("humidity_pct")?,
        haze: within.number("haze")?,
    })
}

/// A `tm_observer_eye` read out of the object a caller passed.
fn read_tm_observer_eye(within: &Within<'_>) -> Result<sys::tm_observer_eye, ProviderError> {
    Ok(sys::tm_observer_eye {
        struct_size: core::mem::size_of::<sys::tm_observer_eye>(),
        age_years: within.number("age_years")?,
        snellen: within.number("snellen")?,
        monocular: within.narrow("monocular")?,
        use_optics: within.narrow("use_optics")?,
        magnification: within.number("magnification")?,
        aperture_mm: within.number("aperture_mm")?,
        transmission: within.number("transmission")?,
    })
}

/// A `tm_heliacal_request` read out of the object a caller passed.
fn read_tm_heliacal_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_heliacal_request, ProviderError> {
    Ok(sys::tm_heliacal_request {
        struct_size: core::mem::size_of::<sys::tm_heliacal_request>(),
        jd_start: within.number("jd_start")?,
        scale: within.narrow("scale")?,
        body: within.narrow("body")?,
        star: within.text("star", keep)?,
        flags: within.narrow("flags")?,
        kind: within.narrow("kind")?,
        options: within.narrow("options")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
        atmosphere: within.pointer("atmosphere", keep, read_tm_visibility_atmosphere)?,
        eye: within.pointer("eye", keep, read_tm_observer_eye)?,
        jd_end: within.number("jd_end")?,
    })
}

/// A `tm_crossing_request` read out of the object a caller passed.
fn read_tm_crossing_request(within: &Within<'_>) -> Result<sys::tm_crossing_request, ProviderError> {
    Ok(sys::tm_crossing_request {
        struct_size: core::mem::size_of::<sys::tm_crossing_request>(),
        jd_start: within.number("jd_start")?,
        scale: within.narrow("scale")?,
        body: within.narrow("body")?,
        target_deg: within.number("target_deg")?,
        flags: within.narrow("flags")?,
        backward: within.narrow("backward")?,
        jd_end: within.number("jd_end")?,
        quantity: within.narrow("quantity")?,
        step_deg: within.number("step_deg")?,
        body_b: within.narrow("body_b")?,
        coeff_a: within.number("coeff_a")?,
        coeff_b: within.number("coeff_b")?,
    })
}

/// A `tm_calendar_request` read out of the object a caller passed.
fn read_tm_calendar_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_calendar_request, ProviderError> {
    Ok(sys::tm_calendar_request {
        struct_size: core::mem::size_of::<sys::tm_calendar_request>(),
        jd_start: within.number("jd_start")?,
        day_count: within.narrow("day_count")?,
        anchor: within.narrow("anchor")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
        anchor_body: within.narrow("anchor_body")?,
        flags: within.narrow("flags")?,
        options: within.narrow("options")?,
        atmosphere: within.pointer("atmosphere", keep, read_tm_atmosphere)?,
        horizon_height_deg: within.number("horizon_height_deg")?,
    })
}

/// A `tm_scan_request` read out of the object a caller passed.
fn read_tm_scan_request(within: &Within<'_>, keep: &Keep) -> Result<sys::tm_scan_request, ProviderError> {
    Ok(sys::tm_scan_request {
        struct_size: core::mem::size_of::<sys::tm_scan_request>(),
        jd_start: within.number("jd_start")?,
        jd_end: within.number("jd_end")?,
        step_days: within.number("step_days")?,
        scale: within.narrow("scale")?,
        body: within.narrow("body")?,
        body_b: within.narrow("body_b")?,
        quantity: within.narrow("quantity")?,
        direction: within.narrow("direction")?,
        threshold: within.number("threshold")?,
        flags: within.narrow("flags")?,
        observer: within.pointer("observer", keep, read_tm_observer)?,
    })
}

/// A `tm_datetime` as the object a caller gets back.
fn write_tm_datetime(value: &sys::tm_datetime) -> Value {
    json!({
        "year": value.year,
        "month": value.month,
        "day": value.day,
        "hour": value.hour,
        "minute": value.minute,
        "second": value.second,
    })
}

/// A `tm_observer` as the object a caller gets back.
fn write_tm_observer(value: &sys::tm_observer) -> Value {
    json!({
        "longitude_deg": value.longitude_deg,
        "latitude_deg": value.latitude_deg,
        "altitude_m": value.altitude_m,
    })
}

/// A `tm_position` as the object a caller gets back.
fn write_tm_position(value: &sys::tm_position) -> Value {
    json!({
        "lon": value.lon,
        "lat": value.lat,
        "dist": value.dist,
        "lon_speed": value.lon_speed,
        "lat_speed": value.lat_speed,
        "dist_speed": value.dist_speed,
        "flags_used": value.flags_used,
        "status": value.status,
    })
}

/// A `tm_angles` as the object a caller gets back.
fn write_tm_angles(value: &sys::tm_angles) -> Value {
    json!({
        "ascendant": value.ascendant,
        "midheaven": value.midheaven,
        "armc": value.armc,
        "vertex": value.vertex,
        "equatorial_ascendant": value.equatorial_ascendant,
        "co_ascendant_koch": value.co_ascendant_koch,
        "co_ascendant_munkasey": value.co_ascendant_munkasey,
        "polar_ascendant": value.polar_ascendant,
        "ascendant_speed": value.ascendant_speed,
        "midheaven_speed": value.midheaven_speed,
        "armc_speed": value.armc_speed,
        "vertex_speed": value.vertex_speed,
        "system_used": value.system_used,
    })
}

/// A `tm_model_details` as the object a caller gets back.
fn write_tm_model_details(value: &sys::tm_model_details) -> Value {
    json!({
        "value": value.value,
        "name": borrowed(value.name),
        "implemented": value.implemented,
        "is_default_compatible": value.is_default_compatible,
        "is_default_max": value.is_default_max,
    })
}

/// A `tm_atmosphere` as the object a caller gets back.
fn write_tm_atmosphere(value: &sys::tm_atmosphere) -> Value {
    json!({
        "pressure_mbar": value.pressure_mbar,
        "temperature_c": value.temperature_c,
        "lapse_rate_k_per_m": value.lapse_rate_k_per_m,
    })
}

/// A `tm_refraction` as the object a caller gets back.
fn write_tm_refraction(value: &sys::tm_refraction) -> Value {
    json!({
        "altitude_true": value.altitude_true,
        "altitude_apparent": value.altitude_apparent,
        "refraction": value.refraction,
        "horizon_dip": value.horizon_dip,
        "observable": value.observable,
    })
}

/// A `tm_obliquity` as the object a caller gets back.
fn write_tm_obliquity(value: &sys::tm_obliquity) -> Value {
    json!({
        "true_obliquity": value.true_obliquity,
        "mean_obliquity": value.mean_obliquity,
        "nutation_lon": value.nutation_lon,
        "nutation_obl": value.nutation_obl,
    })
}

/// A `tm_horizontal` as the object a caller gets back.
fn write_tm_horizontal(value: &sys::tm_horizontal) -> Value {
    json!({
        "azimuth": value.azimuth,
        "azimuth_north": value.azimuth_north,
        "altitude_true": value.altitude_true,
        "altitude_apparent": value.altitude_apparent,
    })
}

/// A `tm_coverage` as the object a caller gets back.
fn write_tm_coverage(value: &sys::tm_coverage) -> Value {
    json!({
        "jd_start": value.jd_start,
        "jd_end": value.jd_end,
        "contiguous": value.contiguous,
        "file_count": value.file_count,
    })
}

/// A `tm_loaded_file` as the object a caller gets back.
fn write_tm_loaded_file(value: &sys::tm_loaded_file) -> Value {
    json!({
        "name": borrowed(value.name),
        "internal_name": borrowed(value.internal_name),
        "jd_start": value.jd_start,
        "jd_end": value.jd_end,
        "denum": value.denum,
        "version": value.version,
        "body_count": value.body_count,
        "origin": value.origin,
        "crc_verified": value.crc_verified,
    })
}

/// A `tm_fallback_stats` as the object a caller gets back.
fn write_tm_fallback_stats(value: &sys::tm_fallback_stats) -> Value {
    json!({
        "moshier_calls": value.moshier_calls,
        "refused_calls": value.refused_calls,
        "last_body": value.last_body,
        "last_jd": value.last_jd,
    })
}

/// A `tm_nodes_apsides` as the object a caller gets back.
fn write_tm_nodes_apsides(value: &sys::tm_nodes_apsides) -> Value {
    json!({
        "ascending": write_tm_position(&value.ascending),
        "descending": write_tm_position(&value.descending),
        "perihelion": write_tm_position(&value.perihelion),
        "aphelion": write_tm_position(&value.aphelion),
        "used_mean": value.used_mean,
        "flags_used": value.flags_used,
        "status": value.status,
    })
}

/// A `tm_orbital_elements` as the object a caller gets back.
fn write_tm_orbital_elements(value: &sys::tm_orbital_elements) -> Value {
    json!({
        "semi_major_axis": value.semi_major_axis,
        "eccentricity": value.eccentricity,
        "inclination": value.inclination,
        "node": value.node,
        "arg_perihelion": value.arg_perihelion,
        "lon_perihelion": value.lon_perihelion,
        "mean_anomaly": value.mean_anomaly,
        "true_anomaly": value.true_anomaly,
        "eccentric_anomaly": value.eccentric_anomaly,
        "mean_longitude": value.mean_longitude,
        "period_sidereal": value.period_sidereal,
        "daily_motion": value.daily_motion,
        "period_tropical": value.period_tropical,
        "period_synodic": value.period_synodic,
        "perihelion_time": value.perihelion_time,
        "perihelion_distance": value.perihelion_distance,
        "aphelion_distance": value.aphelion_distance,
        "status": value.status,
    })
}

/// A `tm_orbit_distances` as the object a caller gets back.
fn write_tm_orbit_distances(value: &sys::tm_orbit_distances) -> Value {
    json!({
        "max_distance": value.max_distance,
        "min_distance": value.min_distance,
        "true_distance": value.true_distance,
        "status": value.status,
    })
}

/// A `tm_phenomena` as the object a caller gets back.
fn write_tm_phenomena(value: &sys::tm_phenomena) -> Value {
    json!({
        "phase_angle": value.phase_angle,
        "phase": value.phase,
        "elongation": value.elongation,
        "diameter": value.diameter,
        "magnitude": value.magnitude,
        "horizontal_parallax": value.horizontal_parallax,
        "magnitude_out_of_range": value.magnitude_out_of_range,
        "flags_used": value.flags_used,
        "status": value.status,
        "magnitude_state": value.magnitude_state,
    })
}

/// A `tm_star` as the object a caller gets back.
fn write_tm_star(value: &sys::tm_star) -> Value {
    json!({
        "id": value.id,
        "catalogue_generation": value.catalogue_generation,
        "magnitude": value.magnitude,
        "epoch": value.epoch,
        "ra_deg": value.ra_deg,
        "dec_deg": value.dec_deg,
        "pm_ra_arcsec_per_century": value.pm_ra_arcsec_per_century,
        "pm_dec_arcsec_per_century": value.pm_dec_arcsec_per_century,
        "radial_velocity_au_per_century": value.radial_velocity_au_per_century,
        "parallax_arcsec": value.parallax_arcsec,
    })
}

/// A `tm_star_query` as the object a caller gets back.
fn write_tm_star_query(value: &sys::tm_star_query) -> Value {
    json!({
        "use_magnitude": value.use_magnitude,
        "magnitude_min": value.magnitude_min,
        "magnitude_max": value.magnitude_max,
        "ra_deg": value.ra_deg,
        "dec_deg": value.dec_deg,
        "radius_deg": value.radius_deg,
        "name_contains": borrowed(value.name_contains),
    })
}

/// A `tm_eclipse_attributes` as the object a caller gets back.
fn write_tm_eclipse_attributes(value: &sys::tm_eclipse_attributes) -> Value {
    json!({
        "magnitude": value.magnitude,
        "diameter_ratio": value.diameter_ratio,
        "obscuration": value.obscuration,
        "core_shadow_km": value.core_shadow_km,
        "azimuth": value.azimuth,
        "azimuth_north": value.azimuth_north,
        "altitude_true": value.altitude_true,
        "altitude_apparent": value.altitude_apparent,
        "separation": value.separation,
        "magnitude_nasa": value.magnitude_nasa,
        "saros_known": value.saros_known,
        "saros_series": value.saros_series,
        "saros_member": value.saros_member,
    })
}

/// A `tm_lunar_eclipse_attributes` as the object a caller gets back.
fn write_tm_lunar_eclipse_attributes(value: &sys::tm_lunar_eclipse_attributes) -> Value {
    json!({
        "umbral_magnitude": value.umbral_magnitude,
        "penumbral_magnitude": value.penumbral_magnitude,
        "azimuth": value.azimuth,
        "azimuth_north": value.azimuth_north,
        "altitude_true": value.altitude_true,
        "altitude_apparent": value.altitude_apparent,
        "opposition": value.opposition,
        "saros_known": value.saros_known,
        "saros_series": value.saros_series,
        "saros_member": value.saros_member,
    })
}

/// A `tm_eclipse_location` as the object a caller gets back.
fn write_tm_eclipse_location(value: &sys::tm_eclipse_location) -> Value {
    json!({
        "longitude": value.longitude,
        "latitude": value.latitude,
        "core_shadow_km": value.core_shadow_km,
        "penumbra_km": value.penumbra_km,
        "axis_distance_km": value.axis_distance_km,
        "umbra_fundamental_km": value.umbra_fundamental_km,
        "penumbra_fundamental_km": value.penumbra_fundamental_km,
    })
}

/// A `tm_solar_eclipse` as the object a caller gets back.
fn write_tm_solar_eclipse(value: &sys::tm_solar_eclipse) -> Value {
    json!({
        "type": value.r#type,
        "maximum": value.maximum,
        "local_noon": value.local_noon,
        "begin": value.begin,
        "end": value.end,
        "total_begin": value.total_begin,
        "total_end": value.total_end,
        "center_begin": value.center_begin,
        "center_end": value.center_end,
        "status": value.status,
    })
}

/// A `tm_lunar_eclipse` as the object a caller gets back.
fn write_tm_lunar_eclipse(value: &sys::tm_lunar_eclipse) -> Value {
    json!({
        "type": value.r#type,
        "maximum": value.maximum,
        "partial_begin": value.partial_begin,
        "partial_end": value.partial_end,
        "total_begin": value.total_begin,
        "total_end": value.total_end,
        "penumbral_begin": value.penumbral_begin,
        "penumbral_end": value.penumbral_end,
        "status": value.status,
    })
}

/// A `tm_occultation` as the object a caller gets back.
fn write_tm_occultation(value: &sys::tm_occultation) -> Value {
    json!({
        "type": value.r#type,
        "maximum": value.maximum,
        "local_noon": value.local_noon,
        "begin": value.begin,
        "end": value.end,
        "total_begin": value.total_begin,
        "total_end": value.total_end,
        "center_begin": value.center_begin,
        "center_end": value.center_end,
        "status": value.status,
    })
}

/// A `tm_solar_eclipse_local` as the object a caller gets back.
fn write_tm_solar_eclipse_local(value: &sys::tm_solar_eclipse_local) -> Value {
    json!({
        "type": value.r#type,
        "phases_visible": value.phases_visible,
        "visible": value.visible,
        "maximum": value.maximum,
        "first_contact": value.first_contact,
        "second_contact": value.second_contact,
        "third_contact": value.third_contact,
        "fourth_contact": value.fourth_contact,
        "sunrise": value.sunrise,
        "sunset": value.sunset,
        "attributes": write_tm_eclipse_attributes(&value.attributes),
        "status": value.status,
    })
}

/// A `tm_lunar_eclipse_local` as the object a caller gets back.
fn write_tm_lunar_eclipse_local(value: &sys::tm_lunar_eclipse_local) -> Value {
    json!({
        "type": value.r#type,
        "phases_visible": value.phases_visible,
        "visible": value.visible,
        "maximum": value.maximum,
        "partial_begin": value.partial_begin,
        "partial_end": value.partial_end,
        "total_begin": value.total_begin,
        "total_end": value.total_end,
        "penumbral_begin": value.penumbral_begin,
        "penumbral_end": value.penumbral_end,
        "moonrise": value.moonrise,
        "moonset": value.moonset,
        "attributes": write_tm_lunar_eclipse_attributes(&value.attributes),
        "status": value.status,
    })
}

/// A `tm_occultation_local` as the object a caller gets back.
fn write_tm_occultation_local(value: &sys::tm_occultation_local) -> Value {
    json!({
        "type": value.r#type,
        "phases_visible": value.phases_visible,
        "daylight": value.daylight,
        "visible": value.visible,
        "maximum": value.maximum,
        "first_contact": value.first_contact,
        "second_contact": value.second_contact,
        "third_contact": value.third_contact,
        "fourth_contact": value.fourth_contact,
        "body_rise": value.body_rise,
        "body_set": value.body_set,
        "attributes": write_tm_eclipse_attributes(&value.attributes),
        "status": value.status,
    })
}

/// A `tm_eclipse_request` as the object a caller gets back.
fn write_tm_eclipse_request(value: &sys::tm_eclipse_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "scale": value.scale,
        "flags": value.flags,
        "want": value.want,
        "backward": value.backward,
        "observer": pointed(value.observer, write_tm_observer),
        "body": value.body,
        "star": borrowed(value.star),
        "one_try": value.one_try,
        "jd_end": value.jd_end,
    })
}

/// A `tm_event_request` as the object a caller gets back.
fn write_tm_event_request(value: &sys::tm_event_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "scale": value.scale,
        "body": value.body,
        "star": borrowed(value.star),
        "flags": value.flags,
        "kind": value.kind,
        "options": value.options,
        "observer": pointed(value.observer, write_tm_observer),
        "atmosphere": pointed(value.atmosphere, write_tm_atmosphere),
        "horizon_height_deg": value.horizon_height_deg,
        "jd_end": value.jd_end,
    })
}

/// A `tm_event` as the object a caller gets back.
fn write_tm_event(value: &sys::tm_event) -> Value {
    json!({
        "jd": value.jd,
        "found": value.found,
        "fast_method": value.fast_method,
        "status": value.status,
    })
}

/// A `tm_visibility_atmosphere` as the object a caller gets back.
fn write_tm_visibility_atmosphere(value: &sys::tm_visibility_atmosphere) -> Value {
    json!({
        "pressure_mbar": value.pressure_mbar,
        "temperature_c": value.temperature_c,
        "humidity_pct": value.humidity_pct,
        "haze": value.haze,
    })
}

/// A `tm_observer_eye` as the object a caller gets back.
fn write_tm_observer_eye(value: &sys::tm_observer_eye) -> Value {
    json!({
        "age_years": value.age_years,
        "snellen": value.snellen,
        "monocular": value.monocular,
        "use_optics": value.use_optics,
        "magnification": value.magnification,
        "aperture_mm": value.aperture_mm,
        "transmission": value.transmission,
    })
}

/// A `tm_visibility` as the object a caller gets back.
fn write_tm_visibility(value: &sys::tm_visibility) -> Value {
    json!({
        "limiting_magnitude": value.limiting_magnitude,
        "object_magnitude": value.object_magnitude,
        "object_altitude": value.object_altitude,
        "object_azimuth": value.object_azimuth,
        "object_azimuth_north": value.object_azimuth_north,
        "sun_altitude": value.sun_altitude,
        "sun_azimuth": value.sun_azimuth,
        "moon_altitude": value.moon_altitude,
        "moon_azimuth": value.moon_azimuth,
        "scotopic": value.scotopic,
        "near_switch": value.near_switch,
        "above_horizon": value.above_horizon,
        "status": value.status,
    })
}

/// A `tm_visibility_best` as the object a caller gets back.
fn write_tm_visibility_best(value: &sys::tm_visibility_best) -> Value {
    json!({
        "object_altitude": value.object_altitude,
        "arcus": value.arcus,
        "sun_altitude": value.sun_altitude,
    })
}

/// A `tm_heliacal_request` as the object a caller gets back.
fn write_tm_heliacal_request(value: &sys::tm_heliacal_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "scale": value.scale,
        "body": value.body,
        "star": borrowed(value.star),
        "flags": value.flags,
        "kind": value.kind,
        "options": value.options,
        "observer": pointed(value.observer, write_tm_observer),
        "atmosphere": pointed(value.atmosphere, write_tm_visibility_atmosphere),
        "eye": pointed(value.eye, write_tm_observer_eye),
        "jd_end": value.jd_end,
    })
}

/// A `tm_heliacal` as the object a caller gets back.
fn write_tm_heliacal(value: &sys::tm_heliacal) -> Value {
    json!({
        "begin": value.begin,
        "optimum": value.optimum,
        "end": value.end,
        "uncertain": value.uncertain,
        "found": value.found,
        "status": value.status,
    })
}

/// A `tm_heliacal_details` as the object a caller gets back.
fn write_tm_heliacal_details(value: &sys::tm_heliacal_details) -> Value {
    json!({
        "object_altitude": value.object_altitude,
        "object_apparent_altitude": value.object_apparent_altitude,
        "object_geocentric_altitude": value.object_geocentric_altitude,
        "object_azimuth": value.object_azimuth,
        "sun_altitude": value.sun_altitude,
        "sun_azimuth": value.sun_azimuth,
        "parallax": value.parallax,
        "magnitude": value.magnitude,
        "arcus_visionis_topocentric": value.arcus_visionis_topocentric,
        "arcus_visionis": value.arcus_visionis,
        "azimuth_difference": value.azimuth_difference,
        "arcus_lucis": value.arcus_lucis,
        "arcus_visionis_min": value.arcus_visionis_min,
        "extinction": value.extinction,
        "has_visible_first": value.has_visible_first,
        "visible_first": value.visible_first,
        "has_visible_best": value.has_visible_best,
        "visible_best": value.visible_best,
        "has_visible_last": value.has_visible_last,
        "visible_last": value.visible_last,
        "visible_duration": value.visible_duration,
        "has_visible_best_yallop": value.has_visible_best_yallop,
        "visible_best_yallop": value.visible_best_yallop,
        "object_riseset": value.object_riseset,
        "sun_riseset": value.sun_riseset,
        "lag": value.lag,
        "moon_crescent_width": value.moon_crescent_width,
        "moon_crescent_length": value.moon_crescent_length,
        "yallop_q": value.yallop_q,
        "yallop_criterion": value.yallop_criterion,
        "elongation": value.elongation,
        "illumination_pct": value.illumination_pct,
    })
}

/// A `tm_crossing_request` as the object a caller gets back.
fn write_tm_crossing_request(value: &sys::tm_crossing_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "scale": value.scale,
        "body": value.body,
        "target_deg": value.target_deg,
        "flags": value.flags,
        "backward": value.backward,
        "jd_end": value.jd_end,
        "quantity": value.quantity,
        "step_deg": value.step_deg,
        "body_b": value.body_b,
        "coeff_a": value.coeff_a,
        "coeff_b": value.coeff_b,
    })
}

/// A `tm_crossing` as the object a caller gets back.
fn write_tm_crossing(value: &sys::tm_crossing) -> Value {
    json!({
        "jd": value.jd,
        "longitude": value.longitude,
        "latitude": value.latitude,
        "status": value.status,
    })
}

/// A `tm_calendar_request` as the object a caller gets back.
fn write_tm_calendar_request(value: &sys::tm_calendar_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "day_count": value.day_count,
        "anchor": value.anchor,
        "observer": pointed(value.observer, write_tm_observer),
        "anchor_body": value.anchor_body,
        "flags": value.flags,
        "options": value.options,
        "atmosphere": pointed(value.atmosphere, write_tm_atmosphere),
        "horizon_height_deg": value.horizon_height_deg,
    })
}

/// A `tm_calendar_day` as the object a caller gets back.
fn write_tm_calendar_day(value: &sys::tm_calendar_day) -> Value {
    json!({
        "jd": value.jd,
        "found": value.found,
        "status": value.status,
    })
}

/// A `tm_scan_request` as the object a caller gets back.
fn write_tm_scan_request(value: &sys::tm_scan_request) -> Value {
    json!({
        "jd_start": value.jd_start,
        "jd_end": value.jd_end,
        "step_days": value.step_days,
        "scale": value.scale,
        "body": value.body,
        "body_b": value.body_b,
        "quantity": value.quantity,
        "direction": value.direction,
        "threshold": value.threshold,
        "flags": value.flags,
        "observer": pointed(value.observer, write_tm_observer),
    })
}

/// A `tm_bracket` as the object a caller gets back.
fn write_tm_bracket(value: &sys::tm_bracket) -> Value {
    json!({
        "jd_lo": value.jd_lo,
        "jd_hi": value.jd_hi,
        "value_lo": value.value_lo,
        "value_hi": value.value_hi,
        "status": value.status,
    })
}

/// A `tm_angle_parts` as the object a caller gets back.
fn write_tm_angle_parts(value: &sys::tm_angle_parts) -> Value {
    json!({
        "negative": value.negative,
        "degrees": value.degrees,
        "minutes": value.minutes,
        "seconds": value.seconds,
        "second_fraction": value.second_fraction,
        "zodiac_sign": value.zodiac_sign,
        "nakshatra": value.nakshatra,
        "pada": value.pada,
    })
}

/// A `tm_ayanamsha_details` as the object a caller gets back.
fn write_tm_ayanamsha_details(value: &sys::tm_ayanamsha_details) -> Value {
    json!({
        "name": borrowed(value.name),
        "t0": value.t0,
        "ayan_t0": value.ayan_t0,
        "t0_is_ut": value.t0_is_ut,
        "needs_ephemeris": value.needs_ephemeris,
        "is_frame": value.is_frame,
        "fitted_precession": value.fitted_precession,
    })
}
