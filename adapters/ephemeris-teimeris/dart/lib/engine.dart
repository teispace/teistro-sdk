// The engine's own operations, typed. **Generated by
// `cargo xtask engine`. Do not edit.**
//
// From the engine's own description at version `0.1.0`.
// Which of its functions are here is a measurement, not a choice:
// `docs/03-design/engine-passthrough-measured.md` classifies all of
// them into what is callable, what this adapter will never hand over,
// and what is queued behind one more shape.
//
// A method's arguments are named the way this language names things;
// the keys that cross are the engine's own, which is what
// `sdk.engine.signature(name)` reads. A call through here is to a
// named engine and does not survive changing it — which is why it is
// taken as a value rather than laid over `sdk.engine` (ADR-0030).
// dart format off

import 'package:teistro/teistro.dart';

/// The engine's own operations, typed.
///
/// An extension rather than a wrapper, because a Dart extension method
/// has a body: importing this file is what makes the names exist, and
/// they are as typed and as real as any method.
extension TeimerisEngine on Engine {
  /// `tm_status_name`.
  String tmStatusName({required int s}) {
    final answered = (call('tm_status_name', <String, Object?>{'s': s})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_context_release_caches`.
  void tmContextReleaseCaches() {
    call('tm_context_release_caches', const <String, Object?>{});
  }

  /// `tm_version_string`.
  String tmVersionString() {
    final answered = (call('tm_version_string', const <String, Object?>{})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_version`.
  ({int major, int minor, int patch}) tmVersion() {
    final answered = (call('tm_version', const <String, Object?>{})) as Map<String, Object?>;
    return (major: (answered['major']! as num).toInt(), minor: (answered['minor']! as num).toInt(), patch: (answered['patch']! as num).toInt());
  }

  /// `tm_body_name`.
  String tmBodyName({required int body}) {
    final answered = (call('tm_body_name', <String, Object?>{'body': body})) as Map<String, Object?>;
    return answered['buf']! as String;
  }

  /// `tm_delta_t`.
  double tmDeltaT({required double jdUt1}) {
    final answered = (call('tm_delta_t', <String, Object?>{'jd_ut1': jdUt1})) as Map<String, Object?>;
    return (answered['out_seconds']! as num).toDouble();
  }

  /// `tm_sidereal_time`.
  double tmSiderealTime({required double jdUt1, required double geoLonDeg}) {
    final answered = (call('tm_sidereal_time', <String, Object?>{'jd_ut1': jdUt1, 'geo_lon_deg': geoLonDeg})) as Map<String, Object?>;
    return (answered['out_hours']! as num).toDouble();
  }

  /// `tm_day_of_week`.
  int tmDayOfWeek({required double jd}) {
    final answered = (call('tm_day_of_week', <String, Object?>{'jd': jd})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_weekday_name`.
  String tmWeekdayName({required int day}) {
    final answered = (call('tm_weekday_name', <String, Object?>{'day': day})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_equation_of_time`.
  double tmEquationOfTime({required double jdUt1}) {
    final answered = (call('tm_equation_of_time', <String, Object?>{'jd_ut1': jdUt1})) as Map<String, Object?>;
    return (answered['out_seconds']! as num).toDouble();
  }

  /// `tm_local_mean_to_apparent`.
  double tmLocalMeanToApparent({required double jdLocalMean, required double geoLonDeg}) {
    final answered = (call('tm_local_mean_to_apparent', <String, Object?>{'jd_local_mean': jdLocalMean, 'geo_lon_deg': geoLonDeg})) as Map<String, Object?>;
    return (answered['out_jd_local_apparent']! as num).toDouble();
  }

  /// `tm_local_apparent_to_mean`.
  double tmLocalApparentToMean({required double jdLocalApparent, required double geoLonDeg}) {
    final answered = (call('tm_local_apparent_to_mean', <String, Object?>{'jd_local_apparent': jdLocalApparent, 'geo_lon_deg': geoLonDeg})) as Map<String, Object?>;
    return (answered['out_jd_local_mean']! as num).toDouble();
  }

  /// `tm_position_value`.
  double tmPositionValue({required double jd, required int scale, required int body, required int flags, required int field}) {
    final answered = (call('tm_position_value', <String, Object?>{'jd': jd, 'scale': scale, 'body': body, 'flags': flags, 'field': field})) as Map<String, Object?>;
    return (answered['out']! as num).toDouble();
  }

  /// `tm_house_cusp_count`.
  int tmHouseCuspCount({required int sys}) {
    final answered = (call('tm_house_cusp_count', <String, Object?>{'sys': sys})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_house_system_name`.
  String tmHouseSystemName({required int sys}) {
    final answered = (call('tm_house_system_name', <String, Object?>{'sys': sys})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_house_system_count`.
  int tmHouseSystemCount() {
    final answered = (call('tm_house_system_count', const <String, Object?>{})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_house_position`.
  double tmHousePosition({required double armc, required double geoLatDeg, required double obliquityDeg, required int sys, required double lonDeg, required double latDeg}) {
    final answered = (call('tm_house_position', <String, Object?>{'armc': armc, 'geo_lat_deg': geoLatDeg, 'obliquity_deg': obliquityDeg, 'sys': sys, 'lon_deg': lonDeg, 'lat_deg': latDeg})) as Map<String, Object?>;
    return (answered['out_house']! as num).toDouble();
  }

  /// `tm_set_jpl_file`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetJplFile({required String filename}) {
    call('tm_set_jpl_file', <String, Object?>{'filename': filename});
  }

  /// `tm_jpl_info`.
  ({int outDenum, double outJdStart, double outJdEnd, double outSegmentDays}) tmJplInfo() {
    final answered = (call('tm_jpl_info', const <String, Object?>{})) as Map<String, Object?>;
    return (outDenum: (answered['out_denum']! as num).toInt(), outJdStart: (answered['out_jd_start']! as num).toDouble(), outJdEnd: (answered['out_jd_end']! as num).toDouble(), outSegmentDays: (answered['out_segment_days']! as num).toDouble());
  }

  /// `tm_set_ayanamsha`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetAyanamsha({required int mode, required double t0, required double ayanT0}) {
    call('tm_set_ayanamsha', <String, Object?>{'mode': mode, 't0': t0, 'ayan_t0': ayanT0});
  }

  /// `tm_ayanamsha_value`.
  double tmAyanamshaValue({required double jd, required int scale, required int flags}) {
    final answered = (call('tm_ayanamsha_value', <String, Object?>{'jd': jd, 'scale': scale, 'flags': flags})) as Map<String, Object?>;
    return (answered['out_degrees']! as num).toDouble();
  }

  /// `tm_ayanamsha_name`.
  String tmAyanamshaName({required int mode}) {
    final answered = (call('tm_ayanamsha_name', <String, Object?>{'mode': mode})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_ayanamsha_count`.
  int tmAyanamshaCount() {
    final answered = (call('tm_ayanamsha_count', const <String, Object?>{})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_set_model`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetModel({required int kind, required int model}) {
    call('tm_set_model', <String, Object?>{'kind': kind, 'model': model});
  }

  /// `tm_get_model`.
  int tmGetModel({required int kind}) {
    final answered = (call('tm_get_model', <String, Object?>{'kind': kind})) as Map<String, Object?>;
    return (answered['out_model']! as num).toInt();
  }

  /// `tm_model_name`.
  String tmModelName({required int kind, required int model}) {
    final answered = (call('tm_model_name', <String, Object?>{'kind': kind, 'model': model})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_model_kind_name`.
  String tmModelKindName({required int kind}) {
    final answered = (call('tm_model_kind_name', <String, Object?>{'kind': kind})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_model_count`.
  int tmModelCount({required int kind}) {
    final answered = (call('tm_model_count', <String, Object?>{'kind': kind})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_set_tidal_acceleration`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetTidalAcceleration({required double arcsecPerCentury2}) {
    call('tm_set_tidal_acceleration', <String, Object?>{'arcsec_per_century2': arcsecPerCentury2});
  }

  /// `tm_clear_tidal_acceleration`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmClearTidalAcceleration() {
    call('tm_clear_tidal_acceleration', const <String, Object?>{});
  }

  /// `tm_get_tidal_acceleration`.
  ({double outValue, int outIsSet}) tmGetTidalAcceleration() {
    final answered = (call('tm_get_tidal_acceleration', const <String, Object?>{})) as Map<String, Object?>;
    return (outValue: (answered['out_value']! as num).toDouble(), outIsSet: (answered['out_is_set']! as num).toInt());
  }

  /// `tm_set_delta_t_override`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetDeltaTOverride({required double seconds}) {
    call('tm_set_delta_t_override', <String, Object?>{'seconds': seconds});
  }

  /// `tm_clear_delta_t_override`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmClearDeltaTOverride() {
    call('tm_clear_delta_t_override', const <String, Object?>{});
  }

  /// `tm_get_delta_t_override`.
  ({double outSeconds, int outIsSet}) tmGetDeltaTOverride() {
    final answered = (call('tm_get_delta_t_override', const <String, Object?>{})) as Map<String, Object?>;
    return (outSeconds: (answered['out_seconds']! as num).toDouble(), outIsSet: (answered['out_is_set']! as num).toInt());
  }

  /// `tm_set_nutation_interpolation`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmSetNutationInterpolation({required int enable}) {
    call('tm_set_nutation_interpolation', <String, Object?>{'enable': enable});
  }

  /// `tm_get_nutation_interpolation`.
  int tmGetNutationInterpolation() {
    final answered = (call('tm_get_nutation_interpolation', const <String, Object?>{})) as Map<String, Object?>;
    return (answered['out']! as num).toInt();
  }

  /// `tm_embedded_coverage`.
  ({double outJdStart, double outJdEnd}) tmEmbeddedCoverage() {
    final answered = (call('tm_embedded_coverage', const <String, Object?>{})) as Map<String, Object?>;
    return (outJdStart: (answered['out_jd_start']! as num).toDouble(), outJdEnd: (answered['out_jd_end']! as num).toDouble());
  }

  /// `tm_fallback_stats_reset`.
  void tmFallbackStatsReset() {
    call('tm_fallback_stats_reset', const <String, Object?>{});
  }

  /// `tm_cache_dir_default`.
  String tmCacheDirDefault() {
    final answered = (call('tm_cache_dir_default', const <String, Object?>{})) as Map<String, Object?>;
    return answered['buf']! as String;
  }

  /// `tm_star_load_catalogue`.
  ///
  /// Changes engine state the SDK's provenance does not record: a chart cast afterwards says it was computed one way and was computed another.
  void tmStarLoadCatalogue({required String path}) {
    call('tm_star_load_catalogue', <String, Object?>{'path': path});
  }

  /// `tm_star_count`.
  int tmStarCount() {
    final answered = (call('tm_star_count', const <String, Object?>{})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_eclipse_type_name`.
  String tmEclipseTypeName({required int bit}) {
    final answered = (call('tm_eclipse_type_name', <String, Object?>{'bit': bit})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_event_kind_name`.
  String tmEventKindName({required int kind}) {
    final answered = (call('tm_event_kind_name', <String, Object?>{'kind': kind})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_angle_normalize_deg`.
  double tmAngleNormalizeDeg({required double deg}) {
    final answered = (call('tm_angle_normalize_deg', <String, Object?>{'deg': deg})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_normalize_rad`.
  double tmAngleNormalizeRad({required double rad}) {
    final answered = (call('tm_angle_normalize_rad', <String, Object?>{'rad': rad})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_diff_deg`.
  double tmAngleDiffDeg({required double a, required double b}) {
    final answered = (call('tm_angle_diff_deg', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_diff_deg_positive`.
  double tmAngleDiffDegPositive({required double a, required double b}) {
    final answered = (call('tm_angle_diff_deg_positive', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_diff_rad`.
  double tmAngleDiffRad({required double a, required double b}) {
    final answered = (call('tm_angle_diff_rad', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_midpoint_deg`.
  double tmAngleMidpointDeg({required double a, required double b}) {
    final answered = (call('tm_angle_midpoint_deg', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_angle_midpoint_rad`.
  double tmAngleMidpointRad({required double a, required double b}) {
    final answered = (call('tm_angle_midpoint_rad', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_degrees_to_centiseconds`.
  int tmDegreesToCentiseconds({required double deg}) {
    final answered = (call('tm_degrees_to_centiseconds', <String, Object?>{'deg': deg})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_centiseconds_to_degrees`.
  double tmCentisecondsToDegrees({required int centiseconds}) {
    final answered = (call('tm_centiseconds_to_degrees', <String, Object?>{'centiseconds': centiseconds})) as Map<String, Object?>;
    return (answered['return']! as num).toDouble();
  }

  /// `tm_centiseconds_normalize`.
  int tmCentisecondsNormalize({required int cs}) {
    final answered = (call('tm_centiseconds_normalize', <String, Object?>{'cs': cs})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_centiseconds_diff`.
  int tmCentisecondsDiff({required int a, required int b}) {
    final answered = (call('tm_centiseconds_diff', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_centiseconds_diff_signed`.
  int tmCentisecondsDiffSigned({required int a, required int b}) {
    final answered = (call('tm_centiseconds_diff_signed', <String, Object?>{'a': a, 'b': b})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_centiseconds_round_seconds`.
  int tmCentisecondsRoundSeconds({required int cs}) {
    final answered = (call('tm_centiseconds_round_seconds', <String, Object?>{'cs': cs})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_round_half_away`.
  int tmRoundHalfAway({required double x}) {
    final answered = (call('tm_round_half_away', <String, Object?>{'x': x})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

  /// `tm_angle_format`.
  String tmAngleFormat({required double deg, required int style, required int decimals}) {
    final answered = (call('tm_angle_format', <String, Object?>{'deg': deg, 'style': style, 'decimals': decimals})) as Map<String, Object?>;
    return answered['buf']! as String;
  }

  /// `tm_zodiac_sign_name`.
  String tmZodiacSignName({required int sign}) {
    final answered = (call('tm_zodiac_sign_name', <String, Object?>{'sign': sign})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_nakshatra_name`.
  String tmNakshatraName({required int nakshatra}) {
    final answered = (call('tm_nakshatra_name', <String, Object?>{'nakshatra': nakshatra})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_nakshatra_lord`.
  String tmNakshatraLord({required int nakshatra}) {
    final answered = (call('tm_nakshatra_lord', <String, Object?>{'nakshatra': nakshatra})) as Map<String, Object?>;
    return answered['return']! as String;
  }

  /// `tm_chart_blob_size`.
  int tmChartBlobSize({required int bodyCount, required int cuspCount, required int withCuspSpeeds, required int withAngles}) {
    final answered = (call('tm_chart_blob_size', <String, Object?>{'body_count': bodyCount, 'cusp_count': cuspCount, 'with_cusp_speeds': withCuspSpeeds, 'with_angles': withAngles})) as Map<String, Object?>;
    return (answered['return']! as num).toInt();
  }

}
