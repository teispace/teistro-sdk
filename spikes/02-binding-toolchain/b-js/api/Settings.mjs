// generated by diplomat-tool
import { Ayanamsha } from "./Ayanamsha.mjs"
import { NodeKind } from "./NodeKind.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * The settings of a context.
 */
export class Settings {
    #ayanamsha;
    get ayanamsha() {
        return this.#ayanamsha;
    }
    set ayanamsha(value){
        this.#ayanamsha = value;
    }
    #node;
    get node() {
        return this.#node;
    }
    set node(value){
        this.#node = value;
    }
    #dashaDepth;
    get dashaDepth() {
        return this.#dashaDepth;
    }
    set dashaDepth(value){
        this.#dashaDepth = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new Settings(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("Settings's constructor takes an object of Settings's fields.");
        }

        if ("ayanamsha" in structObj) {
            this.#ayanamsha = new Ayanamsha(structObj.ayanamsha);
        } else {
            throw new Error("Missing required field ayanamsha.");
        }

        if ("node" in structObj) {
            this.#node = new NodeKind(structObj.node);
        } else {
            throw new Error("Missing required field node.");
        }

        if ("dashaDepth" in structObj) {
            this.#dashaDepth = structObj.dashaDepth;
        } else {
            throw new Error("Missing required field dashaDepth.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 12;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof Settings) {
            return obj;
        }

        return Settings.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, new Ayanamsha(this.#ayanamsha).ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, new NodeKind(this.#node).ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#dashaDepth, Uint8Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("Settings._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const ayanamshaDeref = diplomatRuntime.enumDiscriminant(wasm, ptr);
        structObj.ayanamsha = new Ayanamsha(diplomatRuntime.internalConstructor, ayanamshaDeref);
        const nodeDeref = diplomatRuntime.enumDiscriminant(wasm, ptr + 4);
        structObj.node = new NodeKind(diplomatRuntime.internalConstructor, nodeDeref);
        const dashaDepthDeref = (new Uint8Array(wasm.memory.buffer, ptr + 8, 1))[0];
        structObj.dashaDepth = dashaDepthDeref;

        return new Settings(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}