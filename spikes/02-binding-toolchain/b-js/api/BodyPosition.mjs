// generated by diplomat-tool
import { Body } from "./Body.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * A classified sidereal position.
 */
export class BodyPosition {
    #body;
    get body() {
        return this.#body;
    }
    set body(value){
        this.#body = value;
    }
    #longitudeDeg;
    get longitudeDeg() {
        return this.#longitudeDeg;
    }
    set longitudeDeg(value){
        this.#longitudeDeg = value;
    }
    #longitudeNas;
    get longitudeNas() {
        return this.#longitudeNas;
    }
    set longitudeNas(value){
        this.#longitudeNas = value;
    }
    #latitudeDeg;
    get latitudeDeg() {
        return this.#latitudeDeg;
    }
    set latitudeDeg(value){
        this.#latitudeDeg = value;
    }
    #speedDegPerDay;
    get speedDegPerDay() {
        return this.#speedDegPerDay;
    }
    set speedDegPerDay(value){
        this.#speedDegPerDay = value;
    }
    #sign;
    get sign() {
        return this.#sign;
    }
    set sign(value){
        this.#sign = value;
    }
    #nakshatra;
    get nakshatra() {
        return this.#nakshatra;
    }
    set nakshatra(value){
        this.#nakshatra = value;
    }
    #pada;
    get pada() {
        return this.#pada;
    }
    set pada(value){
        this.#pada = value;
    }
    #retrograde;
    get retrograde() {
        return this.#retrograde;
    }
    set retrograde(value){
        this.#retrograde = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new BodyPosition(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("BodyPosition's constructor takes an object of BodyPosition's fields.");
        }

        if ("body" in structObj) {
            this.#body = new Body(structObj.body);
        } else {
            throw new Error("Missing required field body.");
        }

        if ("longitudeDeg" in structObj) {
            this.#longitudeDeg = structObj.longitudeDeg;
        } else {
            throw new Error("Missing required field longitudeDeg.");
        }

        if ("longitudeNas" in structObj) {
            this.#longitudeNas = structObj.longitudeNas;
        } else {
            throw new Error("Missing required field longitudeNas.");
        }

        if ("latitudeDeg" in structObj) {
            this.#latitudeDeg = structObj.latitudeDeg;
        } else {
            throw new Error("Missing required field latitudeDeg.");
        }

        if ("speedDegPerDay" in structObj) {
            this.#speedDegPerDay = structObj.speedDegPerDay;
        } else {
            throw new Error("Missing required field speedDegPerDay.");
        }

        if ("sign" in structObj) {
            this.#sign = structObj.sign;
        } else {
            throw new Error("Missing required field sign.");
        }

        if ("nakshatra" in structObj) {
            this.#nakshatra = structObj.nakshatra;
        } else {
            throw new Error("Missing required field nakshatra.");
        }

        if ("pada" in structObj) {
            this.#pada = structObj.pada;
        } else {
            throw new Error("Missing required field pada.");
        }

        if ("retrograde" in structObj) {
            this.#retrograde = structObj.retrograde;
        } else {
            throw new Error("Missing required field retrograde.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 48;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof BodyPosition) {
            return obj;
        }

        return BodyPosition.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, new Body(this.#body).ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#longitudeDeg, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 16, this.#longitudeNas, BigInt64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 24, this.#latitudeDeg, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 32, this.#speedDegPerDay, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 40, this.#sign, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 41, this.#nakshatra, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 42, this.#pada, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 43, this.#retrograde, Uint8Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("BodyPosition._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const bodyDeref = diplomatRuntime.enumDiscriminant(wasm, ptr);
        structObj.body = new Body(diplomatRuntime.internalConstructor, bodyDeref);
        const longitudeDegDeref = (new Float64Array(wasm.memory.buffer, ptr + 8, 1))[0];
        structObj.longitudeDeg = longitudeDegDeref;
        const longitudeNasDeref = (new BigInt64Array(wasm.memory.buffer, ptr + 16, 1))[0];
        structObj.longitudeNas = longitudeNasDeref;
        const latitudeDegDeref = (new Float64Array(wasm.memory.buffer, ptr + 24, 1))[0];
        structObj.latitudeDeg = latitudeDegDeref;
        const speedDegPerDayDeref = (new Float64Array(wasm.memory.buffer, ptr + 32, 1))[0];
        structObj.speedDegPerDay = speedDegPerDayDeref;
        const signDeref = (new Uint8Array(wasm.memory.buffer, ptr + 40, 1))[0];
        structObj.sign = signDeref;
        const nakshatraDeref = (new Uint8Array(wasm.memory.buffer, ptr + 41, 1))[0];
        structObj.nakshatra = nakshatraDeref;
        const padaDeref = (new Uint8Array(wasm.memory.buffer, ptr + 42, 1))[0];
        structObj.pada = padaDeref;
        const retrogradeDeref = (new Uint8Array(wasm.memory.buffer, ptr + 43, 1))[0] === 1;
        structObj.retrograde = retrogradeDeref;

        return new BodyPosition(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}