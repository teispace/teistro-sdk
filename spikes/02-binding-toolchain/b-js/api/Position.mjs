// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * A tropical position, the provider's answer.
 */
export class Position {
    #longitudeDeg;
    get longitudeDeg() {
        return this.#longitudeDeg;
    }
    set longitudeDeg(value){
        this.#longitudeDeg = value;
    }
    #latitudeDeg;
    get latitudeDeg() {
        return this.#latitudeDeg;
    }
    set latitudeDeg(value){
        this.#latitudeDeg = value;
    }
    #speedDegPerDay;
    get speedDegPerDay() {
        return this.#speedDegPerDay;
    }
    set speedDegPerDay(value){
        this.#speedDegPerDay = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new Position(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("Position's constructor takes an object of Position's fields.");
        }

        if ("longitudeDeg" in structObj) {
            this.#longitudeDeg = structObj.longitudeDeg;
        } else {
            throw new Error("Missing required field longitudeDeg.");
        }

        if ("latitudeDeg" in structObj) {
            this.#latitudeDeg = structObj.latitudeDeg;
        } else {
            throw new Error("Missing required field latitudeDeg.");
        }

        if ("speedDegPerDay" in structObj) {
            this.#speedDegPerDay = structObj.speedDegPerDay;
        } else {
            throw new Error("Missing required field speedDegPerDay.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 24;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof Position) {
            return obj;
        }

        return Position.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#longitudeDeg, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#latitudeDeg, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 16, this.#speedDegPerDay, Float64Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("Position._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const longitudeDegDeref = (new Float64Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.longitudeDeg = longitudeDegDeref;
        const latitudeDegDeref = (new Float64Array(wasm.memory.buffer, ptr + 8, 1))[0];
        structObj.latitudeDeg = latitudeDegDeref;
        const speedDegPerDayDeref = (new Float64Array(wasm.memory.buffer, ptr + 16, 1))[0];
        structObj.speedDegPerDay = speedDegPerDayDeref;

        return new Position(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}