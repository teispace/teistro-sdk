// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * Which lunar node the provider is asked for.
 */
export class NodeKind {
    #value = undefined;

    static #values = new Map([
        ["Mean", 0],
        ["True", 1]
    ]);

    static getAllEntries() {
        return NodeKind.#values.entries();
    }

    #internalConstructor(value) {
        if (arguments.length > 1 && arguments[0] === diplomatRuntime.internalConstructor) {
            // We pass in two internalConstructor arguments to create *new*
            // instances of this type, otherwise the enums are treated as singletons.
            if (arguments[1] === diplomatRuntime.internalConstructor ) {
                this.#value = arguments[2];
                return this;
            }
            return NodeKind.#objectValues[arguments[1]];
        }

        if (value instanceof NodeKind) {
            return value;
        }

        let intVal = NodeKind.#values.get(value);

        // Nullish check, checks for null or undefined
        if (intVal != null) {
            return NodeKind.#objectValues[intVal];
        }

        throw TypeError(value + " is not a NodeKind and does not correspond to any of its enumerator values.");
    }

    /** @internal */
    static fromValue(value) {
        return new NodeKind(value);
    }

    get value(){
        return [...NodeKind.#values.keys()][this.#value];
    }

    /** @internal */
    get ffiValue(){
        return this.#value;
    }
    static #objectValues = [
        new NodeKind(diplomatRuntime.internalConstructor, diplomatRuntime.internalConstructor, 0),
        new NodeKind(diplomatRuntime.internalConstructor, diplomatRuntime.internalConstructor, 1),
    ];

    static Mean = NodeKind.#objectValues[0];
    /**
     * Renamed for Dart, where `true` is a keyword the backend does not escape.
     */
    static True = NodeKind.#objectValues[1];


    constructor(value) {
        return this.#internalConstructor(...arguments)
    }
}