// generated by diplomat-tool
import { BodyPosition } from "./BodyPosition.mjs"
import { DashaRow } from "./DashaRow.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const Chart_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.Chart_destroy(ptr);
});

/**
 * A computed chart, read through accessors.
 */
export class Chart {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("Chart is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            Chart_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * The instant the chart was computed for.
     */
    get jdUt() {

        const result = wasm.Chart_jd_ut(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The ayanamsha value applied, in degrees.
     */
    get ayanamshaDeg() {

        const result = wasm.Chart_ayanamsha_deg(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The number of positions, nine.
     */
    get positionCount() {

        const result = wasm.Chart_position_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * One position by index.
     */
    position(index) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 49, 8, true);


        const result = wasm.Chart_position(diplomatReceive.buffer, this.ffiValue, index);

        try {
            if (!diplomatReceive.resultFlag) {
                return null;
            }
            return BodyPosition._fromFFI(diplomatRuntime.internalConstructor, diplomatReceive.buffer);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * The number of tree rows.
     */
    get dashaRowCount() {

        const result = wasm.Chart_dasha_row_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * One row of the tree by index, in pre-order.
     */
    dashaRow(index) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 33, 8, true);


        const result = wasm.Chart_dasha_row(diplomatReceive.buffer, this.ffiValue, index);

        try {
            if (!diplomatReceive.resultFlag) {
                return null;
            }
            return DashaRow._fromFFI(diplomatRuntime.internalConstructor, diplomatReceive.buffer);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}