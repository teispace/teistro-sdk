// generated by diplomat-tool
import { Body } from "./Body.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * One row of the tree in pre-order.
 */
export class DashaRow {
    #level;
    get level() {
        return this.#level;
    }
    set level(value){
        this.#level = value;
    }
    #lord;
    get lord() {
        return this.#lord;
    }
    set lord(value){
        this.#lord = value;
    }
    #parent;
    get parent() {
        return this.#parent;
    }
    set parent(value){
        this.#parent = value;
    }
    #startJd;
    get startJd() {
        return this.#startJd;
    }
    set startJd(value){
        this.#startJd = value;
    }
    #endJd;
    get endJd() {
        return this.#endJd;
    }
    set endJd(value){
        this.#endJd = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new DashaRow(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("DashaRow's constructor takes an object of DashaRow's fields.");
        }

        if ("level" in structObj) {
            this.#level = structObj.level;
        } else {
            throw new Error("Missing required field level.");
        }

        if ("lord" in structObj) {
            this.#lord = new Body(structObj.lord);
        } else {
            throw new Error("Missing required field lord.");
        }

        if ("parent" in structObj) {
            this.#parent = structObj.parent;
        } else {
            throw new Error("Missing required field parent.");
        }

        if ("startJd" in structObj) {
            this.#startJd = structObj.startJd;
        } else {
            throw new Error("Missing required field startJd.");
        }

        if ("endJd" in structObj) {
            this.#endJd = structObj.endJd;
        } else {
            throw new Error("Missing required field endJd.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 32;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 8;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof DashaRow) {
            return obj;
        }

        return DashaRow.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#level, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, new Body(this.#lord).ffiValue, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#parent, Int32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 16, this.#startJd, Float64Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 24, this.#endJd, Float64Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("DashaRow._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const levelDeref = (new Uint8Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.level = levelDeref;
        const lordDeref = diplomatRuntime.enumDiscriminant(wasm, ptr + 4);
        structObj.lord = new Body(diplomatRuntime.internalConstructor, lordDeref);
        const parentDeref = (new Int32Array(wasm.memory.buffer, ptr + 8, 1))[0];
        structObj.parent = parentDeref;
        const startJdDeref = (new Float64Array(wasm.memory.buffer, ptr + 16, 1))[0];
        structObj.startJd = startJdDeref;
        const endJdDeref = (new Float64Array(wasm.memory.buffer, ptr + 24, 1))[0];
        structObj.endJd = endJdDeref;

        return new DashaRow(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}