// generated by diplomat-tool
import { Chart } from "./Chart.mjs"
import { ErrorCode } from "./ErrorCode.mjs"
import { Settings } from "./Settings.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const Context_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.Context_destroy(ptr);
});

/**
 * A context: settings plus a provider.
 */
export class Context {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("Context is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            Context_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Creates a context over the built-in analytic test provider.
     */
    #defaultConstructor(settings) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.Context_create(diplomatReceive.buffer, Settings._fromSuppliedValue(diplomatRuntime.internalConstructor, settings)._intoFFI(diplomatRuntime.FUNCTION_PARAM_ALLOC.alloc(Settings._sizeBytes), functionCleanupArena, {}, false));

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new ErrorCode(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('ErrorCode.' + cause.value, { cause });
            }
            return new Context(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * The settings the context was built with.
     */
    settings() {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 12, 4, false);


        const result = wasm.Context_settings(diplomatReceive.buffer, this.ffiValue);

        try {
            return Settings._fromFFI(diplomatRuntime.internalConstructor, diplomatReceive.buffer);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * The one batch call.
     */
    computeChart(jdUt) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.Context_compute_chart(diplomatReceive.buffer, this.ffiValue, jdUt);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new ErrorCode(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('ErrorCode.' + cause.value, { cause });
            }
            return new Chart(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Creates a context over the built-in analytic test provider.
     */
    constructor(settings) {
        if (arguments[0] === diplomatRuntime.exposeConstructor) {
            return this.#internalConstructor(...Array.prototype.slice.call(arguments, 1));
        } else if (arguments[0] === diplomatRuntime.internalConstructor) {
            return this.#internalConstructor(...arguments);
        } else {
            return this.#defaultConstructor(...arguments);
        }
    }
}