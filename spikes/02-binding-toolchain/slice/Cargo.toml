[package]
name = "teistro-spike-slice"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false
description = "Spike 2: the domain slice both binding toolchains bind (a context, settings, an ephemeris port, one batch call returning a tree)"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
