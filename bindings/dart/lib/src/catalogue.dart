// Generated by `cargo xtask gen ffi` from the Teistro SDK's boundary
// crates; do not edit. ABI version 1, SDK 0.0.0.
//
// The description this file was rendered from ships as idl/api.json.
//
// The generator lays this file out, so `dart format .` leaves it alone
// and `cargo xtask check-ffi` can regenerate it on a machine with no
// Dart toolchain and still compare it byte for byte.
// dart format off

// ignore_for_file: constant_identifier_names

/// The ABI these declarations were generated for; the library must agree.
const int generatedAbiVersion = 1;

/// The ABI version of the vtable layout.
const int vtableAbiVersion = 2;

/// A context flag: use the SDK's analytic test provider when no provider
/// vtable is given. For tests and examples only; its positions are not
/// astronomy.
const int contextTestProvider = 1;

/// A kind: a family of entities sharing one key type. The number is the high half of every packed key id.
enum Kind {
  /// The nine grahas of the Parashari tradition and the three outer planets.
  graha(1, 'graha'),
  /// The twelve signs.
  rashi(2, 'rashi'),
  /// The twenty-seven nakshatras; the 28-scheme with Abhijit is a span table, not a member.
  nakshatra(3, 'nakshatra'),
  /// The thirty tithis of the lunar month.
  tithi(4, 'tithi'),
  /// The eleven karanas: seven movable, four fixed.
  karana(5, 'karana'),
  /// The twenty-seven nitya yogas of the Sun and Moon.
  yoga(6, 'yoga'),
  /// The seven weekdays with their lords.
  vara(7, 'vara'),
  /// The twelve lunar months, each named for the nakshatra of its full moon, with the solar month that shares its name.
  masa(8, 'masa'),
  /// The six seasons, two lunar months each.
  ritu(9, 'ritu'),
  /// The two halves of the solar year.
  ayana(10, 'ayana'),
  /// The two halves of the lunar month.
  paksha(11, 'paksha'),
  /// The sixty years of the Jovian cycle, in order.
  samvatsara(12, 'samvatsara'),
  /// The five elements.
  tatwa(13, 'tatwa'),
  /// The varna classification of nakshatras and grahas.
  varna(14, 'varna'),
  /// The three ganas of the nakshatras.
  gana(15, 'gana'),
  /// The three nadis of the nakshatras.
  nadi(16, 'nadi'),
  /// The fourteen yoni animals of the nakshatras, each with its hostile pair.
  yoni(17, 'yoni'),
  /// The deities the nakshatras, tithis and karanas are assigned to.
  deity(18, 'deity'),
  /// The eleven levels of dignity.
  dignity(19, 'dignity'),
  /// The five-fold compound relationship.
  relationship(20, 'relationship'),
  /// The five age states.
  avasthaBaladi(21, 'avastha_baladi'),
  /// Conditions a body can be in.
  state(22, 'state'),
  /// The catalogued ayanamshas; a custom one is a settings value, not a member.
  ayanamsha(23, 'ayanamsha'),
  /// The house systems; the cusp arithmetic lives in the astro layer.
  houseSystem(24, 'house_system'),
  /// The divisional charts the SDK ships; the kernel row of each lives in the vargas crate.
  varga(25, 'varga'),
  /// The dasha systems the catalogue names; each row of the kernel lives in the dasha crate, and only V rows are implemented.
  dashaSystem(26, 'dasha_system'),
  /// The strength schemes; rows live in the strength kernel.
  balaScheme(27, 'bala_scheme'),
  /// The Ashta Koota and the Dasha Koota extensions.
  koota(28, 'koota'),
  /// The Jaimini significators, in rank order.
  charaKaraka(29, 'chara_karaka'),
  /// What a chart is a chart of.
  chartKind(30, 'chart_kind'),
  /// Derived points that behave like bodies; each has a formula in the module that owns it.
  point(31, 'point'),
  /// The seven muhurta natures of the nakshatras.
  muhurtaNature(33, 'muhurta_nature'),
  /// Movable, fixed and dual signs.
  modality(34, 'modality'),
  /// The natural benefic or malefic character of a graha.
  nature(35, 'nature'),
  /// The three gunas.
  guna(36, 'guna'),
  /// The eight directions.
  direction(37, 'direction'),
  /// Grammatical and classical gender.
  gender(38, 'gender'),
  /// The calendars the SDK ships.
  calendar(39, 'calendar'),
  /// The era numbers a date may carry.
  era(40, 'era'),
  /// What kind of body a graha is.
  bodyClass(41, 'body_class'),
  /// Odd and even signs.
  parity(42, 'parity'),
  /// How a sign rises: head first, back first, or both.
  rising(43, 'rising'),
  /// The sex of a nakshatra's yoni animal.
  sex(44, 'sex'),
  /// The five tithi classes.
  tithiClass(45, 'tithi_class'),
  /// The auspiciousness class of a nitya yoga.
  auspiciousness(46, 'auspiciousness'),
  /// Whether a house system is undefined at some latitudes.
  degeneracy(47, 'degeneracy'),
  /// How an ayanamsha is defined or used.
  ayanamshaCategory(49, 'ayanamsha_category'),
  /// Which kernel a dasha system runs on.
  dashaFamily(50, 'dasha_family'),
  /// The three wakefulness states.
  avasthaJagradadi(51, 'avastha_jagradadi'),
  /// The nine Deeptadi states.
  avasthaDeeptadi(52, 'avastha_deeptadi'),
  /// The six Lajjitadi states.
  avasthaLajjitadi(53, 'avastha_lajjitadi'),
  /// The twelve Sayanadi states.
  avasthaSayanadi(54, 'avastha_sayanadi'),
  /// What kind of derived point.
  pointFamily(55, 'point_family'),
  /// The fixed stars and fixed directions the SDK places: the anchors of the star-anchored ayanamshas, the yogataras of the nakshatras, the bright stars of the fixed-star tradition, the galactic centre and the galactic poles, each with its ICRS astrometry at epoch J2000.0 (`03-design/astro-star-table.md`).
  star(56, 'star'),
  /// What kind of object a star-table member is.
  starClass(57, 'star_class');

  const Kind(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Kind byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Kind'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Kind? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The nine grahas of the Parashari tradition and the three outer planets. Members are the catalogue's ids; the full key id is `(TS_KIND_GRAHA << 16) | member`.
enum Graha {
  /// The Sun
  sun(0, 'SUN'),
  /// The Moon
  moon(1, 'MOON'),
  /// Mars
  mars(2, 'MARS'),
  /// Mercury
  mercury(3, 'MERCURY'),
  /// Jupiter
  jupiter(4, 'JUPITER'),
  /// Venus
  venus(5, 'VENUS'),
  /// Saturn
  saturn(6, 'SATURN'),
  /// The ascending lunar node
  rahu(7, 'RAHU'),
  /// The descending lunar node
  ketu(8, 'KETU'),
  /// Uranus
  uranus(9, 'URANUS'),
  /// Neptune
  neptune(10, 'NEPTUNE'),
  /// Pluto
  pluto(11, 'PLUTO'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Graha(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'graha.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Graha byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Graha.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Graha? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The twelve signs. Members are the catalogue's ids; the full key id is `(TS_KIND_RASHI << 16) | member`.
enum Rashi {
  /// Aries
  aries(0, 'ARIES'),
  /// Taurus
  taurus(1, 'TAURUS'),
  /// Gemini
  gemini(2, 'GEMINI'),
  /// Cancer
  cancer(3, 'CANCER'),
  /// Leo
  leo(4, 'LEO'),
  /// Virgo
  virgo(5, 'VIRGO'),
  /// Libra
  libra(6, 'LIBRA'),
  /// Scorpio
  scorpio(7, 'SCORPIO'),
  /// Sagittarius
  sagittarius(8, 'SAGITTARIUS'),
  /// Capricorn
  capricorn(9, 'CAPRICORN'),
  /// Aquarius
  aquarius(10, 'AQUARIUS'),
  /// Pisces
  pisces(11, 'PISCES'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Rashi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'rashi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Rashi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Rashi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Rashi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The twenty-seven nakshatras; the 28-scheme with Abhijit is a span table, not a member. Members are the catalogue's ids; the full key id is `(TS_KIND_NAKSHATRA << 16) | member`.
enum Nakshatra {
  /// Ashwini
  ashwini(0, 'ASHWINI'),
  /// Bharani
  bharani(1, 'BHARANI'),
  /// Krittika
  krittika(2, 'KRITTIKA'),
  /// Rohini
  rohini(3, 'ROHINI'),
  /// Mrigashira
  mrigashira(4, 'MRIGASHIRA'),
  /// Ardra
  ardra(5, 'ARDRA'),
  /// Punarvasu
  punarvasu(6, 'PUNARVASU'),
  /// Pushya
  pushya(7, 'PUSHYA'),
  /// Ashlesha
  ashlesha(8, 'ASHLESHA'),
  /// Magha
  magha(9, 'MAGHA'),
  /// Purva Phalguni
  purvaPhalguni(10, 'PURVA_PHALGUNI'),
  /// Uttara Phalguni
  uttaraPhalguni(11, 'UTTARA_PHALGUNI'),
  /// Hasta
  hasta(12, 'HASTA'),
  /// Chitra
  chitra(13, 'CHITRA'),
  /// Swati
  swati(14, 'SWATI'),
  /// Vishakha
  vishakha(15, 'VISHAKHA'),
  /// Anuradha
  anuradha(16, 'ANURADHA'),
  /// Jyeshtha
  jyeshtha(17, 'JYESHTHA'),
  /// Mula
  mula(18, 'MULA'),
  /// Purva Ashadha
  purvaAshadha(19, 'PURVA_ASHADHA'),
  /// Uttara Ashadha
  uttaraAshadha(20, 'UTTARA_ASHADHA'),
  /// Shravana
  shravana(21, 'SHRAVANA'),
  /// Dhanishtha
  dhanishtha(22, 'DHANISHTHA'),
  /// Shatabhisha
  shatabhisha(23, 'SHATABHISHA'),
  /// Purva Bhadrapada
  purvaBhadrapada(24, 'PURVA_BHADRAPADA'),
  /// Uttara Bhadrapada
  uttaraBhadrapada(25, 'UTTARA_BHADRAPADA'),
  /// Revati
  revati(26, 'REVATI'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Nakshatra(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'nakshatra.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Nakshatra byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Nakshatra.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Nakshatra? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The thirty tithis of the lunar month. Members are the catalogue's ids; the full key id is `(TS_KIND_TITHI << 16) | member`.
enum Tithi {
  /// Shukla Pratipada
  shuklaPratipada(0, 'SHUKLA_PRATIPADA'),
  /// Shukla Dvitiya
  shuklaDvitiya(1, 'SHUKLA_DVITIYA'),
  /// Shukla Tritiya
  shuklaTritiya(2, 'SHUKLA_TRITIYA'),
  /// Shukla Chaturthi
  shuklaChaturthi(3, 'SHUKLA_CHATURTHI'),
  /// Shukla Panchami
  shuklaPanchami(4, 'SHUKLA_PANCHAMI'),
  /// Shukla Shashthi
  shuklaShashthi(5, 'SHUKLA_SHASHTHI'),
  /// Shukla Saptami
  shuklaSaptami(6, 'SHUKLA_SAPTAMI'),
  /// Shukla Ashtami
  shuklaAshtami(7, 'SHUKLA_ASHTAMI'),
  /// Shukla Navami
  shuklaNavami(8, 'SHUKLA_NAVAMI'),
  /// Shukla Dashami
  shuklaDashami(9, 'SHUKLA_DASHAMI'),
  /// Shukla Ekadashi
  shuklaEkadashi(10, 'SHUKLA_EKADASHI'),
  /// Shukla Dwadashi
  shuklaDwadashi(11, 'SHUKLA_DWADASHI'),
  /// Shukla Trayodashi
  shuklaTrayodashi(12, 'SHUKLA_TRAYODASHI'),
  /// Shukla Chaturdashi
  shuklaChaturdashi(13, 'SHUKLA_CHATURDASHI'),
  /// Purnima
  purnima(14, 'PURNIMA'),
  /// Krishna Pratipada
  krishnaPratipada(15, 'KRISHNA_PRATIPADA'),
  /// Krishna Dvitiya
  krishnaDvitiya(16, 'KRISHNA_DVITIYA'),
  /// Krishna Tritiya
  krishnaTritiya(17, 'KRISHNA_TRITIYA'),
  /// Krishna Chaturthi
  krishnaChaturthi(18, 'KRISHNA_CHATURTHI'),
  /// Krishna Panchami
  krishnaPanchami(19, 'KRISHNA_PANCHAMI'),
  /// Krishna Shashthi
  krishnaShashthi(20, 'KRISHNA_SHASHTHI'),
  /// Krishna Saptami
  krishnaSaptami(21, 'KRISHNA_SAPTAMI'),
  /// Krishna Ashtami
  krishnaAshtami(22, 'KRISHNA_ASHTAMI'),
  /// Krishna Navami
  krishnaNavami(23, 'KRISHNA_NAVAMI'),
  /// Krishna Dashami
  krishnaDashami(24, 'KRISHNA_DASHAMI'),
  /// Krishna Ekadashi
  krishnaEkadashi(25, 'KRISHNA_EKADASHI'),
  /// Krishna Dwadashi
  krishnaDwadashi(26, 'KRISHNA_DWADASHI'),
  /// Krishna Trayodashi
  krishnaTrayodashi(27, 'KRISHNA_TRAYODASHI'),
  /// Krishna Chaturdashi
  krishnaChaturdashi(28, 'KRISHNA_CHATURDASHI'),
  /// Amavasya
  amavasya(29, 'AMAVASYA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Tithi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'tithi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Tithi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Tithi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Tithi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The eleven karanas: seven movable, four fixed. Members are the catalogue's ids; the full key id is `(TS_KIND_KARANA << 16) | member`.
enum Karana {
  /// Bava
  bava(0, 'BAVA'),
  /// Balava
  balava(1, 'BALAVA'),
  /// Kaulava
  kaulava(2, 'KAULAVA'),
  /// Taitila
  taitila(3, 'TAITILA'),
  /// Garija
  garija(4, 'GARIJA'),
  /// Vanija
  vanija(5, 'VANIJA'),
  /// Vishti
  vishti(6, 'VISHTI'),
  /// Shakuni
  shakuni(7, 'SHAKUNI'),
  /// Chatushpada
  chatushpada(8, 'CHATUSHPADA'),
  /// Naga
  naga(9, 'NAGA'),
  /// Kimstughna
  kimstughna(10, 'KIMSTUGHNA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Karana(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'karana.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Karana byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Karana.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Karana? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The twenty-seven nitya yogas of the Sun and Moon. Members are the catalogue's ids; the full key id is `(TS_KIND_YOGA << 16) | member`.
enum Yoga {
  /// Vishkambha
  vishkambha(0, 'VISHKAMBHA'),
  /// Priti
  priti(1, 'PRITI'),
  /// Ayushman
  ayushman(2, 'AYUSHMAN'),
  /// Saubhagya
  saubhagya(3, 'SAUBHAGYA'),
  /// Shobhana
  shobhana(4, 'SHOBHANA'),
  /// Atiganda
  atiganda(5, 'ATIGANDA'),
  /// Sukarma
  sukarma(6, 'SUKARMA'),
  /// Dhriti
  dhriti(7, 'DHRITI'),
  /// Shoola
  shoola(8, 'SHOOLA'),
  /// Ganda
  ganda(9, 'GANDA'),
  /// Vriddhi
  vriddhi(10, 'VRIDDHI'),
  /// Dhruva
  dhruva(11, 'DHRUVA'),
  /// Vyaghata
  vyaghata(12, 'VYAGHATA'),
  /// Harshana
  harshana(13, 'HARSHANA'),
  /// Vajra
  vajra(14, 'VAJRA'),
  /// Siddhi
  siddhi(15, 'SIDDHI'),
  /// Vyatipata
  vyatipata(16, 'VYATIPATA'),
  /// Variyana
  variyana(17, 'VARIYANA'),
  /// Parigha
  parigha(18, 'PARIGHA'),
  /// Shiva
  shiva(19, 'SHIVA'),
  /// Siddha
  siddha(20, 'SIDDHA'),
  /// Sadhya
  sadhya(21, 'SADHYA'),
  /// Shubha
  shubha(22, 'SHUBHA'),
  /// Shukla
  shukla(23, 'SHUKLA'),
  /// Brahma
  brahma(24, 'BRAHMA'),
  /// Indra
  indra(25, 'INDRA'),
  /// Vaidhriti
  vaidhriti(26, 'VAIDHRITI'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Yoga(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'yoga.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Yoga byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Yoga.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Yoga? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The seven weekdays with their lords. Members are the catalogue's ids; the full key id is `(TS_KIND_VARA << 16) | member`.
enum Vara {
  /// Sunday
  ravivara(0, 'RAVIVARA'),
  /// Monday
  somavara(1, 'SOMAVARA'),
  /// Tuesday
  mangalavara(2, 'MANGALAVARA'),
  /// Wednesday
  budhavara(3, 'BUDHAVARA'),
  /// Thursday
  guruvara(4, 'GURUVARA'),
  /// Friday
  shukravara(5, 'SHUKRAVARA'),
  /// Saturday
  shanivara(6, 'SHANIVARA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Vara(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'vara.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Vara byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Vara.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Vara? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The twelve lunar months, each named for the nakshatra of its full moon, with the solar month that shares its name. Members are the catalogue's ids; the full key id is `(TS_KIND_MASA << 16) | member`.
enum Masa {
  /// Chaitra
  chaitra(0, 'CHAITRA'),
  /// Vaishakha
  vaishakha(1, 'VAISHAKHA'),
  /// Jyeshtha
  jyeshtha(2, 'JYESHTHA'),
  /// Ashadha
  ashadha(3, 'ASHADHA'),
  /// Shravana
  shravana(4, 'SHRAVANA'),
  /// Bhadrapada
  bhadrapada(5, 'BHADRAPADA'),
  /// Ashwina
  ashwina(6, 'ASHWINA'),
  /// Kartika
  kartika(7, 'KARTIKA'),
  /// Margashirsha
  margashirsha(8, 'MARGASHIRSHA'),
  /// Pausha
  pausha(9, 'PAUSHA'),
  /// Magha
  magha(10, 'MAGHA'),
  /// Phalguna
  phalguna(11, 'PHALGUNA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Masa(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'masa.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Masa byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Masa.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Masa? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The six seasons, two lunar months each. Members are the catalogue's ids; the full key id is `(TS_KIND_RITU << 16) | member`.
enum Ritu {
  /// Vasanta
  vasanta(0, 'VASANTA'),
  /// Grishma
  grishma(1, 'GRISHMA'),
  /// Varsha
  varsha(2, 'VARSHA'),
  /// Sharad
  sharad(3, 'SHARAD'),
  /// Hemanta
  hemanta(4, 'HEMANTA'),
  /// Shishira
  shishira(5, 'SHISHIRA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Ritu(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'ritu.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Ritu byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Ritu.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Ritu? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The two halves of the solar year. Members are the catalogue's ids; the full key id is `(TS_KIND_AYANA << 16) | member`.
enum Ayana {
  /// Uttarayana
  uttarayana(0, 'UTTARAYANA'),
  /// Dakshinayana
  dakshinayana(1, 'DAKSHINAYANA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Ayana(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'ayana.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Ayana byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Ayana.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Ayana? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The two halves of the lunar month. Members are the catalogue's ids; the full key id is `(TS_KIND_PAKSHA << 16) | member`.
enum Paksha {
  /// The bright half
  shukla(0, 'SHUKLA'),
  /// The dark half
  krishna(1, 'KRISHNA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Paksha(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'paksha.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Paksha byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Paksha.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Paksha? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The sixty years of the Jovian cycle, in order. Members are the catalogue's ids; the full key id is `(TS_KIND_SAMVATSARA << 16) | member`.
enum Samvatsara {
  /// Prabhava
  prabhava(0, 'PRABHAVA'),
  /// Vibhava
  vibhava(1, 'VIBHAVA'),
  /// Shukla
  shukla(2, 'SHUKLA'),
  /// Pramodoota
  pramodoota(3, 'PRAMODOOTA'),
  /// Prajotpatti
  prajotpatti(4, 'PRAJOTPATTI'),
  /// Angiras
  angiras(5, 'ANGIRAS'),
  /// Shrimukha
  shrimukha(6, 'SHRIMUKHA'),
  /// Bhava
  bhava(7, 'BHAVA'),
  /// Yuva
  yuva(8, 'YUVA'),
  /// Dhaata
  dhaata(9, 'DHAATA'),
  /// Ishvara
  ishvara(10, 'ISHVARA'),
  /// Bahudhanya
  bahudhanya(11, 'BAHUDHANYA'),
  /// Pramathi
  pramathi(12, 'PRAMATHI'),
  /// Vikrama
  vikrama(13, 'VIKRAMA'),
  /// Vrisha
  vrisha(14, 'VRISHA'),
  /// Chitrabhanu
  chitrabhanu(15, 'CHITRABHANU'),
  /// Subhanu
  subhanu(16, 'SUBHANU'),
  /// Taarana
  taarana(17, 'TAARANA'),
  /// Paarthiva
  paarthiva(18, 'PAARTHIVA'),
  /// Vyaya
  vyaya(19, 'VYAYA'),
  /// Sarvajit
  sarvajit(20, 'SARVAJIT'),
  /// Sarvadhari
  sarvadhari(21, 'SARVADHARI'),
  /// Virodhi
  virodhi(22, 'VIRODHI'),
  /// Vikrita
  vikrita(23, 'VIKRITA'),
  /// Khara
  khara(24, 'KHARA'),
  /// Nandana
  nandana(25, 'NANDANA'),
  /// Vijaya
  vijaya(26, 'VIJAYA'),
  /// Jaya
  jaya(27, 'JAYA'),
  /// Manmatha
  manmatha(28, 'MANMATHA'),
  /// Durmukhi
  durmukhi(29, 'DURMUKHI'),
  /// Hevilambi
  hevilambi(30, 'HEVILAMBI'),
  /// Vilambi
  vilambi(31, 'VILAMBI'),
  /// Vikari
  vikari(32, 'VIKARI'),
  /// Sharvari
  sharvari(33, 'SHARVARI'),
  /// Plava
  plava(34, 'PLAVA'),
  /// Shubhakrit
  shubhakrit(35, 'SHUBHAKRIT'),
  /// Shobhakrit
  shobhakrit(36, 'SHOBHAKRIT'),
  /// Krodhi
  krodhi(37, 'KRODHI'),
  /// Vishvavasu
  vishvavasu(38, 'VISHVAVASU'),
  /// Parabhava
  parabhava(39, 'PARABHAVA'),
  /// Plavanga
  plavanga(40, 'PLAVANGA'),
  /// Kilaka
  kilaka(41, 'KILAKA'),
  /// Saumya
  saumya(42, 'SAUMYA'),
  /// Sadharana
  sadharana(43, 'SADHARANA'),
  /// Virodhikrit
  virodhikrit(44, 'VIRODHIKRIT'),
  /// Paridhaavi
  paridhaavi(45, 'PARIDHAAVI'),
  /// Pramadicha
  pramadicha(46, 'PRAMADICHA'),
  /// Ananda
  ananda(47, 'ANANDA'),
  /// Rakshasa
  rakshasa(48, 'RAKSHASA'),
  /// Nala
  nala(49, 'NALA'),
  /// Pingala
  pingala(50, 'PINGALA'),
  /// Kalayukta
  kalayukta(51, 'KALAYUKTA'),
  /// Siddharthi
  siddharthi(52, 'SIDDHARTHI'),
  /// Raudra
  raudra(53, 'RAUDRA'),
  /// Durmati
  durmati(54, 'DURMATI'),
  /// Dundubhi
  dundubhi(55, 'DUNDUBHI'),
  /// Rudhirodgari
  rudhirodgari(56, 'RUDHIRODGARI'),
  /// Raktakshi
  raktakshi(57, 'RAKTAKSHI'),
  /// Krodhana
  krodhana(58, 'KRODHANA'),
  /// Akshaya
  akshaya(59, 'AKSHAYA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Samvatsara(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'samvatsara.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Samvatsara byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Samvatsara.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Samvatsara? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The five elements. Members are the catalogue's ids; the full key id is `(TS_KIND_TATWA << 16) | member`.
enum Tatwa {
  /// Fire
  agni(0, 'AGNI'),
  /// Earth
  prithvi(1, 'PRITHVI'),
  /// Air
  vayu(2, 'VAYU'),
  /// Water
  jala(3, 'JALA'),
  /// Ether
  akasha(4, 'AKASHA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Tatwa(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'tatwa.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Tatwa byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Tatwa.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Tatwa? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The varna classification of nakshatras and grahas. Members are the catalogue's ids; the full key id is `(TS_KIND_VARNA << 16) | member`.
enum Varna {
  /// Brahmin
  brahmin(0, 'BRAHMIN'),
  /// Kshatriya
  kshatriya(1, 'KSHATRIYA'),
  /// Vaishya
  vaishya(2, 'VAISHYA'),
  /// Shudra
  shudra(3, 'SHUDRA'),
  /// Mleccha
  mleccha(4, 'MLECCHA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Varna(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'varna.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Varna byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Varna.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Varna? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The three ganas of the nakshatras. Members are the catalogue's ids; the full key id is `(TS_KIND_GANA << 16) | member`.
enum Gana {
  /// Deva
  deva(0, 'DEVA'),
  /// Manushya
  manushya(1, 'MANUSHYA'),
  /// Rakshasa
  rakshasa(2, 'RAKSHASA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Gana(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'gana.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Gana byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Gana.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Gana? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The three nadis of the nakshatras. Members are the catalogue's ids; the full key id is `(TS_KIND_NADI << 16) | member`.
enum Nadi {
  /// Aadi
  aadi(0, 'AADI'),
  /// Madhya
  madhya(1, 'MADHYA'),
  /// Antya
  antya(2, 'ANTYA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Nadi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'nadi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Nadi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Nadi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Nadi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The fourteen yoni animals of the nakshatras, each with its hostile pair. Members are the catalogue's ids; the full key id is `(TS_KIND_YONI << 16) | member`.
enum Yoni {
  /// Horse
  horse(0, 'HORSE'),
  /// Elephant
  elephant(1, 'ELEPHANT'),
  /// Goat
  goat(2, 'GOAT'),
  /// Serpent
  serpent(3, 'SERPENT'),
  /// Dog
  dog(4, 'DOG'),
  /// Cat
  cat(5, 'CAT'),
  /// Rat
  rat(6, 'RAT'),
  /// Cow
  cow(7, 'COW'),
  /// Buffalo
  buffalo(8, 'BUFFALO'),
  /// Tiger
  tiger(9, 'TIGER'),
  /// Deer
  deer(10, 'DEER'),
  /// Monkey
  monkey(11, 'MONKEY'),
  /// Mongoose
  mongoose(12, 'MONGOOSE'),
  /// Lion
  lion(13, 'LION'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Yoni(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'yoni.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Yoni byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Yoni.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Yoni? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The deities the nakshatras, tithis and karanas are assigned to. Members are the catalogue's ids; the full key id is `(TS_KIND_DEITY << 16) | member`.
enum Deity {
  /// The Ashwini Kumaras, the divine physicians
  ashwiniKumara(0, 'ASHWINI_KUMARA'),
  /// Yama
  yama(1, 'YAMA'),
  /// Agni
  agni(2, 'AGNI'),
  /// Brahma
  brahma(3, 'BRAHMA'),
  /// Soma
  soma(4, 'SOMA'),
  /// Rudra
  rudra(5, 'RUDRA'),
  /// Aditi
  aditi(6, 'ADITI'),
  /// Brihaspati
  brihaspati(7, 'BRIHASPATI'),
  /// Naga
  naga(8, 'NAGA'),
  /// Pitru
  pitru(9, 'PITRU'),
  /// Bhaga
  bhaga(10, 'BHAGA'),
  /// Aryaman
  aryaman(11, 'ARYAMAN'),
  /// Savitr, the Sun as impeller
  savitr(12, 'SAVITR'),
  /// Vishvakarma
  vishvakarma(13, 'VISHVAKARMA'),
  /// Vayu
  vayu(14, 'VAYU'),
  /// Indra and Agni together
  indragni(15, 'INDRAGNI'),
  /// Mitra
  mitra(16, 'MITRA'),
  /// Indra
  indra(17, 'INDRA'),
  /// Nirriti
  nirriti(18, 'NIRRITI'),
  /// Apas
  apas(19, 'APAS'),
  /// The Vishvadevas, all the gods
  vishvadeva(20, 'VISHVADEVA'),
  /// Vishnu
  vishnu(21, 'VISHNU'),
  /// The eight Vasus
  vasu(22, 'VASU'),
  /// Varuna
  varuna(23, 'VARUNA'),
  /// Aja Ekapada
  ajaEkapada(24, 'AJA_EKAPADA'),
  /// Ahirbudhnya
  ahirbudhnya(25, 'AHIRBUDHNYA'),
  /// Pushan
  pushan(26, 'PUSHAN'),
  /// Vidhata
  vidhata(27, 'VIDHATA'),
  /// Kartikeya
  kartikeya(28, 'KARTIKEYA'),
  /// Surya, the Sun as deity
  surya(29, 'SURYA'),
  /// Shiva
  shiva(30, 'SHIVA'),
  /// Durga
  durga(31, 'DURGA'),
  /// Kamadeva
  kamadeva(32, 'KAMADEVA'),
  /// Chandra, the Moon as deity
  chandra(33, 'CHANDRA'),
  /// Tvashta
  tvashta(34, 'TVASHTA'),
  /// Shani, Saturn as deity
  shani(35, 'SHANI'),
  /// Shukra, Venus as deity
  shukra(36, 'SHUKRA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Deity(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'deity.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Deity byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Deity.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Deity? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The eleven levels of dignity. Members are the catalogue's ids; the full key id is `(TS_KIND_DIGNITY << 16) | member`.
enum Dignity {
  /// Deep Exalted
  deepExalted(0, 'DEEP_EXALTED'),
  /// Exalted
  exalted(1, 'EXALTED'),
  /// Mooltrikona
  mooltrikona(2, 'MOOLTRIKONA'),
  /// Own Sign
  ownSign(3, 'OWN_SIGN'),
  /// Great Friend
  greatFriend(4, 'GREAT_FRIEND'),
  /// Friend
  friend(5, 'FRIEND'),
  /// Neutral
  neutral(6, 'NEUTRAL'),
  /// Enemy
  enemy(7, 'ENEMY'),
  /// Great Enemy
  greatEnemy(8, 'GREAT_ENEMY'),
  /// Debilitated
  debilitated(9, 'DEBILITATED'),
  /// Deep Debilitated
  deepDebilitated(10, 'DEEP_DEBILITATED'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Dignity(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'dignity.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Dignity byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Dignity.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Dignity? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The five-fold compound relationship. Members are the catalogue's ids; the full key id is `(TS_KIND_RELATIONSHIP << 16) | member`.
enum Relationship {
  /// Great Friend
  greatFriend(0, 'GREAT_FRIEND'),
  /// Friend
  friend(1, 'FRIEND'),
  /// Neutral
  neutral(2, 'NEUTRAL'),
  /// Enemy
  enemy(3, 'ENEMY'),
  /// Great Enemy
  greatEnemy(4, 'GREAT_ENEMY'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Relationship(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'relationship.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Relationship byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Relationship.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Relationship? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The five age states. Members are the catalogue's ids; the full key id is `(TS_KIND_AVASTHA_BALADI << 16) | member`.
enum AvasthaBaladi {
  /// Bala
  bala(0, 'BALA'),
  /// Kumara
  kumara(1, 'KUMARA'),
  /// Yuva
  yuva(2, 'YUVA'),
  /// Vriddha
  vriddha(3, 'VRIDDHA'),
  /// Mrita
  mrita(4, 'MRITA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AvasthaBaladi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'avastha_baladi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AvasthaBaladi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AvasthaBaladi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AvasthaBaladi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Conditions a body can be in. Members are the catalogue's ids; the full key id is `(TS_KIND_STATE << 16) | member`.
enum State {
  /// Retrograde
  retrograde(0, 'RETROGRADE'),
  /// Stationary
  stationary(1, 'STATIONARY'),
  /// Combust
  combust(2, 'COMBUST'),
  /// Planetary War
  planetaryWar(3, 'PLANETARY_WAR'),
  /// Gandanta
  gandanta(4, 'GANDANTA'),
  /// Sandhi
  sandhi(5, 'SANDHI'),
  /// Vargottama
  vargottama(6, 'VARGOTTAMA'),
  /// Pushkara Navamsa
  pushkaraNavamsa(7, 'PUSHKARA_NAVAMSA'),
  /// Pushkara Bhaga
  pushkaraBhaga(8, 'PUSHKARA_BHAGA'),
  /// Mrityu Bhaga
  mrityuBhaga(9, 'MRITYU_BHAGA'),
  /// Marana Karaka Sthana
  maranaKarakaSthana(10, 'MARANA_KARAKA_STHANA'),
  /// Eclipsed
  eclipsed(11, 'ECLIPSED'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const State(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'state.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static State byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => State.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static State? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The catalogued ayanamshas; a custom one is a settings value, not a member. Members are the catalogue's ids; the full key id is `(TS_KIND_AYANAMSHA << 16) | member`.
enum Ayanamsha {
  /// Fagan and Bradley
  faganBradley(0, 'FAGAN_BRADLEY'),
  /// Lahiri (Chitrapaksha), the Indian government's ayanamsha
  lahiri(1, 'LAHIRI'),
  /// Deluce
  deluce(2, 'DELUCE'),
  /// B. V. Raman
  raman(3, 'RAMAN'),
  /// Ushashashi
  ushashashi(4, 'USHASHASHI'),
  /// Krishnamurti (KP)
  krishnamurti(5, 'KRISHNAMURTI'),
  /// Djwhal Khul
  djwhalKhul(6, 'DJWHAL_KHUL'),
  /// Yukteshwar
  yukteshwar(7, 'YUKTESHWAR'),
  /// Jn Bhasin
  jnBhasin(8, 'JN_BHASIN'),
  /// Babyl Kugler1
  babylKugler1(9, 'BABYL_KUGLER1'),
  /// Babyl Kugler2
  babylKugler2(10, 'BABYL_KUGLER2'),
  /// Babyl Kugler3
  babylKugler3(11, 'BABYL_KUGLER3'),
  /// Babyl Huber
  babylHuber(12, 'BABYL_HUBER'),
  /// Babyl Etpsc
  babylEtpsc(13, 'BABYL_ETPSC'),
  /// Aldebaran 15tau
  aldebaran15tau(14, 'ALDEBARAN_15TAU'),
  /// Hipparchos
  hipparchos(15, 'HIPPARCHOS'),
  /// Sassanian
  sassanian(16, 'SASSANIAN'),
  /// Galcent 0sag
  galcent0sag(17, 'GALCENT_0SAG'),
  /// J2000
  j2000(18, 'J2000'),
  /// J1900
  j1900(19, 'J1900'),
  /// B1950
  b1950(20, 'B1950'),
  /// Suryasiddhanta
  suryasiddhanta(21, 'SURYASIDDHANTA'),
  /// Suryasiddhanta Msun
  suryasiddhantaMsun(22, 'SURYASIDDHANTA_MSUN'),
  /// Aryabhata
  aryabhata(23, 'ARYABHATA'),
  /// Aryabhata Msun
  aryabhataMsun(24, 'ARYABHATA_MSUN'),
  /// Ss Revati
  ssRevati(25, 'SS_REVATI'),
  /// Ss Citra
  ssCitra(26, 'SS_CITRA'),
  /// True Chitra Paksha: Spica at 180 degrees
  trueChitra(27, 'TRUE_CHITRA'),
  /// True Revati
  trueRevati(28, 'TRUE_REVATI'),
  /// True Pushya
  truePushya(29, 'TRUE_PUSHYA'),
  /// Galcent Rgilbrand
  galcentRgilbrand(30, 'GALCENT_RGILBRAND'),
  /// Galequ Iau1958
  galequIau1958(31, 'GALEQU_IAU1958'),
  /// Galequ True
  galequTrue(32, 'GALEQU_TRUE'),
  /// Galequ Mula
  galequMula(33, 'GALEQU_MULA'),
  /// Galalign Mardyks
  galalignMardyks(34, 'GALALIGN_MARDYKS'),
  /// True Mula
  trueMula(35, 'TRUE_MULA'),
  /// Galcent Mula Wilhelm
  galcentMulaWilhelm(36, 'GALCENT_MULA_WILHELM'),
  /// Aryabhata 522
  aryabhata522(37, 'ARYABHATA_522'),
  /// Babyl Britton
  babylBritton(38, 'BABYL_BRITTON'),
  /// True Sheoran
  trueSheoran(39, 'TRUE_SHEORAN'),
  /// Galcent Cochrane
  galcentCochrane(40, 'GALCENT_COCHRANE'),
  /// Galequ Fiorenza
  galequFiorenza(41, 'GALEQU_FIORENZA'),
  /// Valens Moon
  valensMoon(42, 'VALENS_MOON'),
  /// Lahiri 1940
  lahiri1940(43, 'LAHIRI_1940'),
  /// Lahiri Vp285
  lahiriVp285(44, 'LAHIRI_VP285'),
  /// Krishnamurti Vp291
  krishnamurtiVp291(45, 'KRISHNAMURTI_VP291'),
  /// Lahiri Icrc
  lahiriIcrc(46, 'LAHIRI_ICRC'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Ayanamsha(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'ayanamsha.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Ayanamsha byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Ayanamsha.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Ayanamsha? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The house systems; the cusp arithmetic lives in the astro layer. Members are the catalogue's ids; the full key id is `(TS_KIND_HOUSE_SYSTEM << 16) | member`.
enum HouseSystem {
  /// Whole Sign
  wholeSign(0, 'WHOLE_SIGN'),
  /// Placidus
  placidus(1, 'PLACIDUS'),
  /// Koch
  koch(2, 'KOCH'),
  /// Regiomontanus
  regiomontanus(3, 'REGIOMONTANUS'),
  /// Campanus
  campanus(4, 'CAMPANUS'),
  /// Equal
  equal(5, 'EQUAL'),
  /// Meridian
  meridian(6, 'MERIDIAN'),
  /// Alcabitius
  alcabitius(7, 'ALCABITIUS'),
  /// Porphyry
  porphyry(8, 'PORPHYRY'),
  /// Topocentric
  topocentric(9, 'TOPOCENTRIC'),
  /// Morinus
  morinus(10, 'MORINUS'),
  /// Sripati
  sripati(11, 'SRIPATI'),
  /// Equal Mc
  equalMc(12, 'EQUAL_MC'),
  /// Equal Aries
  equalAries(13, 'EQUAL_ARIES'),
  /// Vehlow
  vehlow(14, 'VEHLOW'),
  /// Carter
  carter(15, 'CARTER'),
  /// Horizon
  horizon(16, 'HORIZON'),
  /// Sunshine
  sunshine(17, 'SUNSHINE'),
  /// Pullen Sd
  pullenSd(18, 'PULLEN_SD'),
  /// Pullen Sr
  pullenSr(19, 'PULLEN_SR'),
  /// Krusinski
  krusinski(20, 'KRUSINSKI'),
  /// Apc
  apc(21, 'APC'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const HouseSystem(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'house_system.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static HouseSystem byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => HouseSystem.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static HouseSystem? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The divisional charts the SDK ships; the kernel row of each lives in the vargas crate. Members are the catalogue's ids; the full key id is `(TS_KIND_VARGA << 16) | member`.
enum Varga {
  /// Rashi
  d1(0, 'D1'),
  /// Hora
  d2(1, 'D2'),
  /// Drekkana
  d3(2, 'D3'),
  /// Chaturthamsha
  d4(3, 'D4'),
  /// Panchamsha
  d5(4, 'D5'),
  /// Shashthamsha
  d6(5, 'D6'),
  /// Saptamsha
  d7(6, 'D7'),
  /// Ashtamsha
  d8(7, 'D8'),
  /// Navamsha
  d9(8, 'D9'),
  /// Dashamsha
  d10(9, 'D10'),
  /// Rudramsha
  d11(10, 'D11'),
  /// Dwadashamsha
  d12(11, 'D12'),
  /// Shodashamsha
  d16(12, 'D16'),
  /// Vimshamsha
  d20(13, 'D20'),
  /// Chaturvimshamsha
  d24(14, 'D24'),
  /// Saptavimshamsha
  d27(15, 'D27'),
  /// Trimshamsha
  d30(16, 'D30'),
  /// Khavedamsha
  d40(17, 'D40'),
  /// Akshavedamsha
  d45(18, 'D45'),
  /// Shashtiamsha
  d60(19, 'D60'),
  /// Nadiamsha
  d150(20, 'D150'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Varga(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'varga.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Varga byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Varga.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Varga? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The dasha systems the catalogue names; each row of the kernel lives in the dasha crate, and only V rows are implemented. Members are the catalogue's ids; the full key id is `(TS_KIND_DASHA_SYSTEM << 16) | member`.
enum DashaSystem {
  /// Vimshottari
  vimshottari(0, 'VIMSHOTTARI'),
  /// Ashtottari
  ashtottari(1, 'ASHTOTTARI'),
  /// Dwadashottari
  dwadashottari(2, 'DWADASHOTTARI'),
  /// Panchottari
  panchottari(3, 'PANCHOTTARI'),
  /// Shatabdika
  shatabdika(4, 'SHATABDIKA'),
  /// Chaturashiti Sama
  chaturashitiSama(5, 'CHATURASHITI_SAMA'),
  /// Dwisaptati Sama
  dwisaptatiSama(6, 'DWISAPTATI_SAMA'),
  /// Yogini
  yogini(7, 'YOGINI'),
  /// Tribhagi
  tribhagi(8, 'TRIBHAGI'),
  /// Shodashottari
  shodashottari(9, 'SHODASHOTTARI'),
  /// Shattrimsha Sama
  shattrimshaSama(10, 'SHATTRIMSHA_SAMA'),
  /// Shashtihayani
  shashtihayani(11, 'SHASHTIHAYANI'),
  /// Tithi Ashtottari
  tithiAshtottari(12, 'TITHI_ASHTOTTARI'),
  /// Tithi Yogini
  tithiYogini(13, 'TITHI_YOGINI'),
  /// Yoga Vimshottari
  yogaVimshottari(14, 'YOGA_VIMSHOTTARI'),
  /// Karana Chaturashiti
  karanaChaturashiti(15, 'KARANA_CHATURASHITI'),
  /// Naisargika
  naisargika(16, 'NAISARGIKA'),
  /// Tara
  tara(17, 'TARA'),
  /// Karaka
  karaka(18, 'KARAKA'),
  /// Aayu
  aayu(19, 'AAYU'),
  /// Ashtakavarga
  ashtakavarga(20, 'ASHTAKAVARGA'),
  /// Panchaswara
  panchaswara(21, 'PANCHASWARA'),
  /// Chara
  chara(22, 'CHARA'),
  /// Narayana
  narayana(23, 'NARAYANA'),
  /// Padanadhamsa
  padanadhamsa(24, 'PADANADHAMSA'),
  /// Trikona
  trikona(25, 'TRIKONA'),
  /// Drig
  drig(26, 'DRIG'),
  /// Shoola
  shoola(27, 'SHOOLA'),
  /// Niryana Shoola
  niryanaShoola(28, 'NIRYANA_SHOOLA'),
  /// Mandooka
  mandooka(29, 'MANDOOKA'),
  /// Sthira
  sthira(30, 'STHIRA'),
  /// Sudasa
  sudasa(31, 'SUDASA'),
  /// Varnada
  varnada(32, 'VARNADA'),
  /// Yogardha
  yogardha(33, 'YOGARDHA'),
  /// Kalachakra
  kalachakra(34, 'KALACHAKRA'),
  /// Sudarshana Chakra
  sudarshanaChakra(35, 'SUDARSHANA_CHAKRA'),
  /// Patyayini
  patyayini(36, 'PATYAYINI'),
  /// Mudda
  mudda(37, 'MUDDA'),
  /// Varsha Narayana
  varshaNarayana(38, 'VARSHA_NARAYANA'),
  /// Varsha Yogini
  varshaYogini(39, 'VARSHA_YOGINI'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const DashaSystem(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'dasha_system.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static DashaSystem byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => DashaSystem.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static DashaSystem? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The strength schemes; rows live in the strength kernel. Members are the catalogue's ids; the full key id is `(TS_KIND_BALA_SCHEME << 16) | member`.
enum BalaScheme {
  /// Parashara
  parashara(0, 'PARASHARA'),
  /// Parashara Extended
  parasharaExtended(1, 'PARASHARA_EXTENDED'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const BalaScheme(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'bala_scheme.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static BalaScheme byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => BalaScheme.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static BalaScheme? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The Ashta Koota and the Dasha Koota extensions. Members are the catalogue's ids; the full key id is `(TS_KIND_KOOTA << 16) | member`.
enum Koota {
  /// Varna
  varna(0, 'VARNA'),
  /// Vashya
  vashya(1, 'VASHYA'),
  /// Tara
  tara(2, 'TARA'),
  /// Yoni
  yoni(3, 'YONI'),
  /// Graha Maitri
  grahaMaitri(4, 'GRAHA_MAITRI'),
  /// Gana
  gana(5, 'GANA'),
  /// Bhakoot
  bhakoot(6, 'BHAKOOT'),
  /// Nadi
  nadi(7, 'NADI'),
  /// Rajju
  rajju(8, 'RAJJU'),
  /// Vedha
  vedha(9, 'VEDHA'),
  /// Stree Deergha
  streeDeergha(10, 'STREE_DEERGHA'),
  /// Mahendra
  mahendra(11, 'MAHENDRA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Koota(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'koota.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Koota byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Koota.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Koota? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The Jaimini significators, in rank order. Members are the catalogue's ids; the full key id is `(TS_KIND_CHARA_KARAKA << 16) | member`.
enum CharaKaraka {
  /// Atmakaraka
  atmakaraka(0, 'ATMAKARAKA'),
  /// Amatyakaraka
  amatyakaraka(1, 'AMATYAKARAKA'),
  /// Bhratrikaraka
  bhratrikaraka(2, 'BHRATRIKARAKA'),
  /// Matrikaraka
  matrikaraka(3, 'MATRIKARAKA'),
  /// Putrakaraka
  putrakaraka(4, 'PUTRAKARAKA'),
  /// Gnatikaraka
  gnatikaraka(5, 'GNATIKARAKA'),
  /// Darakaraka
  darakaraka(6, 'DARAKARAKA'),
  /// Pitrikaraka
  pitrikaraka(7, 'PITRIKARAKA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const CharaKaraka(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'chara_karaka.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static CharaKaraka byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => CharaKaraka.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static CharaKaraka? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What a chart is a chart of. Members are the catalogue's ids; the full key id is `(TS_KIND_CHART_KIND << 16) | member`.
enum ChartKind {
  /// Natal
  natal(0, 'NATAL'),
  /// Transit
  transit(1, 'TRANSIT'),
  /// Event
  event(2, 'EVENT'),
  /// Prashna
  prashna(3, 'PRASHNA'),
  /// Return
  returnValue(4, 'RETURN'),
  /// Relocated
  relocated(5, 'RELOCATED'),
  /// Composite
  composite(6, 'COMPOSITE'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const ChartKind(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'chart_kind.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static ChartKind byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => ChartKind.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static ChartKind? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Derived points that behave like bodies; each has a formula in the module that owns it. Members are the catalogue's ids; the full key id is `(TS_KIND_POINT << 16) | member`.
enum Point {
  /// The ascendant
  lagna(0, 'LAGNA'),
  /// The midheaven
  mc(1, 'MC'),
  /// The descendant
  descendant(2, 'DESCENDANT'),
  /// The imum coeli
  ic(3, 'IC'),
  /// Bhava lagna
  bhavaLagna(4, 'BHAVA_LAGNA'),
  /// Hora lagna
  horaLagna(5, 'HORA_LAGNA'),
  /// Ghati lagna
  ghatiLagna(6, 'GHATI_LAGNA'),
  /// Vighati lagna
  vighatiLagna(7, 'VIGHATI_LAGNA'),
  /// Varnada lagna
  varnadaLagna(8, 'VARNADA_LAGNA'),
  /// Sree lagna
  sreeLagna(9, 'SREE_LAGNA'),
  /// Indu lagna
  induLagna(10, 'INDU_LAGNA'),
  /// Pranapada lagna
  pranapadaLagna(11, 'PRANAPADA_LAGNA'),
  /// Dhuma
  dhuma(12, 'DHUMA'),
  /// Vyatipata
  vyatipata(13, 'VYATIPATA'),
  /// Parivesha
  parivesha(14, 'PARIVESHA'),
  /// Indrachapa
  indrachapa(15, 'INDRACHAPA'),
  /// Upaketu
  upaketu(16, 'UPAKETU'),
  /// Gulika
  gulika(17, 'GULIKA'),
  /// Mandi
  mandi(18, 'MANDI'),
  /// Kala
  kala(19, 'KALA'),
  /// Mrityu
  mrityu(20, 'MRITYU'),
  /// Ardhaprahara
  ardhaprahara(21, 'ARDHAPRAHARA'),
  /// Yamaghantaka
  yamaghantaka(22, 'YAMAGHANTAKA'),
  /// Bhrigu bindu
  bhriguBindu(23, 'BHRIGU_BINDU'),
  /// The yogi point
  yogi(24, 'YOGI'),
  /// The avayogi point
  avayogi(25, 'AVAYOGI'),
  /// The sahayogi point
  sahayogi(26, 'SAHAYOGI'),
  /// Trisphuta
  trisphuta(27, 'TRISPHUTA'),
  /// Chatussphuta
  chatussphuta(28, 'CHATUSSPHUTA'),
  /// Panchasphuta
  panchasphuta(29, 'PANCHASPHUTA'),
  /// Prana sphuta
  pranaSphuta(30, 'PRANA_SPHUTA'),
  /// Deha sphuta
  dehaSphuta(31, 'DEHA_SPHUTA'),
  /// Mrityu sphuta
  mrityuSphuta(32, 'MRITYU_SPHUTA'),
  /// Beeja sphuta
  beejaSphuta(33, 'BEEJA_SPHUTA'),
  /// Kshetra sphuta
  kshetraSphuta(34, 'KSHETRA_SPHUTA'),
  /// Karakamsha
  karakamsha(35, 'KARAKAMSHA'),
  /// Swamsha
  swamsha(36, 'SWAMSHA'),
  /// Arudha of house 1
  a1(37, 'A1'),
  /// Arudha of house 2
  a2(38, 'A2'),
  /// Arudha of house 3
  a3(39, 'A3'),
  /// Arudha of house 4
  a4(40, 'A4'),
  /// Arudha of house 5
  a5(41, 'A5'),
  /// Arudha of house 6
  a6(42, 'A6'),
  /// Arudha of house 7
  a7(43, 'A7'),
  /// Arudha of house 8
  a8(44, 'A8'),
  /// Arudha of house 9
  a9(45, 'A9'),
  /// Arudha of house 10
  a10(46, 'A10'),
  /// Arudha of house 11
  a11(47, 'A11'),
  /// Arudha of house 12
  a12(48, 'A12'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Point(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'point.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Point byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Point.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Point? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The seven muhurta natures of the nakshatras. Members are the catalogue's ids; the full key id is `(TS_KIND_MUHURTA_NATURE << 16) | member`.
enum MuhurtaNature {
  /// Dhruva
  dhruva(0, 'DHRUVA'),
  /// Chara
  chara(1, 'CHARA'),
  /// Ugra
  ugra(2, 'UGRA'),
  /// Kshipra
  kshipra(3, 'KSHIPRA'),
  /// Mridu
  mridu(4, 'MRIDU'),
  /// Mishra
  mishra(5, 'MISHRA'),
  /// Tikshna
  tikshna(6, 'TIKSHNA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const MuhurtaNature(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'muhurta_nature.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static MuhurtaNature byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => MuhurtaNature.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static MuhurtaNature? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Movable, fixed and dual signs. Members are the catalogue's ids; the full key id is `(TS_KIND_MODALITY << 16) | member`.
enum Modality {
  /// Movable
  chara(0, 'CHARA'),
  /// Fixed
  sthira(1, 'STHIRA'),
  /// Dual
  dwiswabhava(2, 'DWISWABHAVA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Modality(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'modality.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Modality byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Modality.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Modality? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The natural benefic or malefic character of a graha. Members are the catalogue's ids; the full key id is `(TS_KIND_NATURE << 16) | member`.
enum Nature {
  /// Benefic
  benefic(0, 'BENEFIC'),
  /// Malefic
  malefic(1, 'MALEFIC'),
  /// Neutral
  neutral(2, 'NEUTRAL'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Nature(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'nature.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Nature byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Nature.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Nature? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The three gunas. Members are the catalogue's ids; the full key id is `(TS_KIND_GUNA << 16) | member`.
enum Guna {
  /// Sattva
  sattva(0, 'SATTVA'),
  /// Rajas
  rajas(1, 'RAJAS'),
  /// Tamas
  tamas(2, 'TAMAS'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Guna(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'guna.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Guna byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Guna.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Guna? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The eight directions. Members are the catalogue's ids; the full key id is `(TS_KIND_DIRECTION << 16) | member`.
enum Direction {
  /// East
  east(0, 'EAST'),
  /// West
  west(1, 'WEST'),
  /// North
  north(2, 'NORTH'),
  /// South
  south(3, 'SOUTH'),
  /// Northeast
  northeast(4, 'NORTHEAST'),
  /// Northwest
  northwest(5, 'NORTHWEST'),
  /// Southeast
  southeast(6, 'SOUTHEAST'),
  /// Southwest
  southwest(7, 'SOUTHWEST'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Direction(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'direction.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Direction byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Direction.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Direction? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Grammatical and classical gender. Members are the catalogue's ids; the full key id is `(TS_KIND_GENDER << 16) | member`.
enum Gender {
  /// Male
  male(0, 'MALE'),
  /// Female
  female(1, 'FEMALE'),
  /// Neutral
  neutral(2, 'NEUTRAL'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Gender(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'gender.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Gender byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Gender.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Gender? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The calendars the SDK ships. Members are the catalogue's ids; the full key id is `(TS_KIND_CALENDAR << 16) | member`.
enum Calendar {
  /// Gregorian
  gregorian(0, 'GREGORIAN'),
  /// Julian
  julian(1, 'JULIAN'),
  /// Mixed
  mixed(2, 'MIXED'),
  /// Iso Week
  isoWeek(3, 'ISO_WEEK'),
  /// Bikram Sambat
  bikramSambat(4, 'BIKRAM_SAMBAT'),
  /// Indian Lunisolar
  indianLunisolar(5, 'INDIAN_LUNISOLAR'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Calendar(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'calendar.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Calendar byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Calendar.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Calendar? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The era numbers a date may carry. Members are the catalogue's ids; the full key id is `(TS_KIND_ERA << 16) | member`.
enum Era {
  /// Vikrama
  vikrama(0, 'VIKRAMA'),
  /// Shaka
  shaka(1, 'SHAKA'),
  /// Kali
  kali(2, 'KALI'),
  /// Nepal Sambat
  nepalSambat(3, 'NEPAL_SAMBAT'),
  /// Buddha
  buddha(4, 'BUDDHA'),
  /// Kollam
  kollam(5, 'KOLLAM'),
  /// Bengali
  bengali(6, 'BENGALI'),
  /// The common era of the Gregorian and Julian calendars
  commonEra(7, 'COMMON_ERA'),
  /// Before the common era; year 1 BCE is astronomical year 0
  beforeCommonEra(8, 'BEFORE_COMMON_ERA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Era(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'era.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Era byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Era.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Era? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What kind of body a graha is. Members are the catalogue's ids; the full key id is `(TS_KIND_BODY_CLASS << 16) | member`.
enum BodyClass {
  /// Luminary
  luminary(0, 'LUMINARY'),
  /// Planet
  planet(1, 'PLANET'),
  /// Node
  node(2, 'NODE'),
  /// Outer
  outer(3, 'OUTER'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const BodyClass(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'body_class.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static BodyClass byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => BodyClass.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static BodyClass? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Odd and even signs. Members are the catalogue's ids; the full key id is `(TS_KIND_PARITY << 16) | member`.
enum Parity {
  /// Odd
  odd(0, 'ODD'),
  /// Even
  even(1, 'EVEN'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Parity(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'parity.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Parity byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Parity.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Parity? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// How a sign rises: head first, back first, or both. Members are the catalogue's ids; the full key id is `(TS_KIND_RISING << 16) | member`.
enum Rising {
  /// Sirshodaya
  sirshodaya(0, 'SIRSHODAYA'),
  /// Prishtodaya
  prishtodaya(1, 'PRISHTODAYA'),
  /// Ubhayodaya
  ubhayodaya(2, 'UBHAYODAYA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Rising(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'rising.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Rising byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Rising.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Rising? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The sex of a nakshatra's yoni animal. Members are the catalogue's ids; the full key id is `(TS_KIND_SEX << 16) | member`.
enum Sex {
  /// Male
  male(0, 'MALE'),
  /// Female
  female(1, 'FEMALE'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Sex(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'sex.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Sex byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Sex.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Sex? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The five tithi classes. Members are the catalogue's ids; the full key id is `(TS_KIND_TITHI_CLASS << 16) | member`.
enum TithiClass {
  /// Nanda
  nanda(0, 'NANDA'),
  /// Bhadra
  bhadra(1, 'BHADRA'),
  /// Jaya
  jaya(2, 'JAYA'),
  /// Rikta
  rikta(3, 'RIKTA'),
  /// Purna
  purna(4, 'PURNA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const TithiClass(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'tithi_class.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static TithiClass byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => TithiClass.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static TithiClass? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The auspiciousness class of a nitya yoga. Members are the catalogue's ids; the full key id is `(TS_KIND_AUSPICIOUSNESS << 16) | member`.
enum Auspiciousness {
  /// Auspicious
  auspicious(0, 'AUSPICIOUS'),
  /// Inauspicious
  inauspicious(1, 'INAUSPICIOUS'),
  /// Mixed
  mixed(2, 'MIXED'),
  /// Highly Inauspicious
  highlyInauspicious(3, 'HIGHLY_INAUSPICIOUS'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Auspiciousness(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'auspiciousness.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Auspiciousness byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Auspiciousness.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Auspiciousness? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Whether a house system is undefined at some latitudes. Members are the catalogue's ids; the full key id is `(TS_KIND_DEGENERACY << 16) | member`.
enum Degeneracy {
  /// None
  none(0, 'NONE'),
  /// Polar Undefined
  polarUndefined(1, 'POLAR_UNDEFINED'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Degeneracy(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'degeneracy.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Degeneracy byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Degeneracy.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Degeneracy? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// How an ayanamsha is defined or used. Members are the catalogue's ids; the full key id is `(TS_KIND_AYANAMSHA_CATEGORY << 16) | member`.
enum AyanamshaCategory {
  /// Vedic Standard
  vedicStandard(0, 'VEDIC_STANDARD'),
  /// Vedic Classical
  vedicClassical(1, 'VEDIC_CLASSICAL'),
  /// Star Anchored
  starAnchored(2, 'STAR_ANCHORED'),
  /// Western Sidereal
  westernSidereal(3, 'WESTERN_SIDEREAL'),
  /// Galactic
  galactic(4, 'GALACTIC'),
  /// Historical
  historical(5, 'HISTORICAL'),
  /// Epoch
  epoch(6, 'EPOCH'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AyanamshaCategory(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'ayanamsha_category.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AyanamshaCategory byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AyanamshaCategory.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AyanamshaCategory? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Which kernel a dasha system runs on. Members are the catalogue's ids; the full key id is `(TS_KIND_DASHA_FAMILY << 16) | member`.
enum DashaFamily {
  /// Udu
  udu(0, 'UDU'),
  /// Rashi
  rashi(1, 'RASHI'),
  /// Kalachakra
  kalachakra(2, 'KALACHAKRA'),
  /// Scale
  scale(3, 'SCALE'),
  /// Composition
  composition(4, 'COMPOSITION'),
  /// Own
  own(5, 'OWN'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const DashaFamily(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'dasha_family.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static DashaFamily byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => DashaFamily.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static DashaFamily? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The three wakefulness states. Members are the catalogue's ids; the full key id is `(TS_KIND_AVASTHA_JAGRADADI << 16) | member`.
enum AvasthaJagradadi {
  /// Jagrat
  jagrat(0, 'JAGRAT'),
  /// Swapna
  swapna(1, 'SWAPNA'),
  /// Sushupti
  sushupti(2, 'SUSHUPTI'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AvasthaJagradadi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'avastha_jagradadi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AvasthaJagradadi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AvasthaJagradadi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AvasthaJagradadi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The nine Deeptadi states. Members are the catalogue's ids; the full key id is `(TS_KIND_AVASTHA_DEEPTADI << 16) | member`.
enum AvasthaDeeptadi {
  /// Deepta
  deepta(0, 'DEEPTA'),
  /// Swastha
  swastha(1, 'SWASTHA'),
  /// Mudita
  mudita(2, 'MUDITA'),
  /// Shanta
  shanta(3, 'SHANTA'),
  /// Dina
  dina(4, 'DINA'),
  /// Dukhi
  dukhi(5, 'DUKHI'),
  /// Vikala
  vikala(6, 'VIKALA'),
  /// Khala
  khala(7, 'KHALA'),
  /// Kopa
  kopa(8, 'KOPA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AvasthaDeeptadi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'avastha_deeptadi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AvasthaDeeptadi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AvasthaDeeptadi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AvasthaDeeptadi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The six Lajjitadi states. Members are the catalogue's ids; the full key id is `(TS_KIND_AVASTHA_LAJJITADI << 16) | member`.
enum AvasthaLajjitadi {
  /// Lajjita
  lajjita(0, 'LAJJITA'),
  /// Garvita
  garvita(1, 'GARVITA'),
  /// Kshudha
  kshudha(2, 'KSHUDHA'),
  /// Trishita
  trishita(3, 'TRISHITA'),
  /// Mudita
  mudita(4, 'MUDITA'),
  /// Kshobhita
  kshobhita(5, 'KSHOBHITA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AvasthaLajjitadi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'avastha_lajjitadi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AvasthaLajjitadi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AvasthaLajjitadi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AvasthaLajjitadi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The twelve Sayanadi states. Members are the catalogue's ids; the full key id is `(TS_KIND_AVASTHA_SAYANADI << 16) | member`.
enum AvasthaSayanadi {
  /// Shayana
  shayana(0, 'SHAYANA'),
  /// Upaveshana
  upaveshana(1, 'UPAVESHANA'),
  /// Netrapani
  netrapani(2, 'NETRAPANI'),
  /// Prakashana
  prakashana(3, 'PRAKASHANA'),
  /// Gamana
  gamana(4, 'GAMANA'),
  /// Agamana
  agamana(5, 'AGAMANA'),
  /// Sabha
  sabha(6, 'SABHA'),
  /// Agama
  agama(7, 'AGAMA'),
  /// Bhojana
  bhojana(8, 'BHOJANA'),
  /// Nrityalipsa
  nrityalipsa(9, 'NRITYALIPSA'),
  /// Kautuka
  kautuka(10, 'KAUTUKA'),
  /// Nidra
  nidra(11, 'NIDRA'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const AvasthaSayanadi(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'avastha_sayanadi.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static AvasthaSayanadi byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => AvasthaSayanadi.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static AvasthaSayanadi? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What kind of derived point. Members are the catalogue's ids; the full key id is `(TS_KIND_POINT_FAMILY << 16) | member`.
enum PointFamily {
  /// Angle
  angle(0, 'ANGLE'),
  /// Special Lagna
  specialLagna(1, 'SPECIAL_LAGNA'),
  /// Upagraha Solar
  upagrahaSolar(2, 'UPAGRAHA_SOLAR'),
  /// Upagraha Day
  upagrahaDay(3, 'UPAGRAHA_DAY'),
  /// Arudha
  arudha(4, 'ARUDHA'),
  /// Sphuta
  sphuta(5, 'SPHUTA'),
  /// Jaimini
  jaimini(6, 'JAIMINI'),
  /// Midpoint
  midpoint(7, 'MIDPOINT'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const PointFamily(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'point_family.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static PointFamily byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => PointFamily.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static PointFamily? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The fixed stars and fixed directions the SDK places: the anchors of the star-anchored ayanamshas, the yogataras of the nakshatras, the bright stars of the fixed-star tradition, the galactic centre and the galactic poles, each with its ICRS astrometry at epoch J2000.0 (`03-design/astro-star-table.md`). Members are the catalogue's ids; the full key id is `(TS_KIND_STAR << 16) | member`.
enum Star {
  /// Sheratan, beta Arietis: the yogatara of Ashwini
  sheratan(0, 'SHERATAN'),
  /// Bharani, 41 Arietis: the yogatara of Bharani
  bharani(1, 'BHARANI'),
  /// Alcyone, eta Tauri: the yogatara of Krittika; the brightest of the Pleiades
  alcyone(2, 'ALCYONE'),
  /// Aldebaran, alpha Tauri: the yogatara of Rohini; a royal star
  aldebaran(3, 'ALDEBARAN'),
  /// Meissa, lambda Orionis: the yogatara of Mrigashira
  meissa(4, 'MEISSA'),
  /// Betelgeuse, alpha Orionis: the yogatara of Ardra
  betelgeuse(5, 'BETELGEUSE'),
  /// Pollux, beta Geminorum: the yogatara of Punarvasu
  pollux(6, 'POLLUX'),
  /// Asellus Australis, delta Cancri: the yogatara of Pushya; the anchor of the True Pushya and Sheoran ayanamshas
  asellusAustralis(7, 'ASELLUS_AUSTRALIS'),
  /// Ashlesha, epsilon Hydrae: the yogatara of Ashlesha
  ashlesha(8, 'ASHLESHA'),
  /// Regulus, alpha Leonis: the yogatara of Magha; a royal star
  regulus(9, 'REGULUS'),
  /// Zosma, delta Leonis: the yogatara of Purva Phalguni
  zosma(10, 'ZOSMA'),
  /// Denebola, beta Leonis: the yogatara of Uttara Phalguni
  denebola(11, 'DENEBOLA'),
  /// Algorab, delta Corvi: the yogatara of Hasta
  algorab(12, 'ALGORAB'),
  /// Spica, alpha Virginis: the yogatara of Chitra; the anchor of the True Chitra ayanamsha (Spica at 180°)
  spica(13, 'SPICA'),
  /// Arcturus, alpha Boötis: the yogatara of Swati
  arcturus(14, 'ARCTURUS'),
  /// Zubenelgenubi, alpha² Librae: the yogatara of Vishakha; the Surya Siddhanta names iota Librae instead
  zubenelgenubi(15, 'ZUBENELGENUBI'),
  /// Dschubba, delta Scorpii: the yogatara of Anuradha
  dschubba(16, 'DSCHUBBA'),
  /// Antares, alpha Scorpii: the yogatara of Jyeshtha; a royal star
  antares(17, 'ANTARES'),
  /// Shaula, lambda Scorpii: the yogatara of Mula; the anchor of the True Mula ayanamsha
  shaula(18, 'SHAULA'),
  /// Kaus Media, delta Sagittarii: the yogatara of Purva Ashadha
  kausMedia(19, 'KAUS_MEDIA'),
  /// Nunki, sigma Sagittarii: the yogatara of Uttara Ashadha
  nunki(20, 'NUNKI'),
  /// Vega, alpha Lyrae: the yogatara of Abhijit, the twenty-eighth nakshatra of the span table
  vega(21, 'VEGA'),
  /// Altair, alpha Aquilae: the yogatara of Shravana
  altair(22, 'ALTAIR'),
  /// Rotanev, beta Delphini: the yogatara of Dhanishtha
  rotanev(23, 'ROTANEV'),
  /// Hydor, lambda Aquarii: the yogatara of Shatabhisha
  hydor(24, 'HYDOR'),
  /// Markab, alpha Pegasi: the yogatara of Purva Bhadrapada
  markab(25, 'MARKAB'),
  /// Algenib, gamma Pegasi: the yogatara of Uttara Bhadrapada
  algenib(26, 'ALGENIB'),
  /// Revati, zeta Piscium A: the yogatara of Revati; the anchor of the True Revati ayanamsha (359°50′)
  revati(27, 'REVATI'),
  /// Alpheratz, alpha Andromedae
  alpheratz(28, 'ALPHERATZ'),
  /// Caph, beta Cassiopeiae
  caph(29, 'CAPH'),
  /// Ankaa, alpha Phoenicis
  ankaa(30, 'ANKAA'),
  /// Schedar, alpha Cassiopeiae
  schedar(31, 'SCHEDAR'),
  /// Diphda, beta Ceti
  diphda(32, 'DIPHDA'),
  /// Mirach, beta Andromedae
  mirach(33, 'MIRACH'),
  /// Achernar, alpha Eridani
  achernar(34, 'ACHERNAR'),
  /// Almach, gamma¹ Andromedae
  almach(35, 'ALMACH'),
  /// Hamal, alpha Arietis
  hamal(36, 'HAMAL'),
  /// Polaris, alpha Ursae Minoris
  polaris(37, 'POLARIS'),
  /// Menkar, alpha Ceti
  menkar(38, 'MENKAR'),
  /// Algol, beta Persei
  algol(39, 'ALGOL'),
  /// Mirfak, alpha Persei
  mirfak(40, 'MIRFAK'),
  /// Ain, epsilon Tauri
  ain(41, 'AIN'),
  /// Chamukuy, theta² Tauri
  chamukuy(42, 'CHAMUKUY'),
  /// Rigel, beta Orionis
  rigel(43, 'RIGEL'),
  /// Capella, alpha Aurigae
  capella(44, 'CAPELLA'),
  /// Bellatrix, gamma Orionis
  bellatrix(45, 'BELLATRIX'),
  /// Elnath, beta Tauri
  elnath(46, 'ELNATH'),
  /// Nihal, beta Leporis
  nihal(47, 'NIHAL'),
  /// Mintaka, delta Orionis
  mintaka(48, 'MINTAKA'),
  /// Alnilam, epsilon Orionis
  alnilam(49, 'ALNILAM'),
  /// Alnitak, zeta Orionis
  alnitak(50, 'ALNITAK'),
  /// Saiph, kappa Orionis
  saiph(51, 'SAIPH'),
  /// Menkalinan, beta Aurigae
  menkalinan(52, 'MENKALINAN'),
  /// Canopus, alpha Carinae
  canopus(53, 'CANOPUS'),
  /// Alhena, gamma Geminorum
  alhena(54, 'ALHENA'),
  /// Sirius, alpha Canis Majoris
  sirius(55, 'SIRIUS'),
  /// Adhara, epsilon Canis Majoris
  adhara(56, 'ADHARA'),
  /// Wezen, delta Canis Majoris
  wezen(57, 'WEZEN'),
  /// Aludra, eta Canis Majoris
  aludra(58, 'ALUDRA'),
  /// Castor, alpha Geminorum
  castor(59, 'CASTOR'),
  /// Procyon, alpha Canis Minoris
  procyon(60, 'PROCYON'),
  /// Naos, zeta Puppis
  naos(61, 'NAOS'),
  /// Tureis, rho Puppis
  tureis(62, 'TUREIS'),
  /// Avior, epsilon Carinae
  avior(63, 'AVIOR'),
  /// Alsephina, delta¹ Velorum
  alsephina(64, 'ALSEPHINA'),
  /// Suhail, lambda Velorum
  suhail(65, 'SUHAIL'),
  /// Miaplacidus, beta Carinae
  miaplacidus(66, 'MIAPLACIDUS'),
  /// Aspidiske, iota Carinae
  aspidiske(67, 'ASPIDISKE'),
  /// Alphard, alpha Hydrae
  alphard(68, 'ALPHARD'),
  /// Algieba, gamma Leonis
  algieba(69, 'ALGIEBA'),
  /// Merak, beta Ursae Majoris
  merak(70, 'MERAK'),
  /// Dubhe, alpha Ursae Majoris
  dubhe(71, 'DUBHE'),
  /// Phecda, gamma Ursae Majoris
  phecda(72, 'PHECDA'),
  /// Zaniah, eta Virginis
  zaniah(73, 'ZANIAH'),
  /// Acrux, alpha Crucis
  acrux(74, 'ACRUX'),
  /// Gacrux, gamma Crucis
  gacrux(75, 'GACRUX'),
  /// Mimosa, beta Crucis
  mimosa(76, 'MIMOSA'),
  /// Alioth, epsilon Ursae Majoris
  alioth(77, 'ALIOTH'),
  /// Vindemiatrix, epsilon Virginis
  vindemiatrix(78, 'VINDEMIATRIX'),
  /// Mizar, zeta Ursae Majoris
  mizar(79, 'MIZAR'),
  /// Heze, zeta Virginis
  heze(80, 'HEZE'),
  /// Alkaid, eta Ursae Majoris
  alkaid(81, 'ALKAID'),
  /// Hadar, beta Centauri
  hadar(82, 'HADAR'),
  /// Thuban, alpha Draconis
  thuban(83, 'THUBAN'),
  /// Menkent, theta Centauri
  menkent(84, 'MENKENT'),
  /// Syrma, iota Virginis
  syrma(85, 'SYRMA'),
  /// Seginus, gamma Boötis
  seginus(86, 'SEGINUS'),
  /// Rigil Kentaurus, alpha Centauri A
  rigilKentaurus(87, 'RIGIL_KENTAURUS'),
  /// Izar, epsilon Boötis
  izar(88, 'IZAR'),
  /// Zubeneschamali, beta Librae
  zubeneschamali(89, 'ZUBENESCHAMALI'),
  /// Alphecca, alpha Coronae Borealis
  alphecca(90, 'ALPHECCA'),
  /// Unukalhai, alpha Serpentis
  unukalhai(91, 'UNUKALHAI'),
  /// Fang, pi Scorpii
  fang(92, 'FANG'),
  /// Yed Prior, delta Ophiuchi
  yedPrior(93, 'YED_PRIOR'),
  /// Kornephoros, beta Herculis
  kornephoros(94, 'KORNEPHOROS'),
  /// Marfik, lambda Ophiuchi
  marfik(95, 'MARFIK'),
  /// Atria, alpha Trianguli Australis
  atria(96, 'ATRIA'),
  /// Sabik, eta Ophiuchi
  sabik(97, 'SABIK'),
  /// Rasalgethi, alpha Herculis
  rasalgethi(98, 'RASALGETHI'),
  /// Rasalhague, alpha Ophiuchi
  rasalhague(99, 'RASALHAGUE'),
  /// Sargas, theta Scorpii
  sargas(100, 'SARGAS'),
  /// Alnasl, gamma² Sagittarii
  alnasl(101, 'ALNASL'),
  /// Kaus Australis, epsilon Sagittarii
  kausAustralis(102, 'KAUS_AUSTRALIS'),
  /// Kaus Borealis, lambda Sagittarii
  kausBorealis(103, 'KAUS_BOREALIS'),
  /// Sheliak, beta Lyrae
  sheliak(104, 'SHELIAK'),
  /// Ascella, zeta Sagittarii
  ascella(105, 'ASCELLA'),
  /// Albaldah, pi Sagittarii
  albaldah(106, 'ALBALDAH'),
  /// Albireo, beta¹ Cygni
  albireo(107, 'ALBIREO'),
  /// Alshain, beta Aquilae
  alshain(108, 'ALSHAIN'),
  /// Algedi, alpha² Capricorni
  algedi(109, 'ALGEDI'),
  /// Sadr, gamma Cygni
  sadr(110, 'SADR'),
  /// Peacock, alpha Pavonis
  peacock(111, 'PEACOCK'),
  /// Deneb, alpha Cygni
  deneb(112, 'DENEB'),
  /// Aljanah, epsilon Cygni
  aljanah(113, 'ALJANAH'),
  /// Kitalpha, alpha Equulei
  kitalpha(114, 'KITALPHA'),
  /// Alderamin, alpha Cephei
  alderamin(115, 'ALDERAMIN'),
  /// Sadalsuud, beta Aquarii
  sadalsuud(116, 'SADALSUUD'),
  /// Enif, epsilon Pegasi
  enif(117, 'ENIF'),
  /// Sadalmelik, alpha Aquarii
  sadalmelik(118, 'SADALMELIK'),
  /// Alnair, alpha Gruis
  alnair(119, 'ALNAIR'),
  /// Sadalbari, mu Pegasi
  sadalbari(120, 'SADALBARI'),
  /// Skat, delta Aquarii
  skat(121, 'SKAT'),
  /// Fomalhaut, alpha Piscis Austrini: a royal star
  fomalhaut(122, 'FOMALHAUT'),
  /// Scheat, beta Pegasi
  scheat(123, 'SCHEAT'),
  /// Errai, gamma Cephei
  errai(124, 'ERRAI'),
  /// Sagittarius A*, the compact radio source at the galactic centre: the anchor of the four Galactic Centre ayanamshas
  sgrAStar(125, 'SGR_A_STAR'),
  /// The north galactic pole of the galactic coordinate system referred to the ICRS (Liu, Zhu and Zhang 2011): the anchor of the True and Mula galactic-equator ayanamshas
  galacticPole(126, 'GALACTIC_POLE'),
  /// The IAU 1958 north galactic pole (12h49m +27°24′ at B1950.0) referred to the ICRS: the anchor of the IAU 1958 galactic-equator ayanamsha
  galacticPoleIau1958(127, 'GALACTIC_POLE_IAU1958'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const Star(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'star.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static Star byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => Star.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static Star? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What kind of object a star-table member is. Members are the catalogue's ids; the full key id is `(TS_KIND_STAR_CLASS << 16) | member`.
enum StarClass {
  /// A star
  star(0, 'STAR'),
  /// A compact radio source, such as the galactic centre
  radioSource(1, 'RADIO_SOURCE'),
  /// A fixed direction on the sky without a body, such as a galactic pole
  direction(2, 'DIRECTION'),

  /// A member this build does not know: from a newer library, or
  /// registered at run time.
  unknown(-1, 'UNKNOWN');

  const StarClass(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The full key, as every pack and fixture spells it.
  String get fullKey => 'star_class.$key';

  /// The member with an id; one this build does not know is
  /// [unknown], so a `switch` over the result stays exhaustive.
  static StarClass byId(int id) =>
      values.firstWhere((member) => member.id == id, orElse: () => StarClass.unknown);

  /// The member with a key, or `null` for one this build does not know.
  static StarClass? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The status of a call, with the code it has at the C boundary.
enum Status {
  /// Success.
  ok(0, 'ok'),
  /// A value refused at construction, or a request that contradicts itself.
  invalidArg(-1, 'invalid-arg'),
  /// An instant or place outside the provider's or the calendar's coverage.
  outOfRange(-2, 'out-of-range'),
  /// The settings need something the provider does not declare.
  capability(-3, 'capability'),
  /// The provider failed; its own code and message are carried.
  provider(-4, 'provider'),
  /// A search hit its iteration cap.
  notConverged(-5, 'not-converged'),
  /// A registered but unimplemented variant, or an unknown key.
  unsupported(-6, 'unsupported'),
  /// A pack failed validation or targets another catalogue version.
  pack(-7, 'pack'),
  /// A batch, range or cache limit was exceeded.
  limit(-8, 'limit'),
  /// A struct or blob from an incompatible version.
  schemaVersion(-9, 'schema-version'),
  /// A panic caught at the boundary; never expected.
  internal(-10, 'internal');

  const Status(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Status byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Status'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Status? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The bodies the port knows: the ten classical bodies of modern
/// astrology and the four lunar points. Adapters map them to their own
/// numbering; a body an adapter cannot compute is reported per cell,
/// never guessed. The astrological grahas of the catalogue map onto these
/// under the node knob (Rahu is the mean or the true node).
///
/// ```
/// use teistro_port_ephemeris::Body;
///
/// assert_eq!(Body::MeanNode.key(), "MEAN_NODE");
/// assert_eq!("moon".parse::<Body>().ok(), Some(Body::Moon));
/// assert_eq!(Body::from_id(Body::Pluto.id()), Some(Body::Pluto));
/// assert!(!Body::TrueNode.has_distance());
/// ```
enum Body {
  /// The Sun.
  sun(0, 'sun'),
  /// The Moon.
  moon(1, 'moon'),
  /// Mercury.
  mercury(2, 'mercury'),
  /// Venus.
  venus(3, 'venus'),
  /// Mars.
  mars(4, 'mars'),
  /// Jupiter.
  jupiter(5, 'jupiter'),
  /// Saturn.
  saturn(6, 'saturn'),
  /// Uranus.
  uranus(7, 'uranus'),
  /// Neptune.
  neptune(8, 'neptune'),
  /// Pluto.
  pluto(9, 'pluto'),
  /// The mean ascending lunar node.
  meanNode(10, 'mean-node'),
  /// The true (osculating) ascending lunar node.
  trueNode(11, 'true-node'),
  /// The mean lunar apogee.
  meanApogee(12, 'mean-apogee'),
  /// The osculating lunar apogee.
  osculatingApogee(13, 'osculating-apogee');

  const Body(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Body byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Body'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Body? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The time scale of the instants in a request.
///
/// ```
/// use teistro_port_ephemeris::TimeScale;
///
/// assert_eq!(TimeScale::from_id(TimeScale::Tt.id()), Some(TimeScale::Tt));
/// assert_eq!(TimeScale::Ut1.to_string(), "UT1");
/// ```
enum TimeScale {
  /// Universal Time (UT1), the scale of civil time and of rise and set.
  ut1(0, 'ut1'),
  /// Terrestrial Time, the scale of the ephemerides.
  tt(1, 'tt');

  const TimeScale(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static TimeScale byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a TimeScale'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static TimeScale? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Where a position is seen from.
enum Centre {
  /// The centre of the Earth.
  geocentric(0, 'geocentric'),
  /// An observer on the Earth; the request carries the place.
  topocentric(1, 'topocentric'),
  /// The centre of the Sun.
  heliocentric(2, 'heliocentric'),
  /// The solar-system barycentre.
  barycentric(3, 'barycentric');

  const Centre(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Centre byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Centre'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Centre? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The equinox and equator the coordinates refer to.
enum Equinox {
  /// The equinox of date.
  ofDate(0, 'of-date'),
  /// The J2000.0 equinox.
  j2000(1, 'j2000');

  const Equinox(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Equinox byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Equinox'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Equinox? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// The coordinate system of a position.
enum Coordinates {
  /// Ecliptic longitude and latitude.
  ecliptic(0, 'ecliptic'),
  /// Right ascension and declination.
  equatorial(1, 'equatorial');

  const Coordinates(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Coordinates byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Coordinates'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Coordinates? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// How a date was resolved (`docs/03-design/calendar-bikram-sambat.md`).
enum Resolution {
  /// A mathematical definition; exact by construction.
  defined(0, 'defined'),
  /// From the authority's published table.
  tabular(1, 'tabular'),
  /// Computed by the SDK's engine outside the table's range.
  computed(2, 'computed'),
  /// Inside the range and the table and the engine disagree; the table
  /// was followed and the engine's month and day are reported beside it.
  divergent(3, 'divergent');

  const Resolution(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Resolution byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Resolution'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Resolution? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// A time scale of the conversions; the first two ids are the port's.
enum Scale {
  /// Universal Time (UT1).
  ut1(0, 'ut1'),
  /// Terrestrial Time.
  tt(1, 'tt'),
  /// Coordinated Universal Time, through the leap-second table from
  /// 1972 and read as UT1 before it.
  utc(2, 'utc');

  const Scale(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Scale byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Scale'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Scale? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What a zone specification names.
enum ZoneKind {
  /// A zone of the embedded database, by IANA name.
  iana(0, 'iana'),
  /// A fixed offset from UTC.
  fixed(1, 'fixed'),
  /// Local mean time at a longitude.
  localMean(2, 'local-mean');

  const ZoneKind(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static ZoneKind byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a ZoneKind'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static ZoneKind? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Where a resolution's offset came from.
enum ZoneSource {
  /// The zone database.
  iana(0, 'iana'),
  /// Local mean time from the longitude.
  localMean(1, 'local-mean'),
  /// A fixed offset the consumer stated.
  manual(2, 'manual');

  const ZoneSource(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static ZoneSource byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a ZoneSource'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static ZoneSource? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Which rules produced the offset.
enum ZoneEra {
  /// An offset the zone applies in the database's own year.
  current(0, 'current'),
  /// An offset from the zone's earlier rules.
  historical(1, 'historical'),
  /// Before the zone's first rule: the database's local-mean-time stub.
  beforeRules(2, 'before-rules');

  const ZoneEra(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static ZoneEra byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a ZoneEra'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static ZoneEra? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What the daylight-saving policy did.
enum Dst {
  /// The civil time was unambiguous.
  none(0, 'none'),
  /// The civil time fell in a gap and was shifted forward past it.
  gap(1, 'gap'),
  /// The civil time fell in an overlap and one occurrence was chosen.
  overlap(2, 'overlap');

  const Dst(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Dst byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Dst'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Dst? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// Which occurrence an overlap resolved to.
enum Chosen {
  /// The first occurrence, the earlier instant.
  earlier(0, 'earlier'),
  /// The second occurrence, the later instant.
  later(1, 'later');

  const Chosen(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static Chosen byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a Chosen'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static Chosen? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// A warning of a resolution; `warnings` in the resolution is a bit set,
/// bit `n` for the warning with value `n`.
enum ZoneWarning {
  /// The offset is not one the zone applies today.
  offsetDiffersFromCurrentRules(0, 'offset-differs-from-current-rules'),
  /// The civil time occurred twice; the policy chose.
  dstAmbiguous(1, 'dst-ambiguous'),
  /// The civil time did not exist; the policy shifted it forward.
  dstGapShifted(2, 'dst-gap-shifted'),
  /// A leap second (23:59:60) was folded onto the following midnight.
  leapSecondFolded(3, 'leap-second-folded'),
  /// The instant lies beyond the leap-second table's word.
  leapTableExpired(4, 'leap-table-expired'),
  /// The time was not given; the policy supplied one.
  timeUnknownFallback(5, 'time-unknown-fallback');

  const ZoneWarning(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static ZoneWarning byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a ZoneWarning'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static ZoneWarning? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

/// What produced a Delta T value.
enum DeltaTSource {
  /// Interpolated in the IERS table.
  table(0, 'table'),
  /// From a model, outside the table.
  model(1, 'model'),
  /// Through the leap-second table: TT less UTC, exact.
  leapSeconds(2, 'leap-seconds'),
  /// Supplied by the consumer.
  custom(3, 'custom');

  const DeltaTSource(this.id, this.key);

  /// The id the C boundary carries.
  final int id;

  /// The key every pack, fixture and serialised result spells it with.
  final String key;

  /// The member with an id.
  ///
  /// Throws [ArgumentError] for an id this build does not know, because
  /// a value outside a closed set is a fault, not a state.
  static DeltaTSource byId(int id) => values.firstWhere(
        (member) => member.id == id,
        orElse: () => throw ArgumentError.value(id, 'id', 'not a DeltaTSource'),
      );

  /// The member with a key, or `null` for one this build does not know.
  static DeltaTSource? byKey(String key) {
    final wanted = key.contains('.') ? key.split('.').last : key;
    for (final member in values) {
      if (member.key == wanted) return member;
    }
    return null;
  }
}

